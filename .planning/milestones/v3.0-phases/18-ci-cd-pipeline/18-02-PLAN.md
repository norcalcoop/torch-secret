---
phase: 18-ci-cd-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - render.yaml
  - README.md
autonomous: true
requirements: [CICD-03]

must_haves:
  truths:
    - "Merging to main triggers an automatic deployment to Render.com only after CI checks pass"
    - "The GitHub repository has a CI status badge visible in the README"
  artifacts:
    - path: "render.yaml"
      provides: "Render Blueprint with CI-gated auto-deploy"
      contains: "autoDeployTrigger"
    - path: "README.md"
      provides: "Minimal README with CI badge"
      contains: "actions/workflows/ci.yml/badge"
  key_links:
    - from: "render.yaml (autoDeployTrigger)"
      to: ".github/workflows/ci.yml"
      via: "Render waits for GitHub Actions checks to pass before deploying"
      pattern: "autoDeployTrigger:\\s*checksPass"
    - from: "README.md"
      to: ".github/workflows/ci.yml"
      via: "Badge URL references workflow file"
      pattern: "github.com/norcalcoop/secureshare/actions/workflows/ci.yml"
---

<objective>
Configure Render.com to auto-deploy only after CI checks pass, and create a minimal README with the CI status badge.

Purpose: Ensure broken code never reaches production (CICD-03), and provide at-a-glance CI status for repository visitors. The README is intentionally minimal — Phase 19 expands it into a full project README.
Output: Updated `render.yaml`, new `README.md`.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-ci-cd-pipeline/18-RESEARCH.md
@render.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Render auto-deploy trigger and create CI badge README</name>
  <files>render.yaml, README.md</files>
  <action>
1. **Update `render.yaml`** — Add `autoDeployTrigger: checksPass` to the web service definition. Insert it after the `region: oregon` line and before `healthCheckPath`. This tells Render to wait for all GitHub Actions checks to pass before deploying on merge to main. Without this, Render would deploy immediately on push, potentially shipping broken code.

   The updated service block should include:
   ```yaml
   services:
     - type: web
       name: secureshare
       runtime: docker
       plan: free
       region: oregon
       autoDeployTrigger: checksPass
       healthCheckPath: /api/health
       # ... rest unchanged
   ```

   Do NOT change any other fields in render.yaml. Do NOT remove existing comments.

2. **Create `README.md`** at project root — This is a minimal placeholder README. Phase 19 (GitHub Repository Polish, REPO-01) will expand it into a full README with screenshots, install instructions, architecture overview, and license badge. For now, include only:

   ```markdown
   # SecureShare

   Zero-knowledge, one-time secret sharing.

   [![CI](https://github.com/norcalcoop/secureshare/actions/workflows/ci.yml/badge.svg)](https://github.com/norcalcoop/secureshare/actions/workflows/ci.yml)
   ```

   The badge URL uses the confirmed GitHub remote: `norcalcoop/secureshare`. The badge will show "no status" until the first workflow run completes, which is expected.
  </action>
  <verify>
- `render.yaml` contains `autoDeployTrigger: checksPass` under the web service
- `render.yaml` is valid YAML (`python3 -c "import yaml; yaml.safe_load(open('render.yaml'))"`)
- `README.md` exists at project root
- `README.md` contains the CI badge markdown with correct repo URL
- All other `render.yaml` fields are unchanged (diff shows only the autoDeployTrigger addition)
  </verify>
  <done>Render.yaml configured to deploy only after CI passes, README displays CI status badge linking to the workflow</done>
</task>

</tasks>

<verification>
1. `render.yaml` has `autoDeployTrigger: checksPass` on the web service
2. No other `render.yaml` fields were modified
3. `README.md` exists with project name and CI badge
4. Badge URL correctly references `norcalcoop/secureshare` and `ci.yml` workflow
</verification>

<success_criteria>
- Render will wait for CI checks before deploying on merge to main
- Repository visitors can see CI status at a glance in the README
- Phase 19 can expand the README without conflicting with this minimal version
</success_criteria>

<output>
After completion, create `.planning/phases/18-ci-cd-pipeline/18-02-SUMMARY.md`
</output>
