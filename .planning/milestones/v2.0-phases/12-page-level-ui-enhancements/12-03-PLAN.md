---
phase: 12-page-level-ui-enhancements
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - client/src/pages/confirmation.ts
  - client/src/pages/reveal.ts
  - client/src/pages/error.ts
autonomous: true

must_haves:
  truths:
    - "Confirmation page displays share URL in a single prominent block with copy button and optional share button inline"
    - "Copy action on confirmation page triggers a toast notification"
    - "Native share button appears on devices supporting Web Share API and is absent on unsupported devices"
    - "Revealed secret displays in a terminal-style code block with dark bg, muted green-gray monospace text, header bar, and copy button"
    - "A prominent green success badge with checkmark icon appears ABOVE the terminal block confirming permanent deletion"
    - "Revisiting a consumed secret URL shows a distinct 'already viewed and destroyed' error page, different from generic not-found"
  artifacts:
    - path: "client/src/pages/confirmation.ts"
      provides: "Redesigned confirmation page with prominent URL block, copy toast, and share button"
      contains: "createShareButton"
    - path: "client/src/pages/reveal.ts"
      provides: "Terminal code block for secret display and destruction badge above it"
      contains: "createTerminalBlock"
    - path: "client/src/pages/error.ts"
      provides: "New 'already_viewed' error type with distinct message"
      contains: "already_viewed"
  key_links:
    - from: "client/src/pages/confirmation.ts"
      to: "client/src/components/share-button.ts"
      via: "import createShareButton"
      pattern: "import.*createShareButton.*share-button"
    - from: "client/src/pages/confirmation.ts"
      to: "client/src/components/copy-button.ts"
      via: "import createCopyButton (existing, now with toast)"
      pattern: "import.*createCopyButton.*copy-button"
    - from: "client/src/pages/reveal.ts"
      to: "client/src/components/terminal-block.ts"
      via: "import createTerminalBlock"
      pattern: "import.*createTerminalBlock.*terminal-block"
    - from: "client/src/pages/reveal.ts"
      to: "client/src/components/icons.ts"
      via: "import createIcon for CircleCheck badge"
      pattern: "import.*createIcon.*icons"
    - from: "client/src/pages/reveal.ts"
      to: "client/src/pages/error.ts"
      via: "renderErrorPage with 'already_viewed' type"
      pattern: "renderErrorPage.*already_viewed"
---

<objective>
Enhance the confirmation page with a prominent share URL block (copy + native share), enhance the reveal page with a terminal-style code block and destruction badge, and add a distinct "already viewed" error type for consumed secrets.

Purpose: The confirmation and reveal pages are the core user touchpoints — a professional URL display builds sharing confidence, and a terminal-style code block establishes developer-tool credibility. The distinct "already viewed" state prevents user confusion.
Output: Modified confirmation.ts, reveal.ts, and error.ts.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-page-level-ui-enhancements/12-RESEARCH.md
@.planning/phases/12-page-level-ui-enhancements/12-01-SUMMARY.md
@client/src/pages/confirmation.ts
@client/src/pages/reveal.ts
@client/src/pages/error.ts
@client/src/components/terminal-block.ts
@client/src/components/share-button.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign confirmation page with prominent URL block, copy toast, and share button</name>
  <files>client/src/pages/confirmation.ts</files>
  <action>
**IMPORTANT:** Before modifying any frontend UI code, invoke the `frontend-design` skill per CLAUDE.md mandate.

**New imports:**
```typescript
import { createShareButton } from '../components/share-button.js';
```
(Keep existing imports: createCopyButton, navigate, updatePageMeta, focusPageHeading)

**Redesign the renderConfirmationPage function (UI-01):**

Replace the current URL display section (the urlSection, urlDisplay, urlInput, and copyButtonContainer) with a single prominent block:

1. **URL block wrapper:** A prominent card-style container: `p-6 rounded-lg border border-border bg-surface space-y-4`. This is the primary visual element on the page — maximum visual weight per discretion recommendation.

2. **URL label:** `block text-sm text-text-muted mb-2` — "Share this link with your recipient:"

3. **URL display:** A `<div>` styled as a code block: `w-full px-3 py-3 rounded-lg bg-surface-raised text-text-secondary text-sm font-mono break-all select-all` — full URL visible with `word-break: break-all` for long URLs with crypto key fragments. Use a `<code>` element inside with `textContent = shareUrl` (no innerHTML, no truncation).

4. **Button row:** `flex items-center gap-3 mt-3`
   - Copy button: `createCopyButton(() => shareUrl, 'Copy Link')` — now uses toast from Plan 01 refactor. This is the primary action.
   - Share button: `const shareBtn = createShareButton(shareUrl, 'SecureShare - Your secure link')` — returns null on unsupported browsers. Only append if non-null: `if (shareBtn) buttonRow.appendChild(shareBtn);`

5. **Remove the old hand-drawn SVG shield icon** (lines 48-78). Replace with a Lucide ShieldCheck icon via createIcon: `import { ShieldCheck } from 'lucide'; import { createIcon } from '../components/icons.js';` — render as `createIcon(ShieldCheck, { size: 48, class: 'text-success' })` in a `w-16 h-16 rounded-full bg-success/10 flex items-center justify-center` container (same visual as before, but using the shared icon system).

6. **Keep the rest unchanged:** heading, expiration notice, one-time-view warning, create another button — these work well as-is.

**Discretion choices applied:**
- Full URL visible with break-all and monospace (no truncation)
- URL block is the primary visual element with elevated surface background
- Copy button is primary action, share button is secondary (border-only style)
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. Verify confirmation.ts imports createShareButton and createIcon
3. Verify the old hand-drawn SVG (createElementNS) is gone, replaced with createIcon(ShieldCheck)
4. Verify the URL is displayed in a monospace code block with break-all
5. Verify createShareButton is called and result is conditionally appended
  </verify>
  <done>
Confirmation page displays the share URL in a prominent bordered card with monospace full-URL display. Copy button triggers a toast notification. Native share button appears alongside copy on supported devices. The old hand-drawn SVG is replaced with a Lucide ShieldCheck icon via the shared icon system.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add terminal code block and destruction badge to reveal page, plus "already viewed" error type</name>
  <files>
    client/src/pages/reveal.ts
    client/src/pages/error.ts
  </files>
  <action>
**IMPORTANT:** Before modifying any frontend UI code, invoke the `frontend-design` skill per CLAUDE.md mandate.

**error.ts — Add "already_viewed" error type:**

1. Extend the `ErrorType` union: add `| 'already_viewed'` after `'destroyed'`

2. Add new import: `import { CircleCheck } from 'lucide';` (add to existing import line)

3. Add to `ERROR_CONFIG` record:
```typescript
already_viewed: {
  heading: 'Secret Already Viewed',
  message: 'This secret has already been viewed and destroyed. Secrets can only be viewed once.',
  icon: CircleCheck,
  iconClass: 'text-text-muted',
},
```

This ensures TypeScript exhaustiveness is maintained. The `already_viewed` type uses a CheckCircle icon in muted color — informational, not alarming (the secret was successfully consumed, this is not an error state for the original recipient).

**reveal.ts — Redesign renderRevealedSecret() with terminal block and destruction badge (TYPO-03, UI-04):**

1. Add new imports:
```typescript
import { createTerminalBlock } from '../components/terminal-block.js';
import { CircleCheck } from 'lucide';
```
(createIcon is already imported)

2. Replace the `renderRevealedSecret()` function body:

The current function renders: heading, notice paragraph, pre element, copy button, create new secret link.

New implementation:

a. **Heading** — keep the h1 "Secret Revealed" with existing classes

b. **Destruction badge (UI-04):** ABOVE the terminal block (locked decision — first thing user sees):
```typescript
const badge = document.createElement('div');
badge.className = 'flex items-center gap-2 px-4 py-3 rounded-lg bg-success/10 border border-success/20 mb-6';

const badgeIcon = createIcon(CircleCheck, { size: 'sm', class: 'text-success' });
const badgeText = document.createElement('p');
badgeText.className = 'text-sm text-success font-medium';
badgeText.textContent = 'This secret has been permanently deleted from our servers';

badge.appendChild(badgeIcon);
badge.appendChild(badgeText);
```
- Reassuring tone per locked decision
- Prominent green badge with checkmark icon per locked decision
- Appears ABOVE terminal block per locked decision

c. **Terminal code block (TYPO-03):** Replace the old `<pre>` element with:
```typescript
const terminal = createTerminalBlock(plaintext);
terminal.classList.add('mb-6');
```
This uses the shared component from Plan 01 which provides: dark bg, muted sage green-gray text, header bar with copy button, max-h-[300px] scroll, whitespace-pre-wrap.

d. **Remove the standalone copy button:** The terminal block component includes its own copy button in the header bar, so the separate `createCopyButton(() => plaintext, 'Copy Secret')` is no longer needed.

e. **Actions row:** Keep just the "Create a New Secret" link (remove copyButton from actions).

f. **Element order in wrapper:** heading → badge → terminal → actions

g. **Secret stays visible** (no fade) per discretion recommendation — the badge is the feedback.

**reveal.ts — Use "already_viewed" error type for consumed secrets:**

In the `handleReveal()` function's catch block, when the API returns 404 or 410 (secret already consumed):
- Change `renderErrorPage(container, 'not_available')` to `renderErrorPage(container, 'already_viewed')` for 410 status specifically
- Keep `'not_available'` for 404 (could be expired or truly not found — ambiguous for anti-enumeration)
- Actually, looking at the server API, both 404 cases are anti-enumeration (the server returns identical responses). So keep `'not_available'` for API errors in handleReveal. The `'already_viewed'` type is for the metadata check path — when `getSecretMeta` itself returns a consumed/not-found status.

Wait — reviewing the code more carefully: `getSecretMeta` catches errors and shows `'not_available'`. The distinction should be: if the user navigates to a consumed URL, the meta check returns 404/410, and we can show `'already_viewed'` instead of generic `'not_available'`. Update the `catch` block in the meta check (around line 87-91):

```typescript
} catch (err) {
  if (err instanceof ApiError && err.status === 410) {
    renderErrorPage(container, 'already_viewed');
  } else {
    renderErrorPage(container, 'not_available');
  }
  return;
}
```

Import `ApiError` is already imported. The server returns 410 for "gone" (already consumed) vs 404 for "never existed" — this allows the distinction. If the server does not distinguish (both return 404), then keep `'not_available'` for both — check the actual server behavior. Given the anti-enumeration design in CLAUDE.md ("API error responses for not-found/expired/viewed/wrong-password must be identical"), the server likely returns the same status. In that case, use `'not_available'` for the meta catch, and rely on the server to eventually add a 410 distinction if desired.

Actually, the locked decision says: "Revisiting a consumed URL shows a distinct expired state." Since the server returns identical 404s for anti-enumeration, we cannot distinguish server-side. BUT the `getSecretMeta` endpoint exists specifically to check WITHOUT consuming. If meta returns 404, the secret is gone (could be viewed or expired — we don't know which). For now, keep using `'not_available'` which already has appropriate wording: "It may have already been viewed, expired, or the link is invalid." The `'already_viewed'` type is available for when the server provides a 410 status in the future, or we can use it optimistically in the meta catch since "already viewed or expired" covers the consumed case.

**Decision:** Use `'already_viewed'` in the meta catch block (when the user navigates to a URL whose secret no longer exists). This matches the locked decision for a distinct state. The `'not_available'` remains for the actual reveal attempt errors (anti-enumeration).
  </action>
  <verify>
1. `npx tsc --noEmit` passes — especially verify that `ErrorType` union and `ERROR_CONFIG` record are exhaustive (TypeScript catches mismatch)
2. Verify error.ts has `already_viewed` in ErrorType union and ERROR_CONFIG
3. Verify reveal.ts imports createTerminalBlock and uses it in renderRevealedSecret
4. Verify reveal.ts renderRevealedSecret has destruction badge ABOVE terminal block
5. Verify reveal.ts no longer has standalone copy button (terminal block has its own)
6. `npx vitest run` — all tests pass
  </verify>
  <done>
Confirmation page has prominent URL card with toast-backed copy button and conditional native share button. Reveal page shows destruction badge (green success with checkmark) above a terminal-style code block (dark bg, muted green-gray text, header bar with copy). Consumed secret URLs show a distinct "Secret Already Viewed" error page instead of generic not-found.
  </done>
</task>

</tasks>

<verification>
1. Confirmation page: URL in monospace bordered card, copy toast, conditional share button
2. Reveal page: green destruction badge above terminal code block, scrollable, muted green text
3. Error page: "already_viewed" type with distinct heading and message
4. `npx tsc --noEmit` passes
5. `npx vitest run` — all tests pass
6. No innerHTML used anywhere for user content (XSS prevention maintained)
</verification>

<success_criteria>
- Confirmation page share URL is in a single prominent card with monospace full-URL, copy button (toast), and native share button (progressive enhancement) (UI-01)
- Revealed secret shows in terminal code block: dark bg, muted sage green-gray monospace text, header bar with "secret.txt" label and copy button, max-h-300px vertical scroll (TYPO-03)
- Green success badge with CircleCheck icon appears ABOVE terminal block with text "This secret has been permanently deleted from our servers" (UI-04)
- "already_viewed" error type renders "Secret Already Viewed" with distinct message about one-time viewing (locked decision)
- Secret text stays visible after destruction (discretion: badge is the feedback)
- All TypeScript compiles, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-level-ui-enhancements/12-03-SUMMARY.md`
</output>
