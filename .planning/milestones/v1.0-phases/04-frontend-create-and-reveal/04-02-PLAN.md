---
phase: 04-frontend-create-and-reveal
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - client/src/pages/create.ts
  - client/src/pages/confirmation.ts
  - client/src/components/expiration-select.ts
autonomous: true

must_haves:
  truths:
    - "User lands on / and sees a textarea with max 10,000 character limit"
    - "User sees an expiration dropdown with 1h/24h/7d/30d options, defaulting to 24h"
    - "User clicks 'Create Secure Link' and the secret is encrypted in the browser, sent to the API, and a shareable URL is built with the key in the fragment"
    - "After creation, user sees confirmation page with the shareable URL and one-click copy button"
    - "Confirmation page shows the expiration timestamp in a human-readable format"
    - "Password field is present but disabled (placeholder for Phase 5)"
  artifacts:
    - path: "client/src/pages/create.ts"
      provides: "Create page with textarea, expiration selector, password field, submit handler"
      exports: ["renderCreatePage"]
      min_lines: 80
    - path: "client/src/pages/confirmation.ts"
      provides: "Confirmation page with share URL, copy button, expiration display"
      exports: ["renderConfirmationPage"]
      min_lines: 40
    - path: "client/src/components/expiration-select.ts"
      provides: "Dropdown component for expiration time selection"
      exports: ["createExpirationSelect"]
  key_links:
    - from: "client/src/pages/create.ts"
      to: "client/src/crypto"
      via: "import { encrypt } for browser-side encryption"
      pattern: "import.*encrypt.*crypto"
    - from: "client/src/pages/create.ts"
      to: "client/src/api/client.ts"
      via: "import { createSecret } for API call"
      pattern: "import.*createSecret.*api"
    - from: "client/src/pages/create.ts"
      to: "client/src/pages/confirmation.ts"
      via: "programmatic navigation after successful creation"
      pattern: "renderConfirmationPage"
    - from: "client/src/pages/confirmation.ts"
      to: "client/src/components/copy-button.ts"
      via: "copy button for share URL"
      pattern: "createCopyButton"
---

<objective>
Build the complete secret creation flow: landing page with textarea/options/submit, browser-side encryption, API submission, and confirmation page with shareable link and copy button.

Purpose: This is the sender's entire journey -- the first half of the product. Without it, no secrets can be created.

Output: A working create flow where typing a secret, selecting expiration, and clicking submit produces a confirmation page with a copyable share URL.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-create-and-reveal/04-RESEARCH.md
@.planning/phases/04-frontend-create-and-reveal/04-01-SUMMARY.md
@client/src/crypto/index.ts
@client/src/crypto/types.ts
@shared/types/api.ts
@client/src/router.ts
@client/src/api/client.ts
@client/src/components/copy-button.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create expiration select component and create page with form, encryption, and API submission</name>
  <files>
    client/src/components/expiration-select.ts
    client/src/pages/create.ts
  </files>
  <action>
    1. Create `client/src/components/expiration-select.ts`:
       - Export function `createExpirationSelect(): HTMLSelectElement`
       - Creates a select element with id="expiration"
       - Options: { value: '1h', label: '1 hour' }, { value: '24h', label: '24 hours', selected: true }, { value: '7d', label: '7 days' }, { value: '30d', label: '30 days' }
       - Tailwind classes: w-full, px-3, py-2, border, border-gray-300, rounded-lg, bg-white, text-gray-900, focus:ring-2, focus:ring-primary-500, focus:outline-none

    2. Create `client/src/pages/create.ts`:
       - Export async function `renderCreatePage(container: HTMLElement): Promise<void>`
       - Build the page using DOM APIs (createElement, appendChild) -- NEVER innerHTML with user content
       - Layout structure:
         a. Header: h1 "Share a Secret" with subtext "End-to-end encrypted. One-time view. No accounts."
         b. Textarea: id="secret-text", maxlength=10000, placeholder="Paste your secret here...", rows=6, required
            - Character counter below: "0 / 10,000" that updates on input
         c. Expiration section: Label "Expires after" + createExpirationSelect()
         d. Password section (Phase 5 placeholder):
            - Collapsible "Advanced options" toggle (details/summary or button)
            - Password input field with type="password", disabled attribute, placeholder="Password protection (coming soon)"
            - This satisfies CREA-03 UI presence while deferring Phase 5 logic
         e. Submit button: "Create Secure Link", primary styling (bg-primary-600, text-white, w-full, py-3, rounded-lg, font-semibold, hover:bg-primary-700, transition-colors)
            - Disabled state while submitting (prevent double-submit)
         f. Error display area: hidden div that shows API errors or validation errors

       - Submit handler (on form submit or button click):
         a. Get text from textarea, validate non-empty
         b. Get selected expiration value
         c. Show loading state: disable button, change text to "Encrypting..."
         d. Call `encrypt(text)` from client/src/crypto -- this encrypts entirely in the browser
         e. Change button text to "Sending..."
         f. Call `createSecret(result.payload.ciphertext, expiresIn)` from client/src/api/client
         g. Build share URL: `${window.location.origin}/secret/${response.id}#${result.keyBase64Url}`
         h. Call renderConfirmationPage(container, shareUrl, response.expiresAt) -- swap content in same container
         i. On error: show error message in error area, restore button state

       - Tailwind styling: responsive layout, proper spacing (space-y-6), focus states, mobile-friendly touch targets (min 44px)
       - All user-visible text uses textContent, never innerHTML
  </action>
  <verify>
    Run `npx vite build` -- the create page should compile. Run `npm run test:run` to verify no regressions.
  </verify>
  <done>
    - Create page renders textarea, expiration dropdown, disabled password field, and submit button
    - Character counter updates on input
    - Submit handler encrypts in browser, posts to API, builds share URL with key in fragment
    - Errors display in error area without crashing
  </done>
</task>

<task type="auto">
  <name>Task 2: Create confirmation page with share URL display, copy button, and expiration timestamp</name>
  <files>
    client/src/pages/confirmation.ts
  </files>
  <action>
    1. Create `client/src/pages/confirmation.ts`:
       - Export function `renderConfirmationPage(container: HTMLElement, shareUrl: string, expiresAt: string): void`
       - Clear container first (while container.firstChild... pattern)
       - Layout structure:
         a. Success icon/indicator: a checkmark or shield icon (use a simple SVG inline or Unicode character, not an external icon library)
         b. Heading: "Your Secure Link is Ready"
         c. Share URL display:
            - A readonly text input or styled div containing the full share URL
            - The URL should be selectable but not editable
            - Use textContent to set the URL (it's not user-generated content, but good habit)
         d. Copy button: use createCopyButton(() => shareUrl, "Copy Link") from components/copy-button
            - Position next to or below the URL display
         e. Expiration notice: "This link expires on {formatted date}" or "Expires in {relative time}"
            - Parse expiresAt ISO string, format as locale-aware date/time: new Date(expiresAt).toLocaleString()
         f. Warning text: "This link can only be viewed once. Once opened, the secret is permanently destroyed."
         g. "Create Another" link/button that navigates back to / using the router's navigate function

       - Styling: centered layout, success-themed (green accents for checkmark), clear visual hierarchy
       - IMPORTANT: This page is rendered programmatically (not via URL navigation). If the user refreshes, they should return to the create page (the key is gone from memory). The router handles this because / is the default route and /confirmation is not a registered route.
       - Import navigate from '../router' for the "Create Another" button

    2. Wire the confirmation page into the create page flow:
       - The create page's submit handler (Task 1) already calls renderConfirmationPage directly
       - This is a state transition within the same page lifecycle, NOT a URL change
       - No router changes needed -- confirmation is rendered by replacing container children
  </action>
  <verify>
    Run `npx vite build` -- should compile successfully with the confirmation page. Run `npm run test:run` to verify no regressions.
  </verify>
  <done>
    - Confirmation page displays the share URL in a selectable format
    - Copy button copies URL to clipboard with "Copied!" feedback
    - Expiration timestamp displayed in human-readable format
    - "Create Another" navigates back to create page
    - Page refresh returns to create page (confirmation is state-based, not URL-based)
  </done>
</task>

</tasks>

<verification>
- Create page renders all required form elements (textarea, expiration, password placeholder, submit)
- Textarea enforces 10,000 character max with visible counter
- Expiration defaults to 24h with all 4 options available
- Submit encrypts client-side using Phase 1 crypto module, sends to Phase 2 API
- Share URL has format: origin/secret/{id}#{base64urlKey}
- Confirmation shows URL, copy button, expiration, and "create another" option
- No innerHTML used for any user-provided or server-provided content
</verification>

<success_criteria>
- Complete sender flow: type secret -> select expiration -> submit -> see confirmation with copyable link
- Encryption happens in browser (encrypt import from crypto module)
- API submission uses typed client (createSecret from api/client)
- Share URL contains key in fragment (never in query string or path)
- Confirmation page is state-based (not URL-based) -- refresh returns to create page
- Password field present but disabled (Phase 5 placeholder)
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-create-and-reveal/04-02-SUMMARY.md`
</output>
