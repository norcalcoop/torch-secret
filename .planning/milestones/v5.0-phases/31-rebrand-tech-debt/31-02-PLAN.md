---
phase: 31-rebrand-tech-debt
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docker-compose.yml
  - render.yaml
  - .github/workflows/ci.yml
  - server/src/config/env.ts
  - vite.config.ts
  - README.md
autonomous: true
requirements:
  - BRAND-03
  - BRAND-04
  - TECH-01

must_haves:
  truths:
    - "package.json name field reads 'torch-secret'"
    - "docker-compose.yml service names, container names, and env vars reflect Torch Secret brand"
    - "render.yaml service names, DB/Redis refs, and inline comments use torch-secret naming"
    - "CI workflow test and e2e jobs include all required env vars with placeholder values that pass Zod validation"
    - "Docker build in CI tags image as torch-secret:ci-test"
    - "README H1 reads 'Torch Secret' with live-site badge linking to torchsecret.com"
    - "README tech stack section reflects current v4.0 stack (Better Auth, Drizzle, Pino, Resend, PostHog)"
    - "README setup section is Docker-first"
    - "Lucide upgraded to 0.575.0; alias removed if ESM entry point is fixed"
    - "server/src/config/env.ts APP_URL has a .default('http://localhost:3000') fallback"
  artifacts:
    - path: "package.json"
      provides: "Updated package name"
      contains: "torch-secret"
    - path: ".github/workflows/ci.yml"
      provides: "CI env vars for integration tests"
      contains: "BETTER_AUTH_SECRET"
    - path: "README.md"
      provides: "Updated project README"
      contains: "Torch Secret"
    - path: "docker-compose.yml"
      provides: "Updated service names"
      contains: "torch-secret"
    - path: "server/src/config/env.ts"
      provides: "APP_URL with local-dev default"
      contains: "localhost:3000"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "server/src/config/env.ts"
      via: "env vars matching Zod EnvSchema required fields"
      pattern: "BETTER_AUTH_SECRET|BETTER_AUTH_URL|RESEND_API_KEY|RESEND_FROM_EMAIL"
    - from: "server/src/config/env.ts"
      to: "APP_URL default"
      via: "Zod .default() fallback"
      pattern: "localhost:3000"
---

<objective>
Update infrastructure files (package.json, Docker Compose, Render deploy config, CI workflow) with the Torch Secret brand, add all missing required CI env vars so integration tests no longer fail from Zod validation errors, add a local-dev default to APP_URL in env.ts, upgrade Lucide to 0.575.0 and remove the vite.config alias workaround if the ESM fix is confirmed, and refresh README.md with the Torch Secret name, live-site badge, and current v4.0 tech stack.

Purpose: Infrastructure files are the foundation that CI, deployment, and developer onboarding depend on. Brand-correct, complete CI env vars are a blocker for any future CI run. README is the public face of the repository.
Output: All infrastructure files updated with Torch Secret brand; CI passes with placeholder env vars; APP_URL defaults to localhost:3000; Lucide upgraded with alias removed (or noted as deferred if bug persists); README refreshed for public launch.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-rebrand-tech-debt/31-CONTEXT.md
@.planning/phases/31-rebrand-tech-debt/31-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename brand in package.json, Docker Compose, render.yaml; add CI env vars; update APP_URL default in env.ts</name>
  <files>
    package.json
    docker-compose.yml
    render.yaml
    .github/workflows/ci.yml
    server/src/config/env.ts
  </files>
  <action>
    These files are tightly coordinated — BRAND-04 and TECH-01 both touch ci.yml. Make all changes in this single task to avoid conflicts.

    **package.json:**
    Read file. Change: `"name": "secureshare"` → `"name": "torch-secret"`. No other changes. Idiomatic npm kebab-case per CONTEXT.md.

    **docker-compose.yml:**
    Read file. Apply:
    - Service names: rename any `secureshare-*` pattern to `torch-secret-*` (e.g., `secureshare-app` → `torch-secret-app`, `secureshare-db` → `torch-secret-db`)
    - Container names: same pattern if present
    - `RESEND_FROM_EMAIL: 'SecureShare <noreply@localhost>'` → `RESEND_FROM_EMAIL: 'Torch Secret <noreply@localhost>'`
    - Any other `SecureShare` occurrences in the file

    NOTE: The postgres service credentials (`POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`) may use `secureshare` as the database/user name — these are infrastructure identifiers (the literal postgres username and database name on disk), NOT brand surfaces. Do NOT rename them. They are internal to the Docker-compose postgres service definition and unrelated to the Torch Secret brand.

    **render.yaml:**
    Read file. Apply:
    - Line 1 comment: update `SecureShare` → `Torch Secret` if present
    - Line 15 service name: `secureshare` → `torch-secret`
    - Line 24 DB ref: `secureshare-db` → `torch-secret-db`
    - Line 29 Redis ref: `secureshare-redis` → `torch-secret-redis`
    - Line 43 comment for BETTER_AUTH_URL: update the example URL comment from `https://secureshare.onrender.com` → `https://torchsecret.onrender.com` (this is a brand surface — a developer instruction showing the example URL format)
    - Line 50 `RESEND_FROM_EMAIL` comment if present: update brand name `SecureShare` → `Torch Secret`
    - Line 77 Redis service name: `secureshare-redis` → `torch-secret-redis`
    - Line 83 DB name: `secureshare-db` → `torch-secret-db`

    **`.github/workflows/ci.yml`:**
    Read file. Apply two changes:

    1. Docker build job: update image tag from `secureshare:ci-test` → `torch-secret:ci-test` wherever it appears (line ~167).

    2. Add missing required env vars to BOTH the `test` job and the `e2e` job env blocks. Current test job has only `DATABASE_URL` and `NODE_ENV`. Add these five vars:
    ```yaml
    BETTER_AUTH_SECRET: ci-test-secret-placeholder-must-be-at-least-32-chars
    BETTER_AUTH_URL: http://localhost:3000
    APP_URL: http://localhost:3000
    RESEND_API_KEY: re_placeholder
    RESEND_FROM_EMAIL: "Torch Secret <noreply@torchsecret.com>"
    ```
    Note: `BETTER_AUTH_SECRET` must be ≥32 characters (`z.string().min(32)` in Zod schema). The value above is 49 chars. `RESEND_FROM_EMAIL` is `z.string().min(1)`. `RESEND_API_KEY` is `z.string().min(1)`.

    The postgres service credentials (`POSTGRES_USER: secureshare`, `POSTGRES_PASSWORD: secureshare`, `POSTGRES_DB: secureshare`) and `DATABASE_URL: postgresql://secureshare:secureshare@localhost:5432/secureshare` must NOT be changed — these are the actual postgres service credentials inside the CI runner, not brand strings. Changing them would break the database connection used by the test runner.

    Also update any POSTHOG env var placeholders if present (check for `POSTHOG_API_KEY` or similar in the existing env block).

    **server/src/config/env.ts:**
    Read file. Find the `APP_URL` field in the Zod schema. It is currently `z.string().url().optional()`. Change it to use a `.default('http://localhost:3000')` fallback:
    ```typescript
    APP_URL: z.string().url().default('http://localhost:3000'),
    ```
    This makes APP_URL always defined (never undefined) in application code while still being overridable in production. Do NOT change any other env var definitions. Do NOT alter the `toAppUrl()` helper function or any other code in this file.

    After all edits: `grep -n "SecureShare" package.json docker-compose.yml render.yaml .github/workflows/ci.yml` — expect zero results. Note: lowercase `secureshare` postgres credentials in ci.yml and docker-compose.yml are intentionally left unchanged.
  </action>
  <verify>
    Run: `grep -n "SecureShare" package.json docker-compose.yml render.yaml .github/workflows/ci.yml server/src/config/env.ts` — must return zero results.
    Run: `grep "torch-secret" package.json` — must show `"name": "torch-secret"`.
    Run: `grep "BETTER_AUTH_SECRET" .github/workflows/ci.yml` — must show the placeholder value in both test and e2e job blocks.
    Run: `grep "RESEND_FROM_EMAIL" .github/workflows/ci.yml` — must show "Torch Secret" value in both job blocks.
    Run: `grep "torchsecret.onrender.com" render.yaml` — must show updated example URL comment.
    Run: `grep "APP_URL" server/src/config/env.ts` — must show `.default('http://localhost:3000')`.

    Note on postgres credentials: `grep "secureshare" .github/workflows/ci.yml docker-compose.yml` will still return results for POSTGRES_USER/PASSWORD/DB and DATABASE_URL — this is expected and correct. These are internal postgres service credentials, not brand surfaces.
  </verify>
  <done>
    Infrastructure files use torch-secret naming for all brand surfaces. CI workflow test and e2e jobs have all required Zod-valid placeholder env vars. Docker build tags image as torch-secret:ci-test. render.yaml example URL comment updated to torchsecret.onrender.com. server/src/config/env.ts APP_URL defaults to http://localhost:3000. Postgres credentials unchanged (correct behavior).
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Lucide to 0.575.0, remove alias if ESM fixed; refresh README.md</name>
  <files>
    vite.config.ts
    README.md
  </files>
  <action>
    **Lucide upgrade:**
    1. Run: `npm install lucide@latest`
       This installs 0.575.0 (latest as of 2026-02-22).
    2. Run: `npm run build:client`
       If the build succeeds without errors:
       - Read `vite.config.ts`
       - Remove the `resolve: { alias: { lucide: 'lucide/dist/esm/lucide/src/lucide.js' } }` block entirely
       - Run `npm run build:client` again to confirm icons still render without the alias
       - If second build also succeeds: alias removal is complete
       If the build fails at step 2 (Lucide ESM entry point still broken):
       - Keep the alias in vite.config.ts unchanged
       - Note in SUMMARY: "Lucide 0.575.0 installed but ESM entry point bug persists; alias retained"
       - Still proceed with the README update regardless

    **README.md refresh:**
    Read the current README.md first. Write a fully refreshed README with the following structure:

    ```markdown
    # Torch Secret

    [![CI](https://github.com/norcalcoop/secureshare/actions/workflows/ci.yml/badge.svg)](https://github.com/norcalcoop/secureshare/actions/workflows/ci.yml)
    [![Live](https://img.shields.io/badge/live-torchsecret.com-orange)](https://torchsecret.com)

    Zero-knowledge, one-time secret sharing. Paste sensitive text, get an encrypted link, share it — the secret self-destructs after one view. No accounts required. The encryption key lives exclusively in the URL fragment and never reaches the server.
    ```

    Structure the README with these sections (content and length at Claude's discretion for developer appeal):
    - **How it works** — brief zero-knowledge model explanation (key in URL fragment, server stores only ciphertext)
    - **Features** — reflect v4.0 state: anonymous + account hybrid, secret dashboard, EFF Diceware passphrase protection, password protection, expiration options, email notification on view, PostHog analytics (privacy-safe)
    - **Tech stack** — accurate for current codebase: Node.js 24, TypeScript 5.9 (ESM), Express 5, Drizzle ORM, PostgreSQL 17, Better Auth 1.x, Resend, PostHog, Pino, Vite 7, Tailwind CSS 4, Vitest 4, Playwright, Docker
    - **Getting started** — Docker-first as primary path (`docker compose up`), manual PostgreSQL as secondary note. Include `cp .env.example .env` step.
    - **Development commands** — same commands as CLAUDE.md (npm run dev:server, dev:client, test:run, etc.)
    - **Architecture** — brief security boundary description (browser vs server, server vs DB). Zero-knowledge invariant mention.
    - **Contributing** — link to CONTRIBUTING.md

    Zero-knowledge architecture positioning: use it as the headline concept (per Claude's discretion — most compelling for developers).
    Badge paths: keep `norcalcoop/secureshare` for CI badge (repo rename not in scope per RESEARCH.md open question #2). Add the new live-site shields.io badge.
    Screenshot alt text: if any screenshots are referenced, update alt text from "SecureShare" → "Torch Secret".
  </action>
  <verify>
    Run: `grep "SecureShare\|secureshare" README.md` — must return zero results.
    Run: `grep "Torch Secret" README.md` — must show H1 heading match.
    Run: `grep "torchsecret.com" README.md` — must show live-site badge and description link.
    Run: `grep "lucide" package.json` — must show version 0.575.0 (or close to latest).
    Run: `npm run build:client` — must complete without errors.
  </verify>
  <done>
    README.md reads "Torch Secret" with live-site badge and accurate v4.0 tech stack. Lucide upgraded to 0.575.0. vite.config.ts alias removed if ESM fix confirmed, or retained with a note in SUMMARY if bug persists.
  </done>
</task>

</tasks>

<verification>
Final checks:

1. `grep -n "SecureShare" package.json docker-compose.yml render.yaml .github/workflows/ci.yml README.md` — zero results.

2. `grep "BETTER_AUTH_SECRET\|BETTER_AUTH_URL\|RESEND_API_KEY\|RESEND_FROM_EMAIL" .github/workflows/ci.yml` — all four vars present in both test and e2e job blocks.

3. `npm run build:client` — exits 0 (Lucide upgrade did not break the build).

4. `grep "\"name\"" package.json` — returns `"name": "torch-secret"`.

5. `grep "APP_URL" server/src/config/env.ts` — shows `.default('http://localhost:3000')`.

6. `grep "torchsecret.onrender.com" render.yaml` — shows updated example URL comment on the BETTER_AUTH_URL line.

Note: `grep "secureshare" .github/workflows/ci.yml docker-compose.yml` will still return results — postgres service credentials (`POSTGRES_USER/PASSWORD/DB` and `DATABASE_URL`) are intentionally preserved as-is. These are postgres service identifiers, not brand strings.
</verification>

<success_criteria>
- package.json name field: "torch-secret"
- Docker Compose and render.yaml: torch-secret service/container/DB/Redis names throughout; render.yaml BETTER_AUTH_URL comment updated to torchsecret.onrender.com
- CI workflow: BETTER_AUTH_SECRET, BETTER_AUTH_URL, APP_URL, RESEND_API_KEY, RESEND_FROM_EMAIL present in test and e2e env blocks with valid placeholder values; image tagged torch-secret:ci-test; postgres credentials unchanged
- server/src/config/env.ts: APP_URL uses .default('http://localhost:3000')
- Lucide 0.575.0 installed; vite.config.ts alias removed if ESM fixed (deferred with SUMMARY note if not)
- README.md: H1 "Torch Secret", live-site badge for torchsecret.com, accurate v4.0 features and tech stack, Docker-first setup instructions
</success_criteria>

<output>
After completion, create `.planning/phases/31-rebrand-tech-debt/31-02-SUMMARY.md`
</output>
