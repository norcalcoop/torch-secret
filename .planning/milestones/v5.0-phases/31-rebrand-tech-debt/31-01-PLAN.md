---
phase: 31-rebrand-tech-debt
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - client/index.html
  - client/src/router.ts
  - client/src/components/layout.ts
  - client/src/pages/confirmation.ts
  - client/src/pages/terms.ts
  - client/src/pages/privacy.ts
  - client/public/robots.txt
  - client/public/sitemap.xml
  - client/public/site.webmanifest
  - client/src/analytics/posthog.ts
  - client/src/api/client.ts
  - client/src/app.ts
  - client/src/crypto/encrypt.ts
  - client/src/crypto/keys.ts
  - client/src/crypto/encoding.ts
  - client/src/crypto/decrypt.ts
  - client/src/crypto/index.ts
  - client/src/crypto/types.ts
  - client/src/crypto/constants.ts
  - server/src/auth.ts
  - server/src/services/notification.service.ts
  - server/src/server.ts
  - .github/ISSUE_TEMPLATE/feature-request.yml
  - scripts/screenshots.ts
  - .planning/INVARIANTS.md
  - .planning/PROJECT.md
  - .env.example
  - CONTRIBUTING.md
  - SECURITY.md
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02

must_haves:
  truths:
    - "Every HTML <title>, OG tag, header logo text, and email sender string reads 'Torch Secret', not 'SecureShare'"
    - "All canonical URLs, OG image URLs, sitemap entries, and JSON-LD url fields use torchsecret.com domain"
    - "No occurrence of 'SecureShare' or 'secureshare.example.com' remains in any active source file (outside .planning/milestones/ archive)"
    - "Static files (index.html, sitemap.xml, robots.txt, site.webmanifest) hardcode torchsecret.com directly — correct because static files cannot read runtime env vars"
    - "INVARIANTS.md and PROJECT.md title and brand references reflect Torch Secret"
  artifacts:
    - path: "client/index.html"
      provides: "Updated HTML title, OG tags, JSON-LD, and canonical URL"
      contains: "Torch Secret"
    - path: "client/src/components/layout.ts"
      provides: "Updated header wordmark"
      contains: "Torch Secret"
    - path: "client/public/sitemap.xml"
      provides: "Updated sitemap domain"
      contains: "torchsecret.com"
    - path: "client/public/robots.txt"
      provides: "Updated sitemap URL"
      contains: "torchsecret.com"
    - path: "server/src/auth.ts"
      provides: "Updated email subjects"
      contains: "Torch Secret"
    - path: ".planning/INVARIANTS.md"
      provides: "Updated active planning doc"
      contains: "Torch Secret"
    - path: ".planning/PROJECT.md"
      provides: "Updated active planning doc"
      contains: "Torch Secret"
  key_links:
    - from: "client/src/router.ts"
      to: "document.title and OG meta tags"
      via: "updateMeta() calls with brand strings"
      pattern: "Torch Secret"
    - from: "server/src/auth.ts"
      to: "Resend email subjects"
      via: "sendEmail() calls"
      pattern: "Torch Secret"
---

<objective>
Rename every user-facing occurrence of "SecureShare" to "Torch Secret" and every occurrence of "secureshare.example.com" to "torchsecret.com" across client source files, server source files, static public files, and active planning documentation.

Purpose: Establish the Torch Secret brand in all user-visible surfaces — HTML titles, OG tags, email subjects, in-app text, static metadata files, and file-level comments — before any new v5.0 feature code is written.
Output: All active source files updated with the Torch Secret brand; no "SecureShare" string or "secureshare.example.com" domain remains in any served or compiled output.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-rebrand-tech-debt/31-CONTEXT.md
@.planning/phases/31-rebrand-tech-debt/31-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename brand strings in user-facing HTML, TypeScript pages, and static files</name>
  <files>
    client/index.html
    client/src/router.ts
    client/src/components/layout.ts
    client/src/pages/confirmation.ts
    client/src/pages/terms.ts
    client/src/pages/privacy.ts
    client/public/robots.txt
    client/public/sitemap.xml
    client/public/site.webmanifest
  </files>
  <action>
    Read each file in turn and apply these replacements. Do NOT touch any file in .planning/milestones/ — those are read-only historical archives.

    **ARCHITECTURAL RULE for this task:** Static files (index.html, sitemap.xml, robots.txt) MUST hardcode `https://torchsecret.com` directly. This is the correct and intended approach — these files are served as-is by Vite/Express and cannot read runtime env vars. Hardcoding the production domain in static files is explicitly called out as correct in RESEARCH.md (lines 192-194) and reconciles with CONTEXT.md line 32 ("no hardcoded secureshare.example.com or torchsecret.com strings") which applies to server-rendered code, not static assets. The CONTEXT.md intent is: server-side code uses env.APP_URL; static served files use the literal domain.

    **client/index.html** — 12 occurrences to update (read file first, verify exact line numbers match research):
    - Line 19 canonical: `https://secureshare.example.com/` → `https://torchsecret.com/`
    - Line 20 title: `SecureShare - Share Secrets Securely` → `Torch Secret - Share Secrets Securely`
    - Line 22 og:title: `SecureShare - Zero-Knowledge Secret Sharing` → `Torch Secret - Zero-Knowledge Secret Sharing`
    - Line 25 og:url: `https://secureshare.example.com/` → `https://torchsecret.com/`
    - Line 26 og:image: `https://secureshare.example.com/og-image.png` → `https://torchsecret.com/og-image.png`
    - Line 29 og:image:alt: `SecureShare - Zero-knowledge...` → `Torch Secret - Zero-knowledge...`
    - Line 30 og:site_name: `SecureShare` → `Torch Secret`
    - Line 33 twitter:title: `SecureShare - Zero-Knowledge...` → `Torch Secret - Zero-Knowledge...`
    - Line 45 JSON-LD name: `"name": "SecureShare"` → `"name": "Torch Secret"`
    - Line 46 JSON-LD url: `"url": "https://secureshare.example.com/"` → `"url": "https://torchsecret.com/"`
    - Line 84 noscript h1: `SecureShare` → `Torch Secret`
    - Line 107 noscript text: `JavaScript is required to use SecureShare` → `JavaScript is required to use Torch Secret`

    **client/src/router.ts** — update all `document.title` strings and OG meta content strings (approximately lines 67, 131, 134, 138, 141, 202, 212, 222, 232, 242, 252, 262, 292). Replace `SecureShare` → `Torch Secret` in every `document.title =` assignment and every `updateMeta()` / `updateOgTags()` call that passes brand name strings. CRITICAL: Do NOT change the conditional logic of `updateOgTags(true)` — only change the string values "SecureShare" to "Torch Secret". Preserve the existing privacy-protecting pattern where secret routes get a generic OG title.

    **client/src/components/layout.ts** — Line 65: `wordmark.textContent = 'SecureShare'` → `wordmark.textContent = 'Torch Secret'`

    **client/src/pages/confirmation.ts** — update the shareButton call: `'SecureShare - Your secure link'` → `'Torch Secret - Your secure link'`

    **client/src/pages/terms.ts** — replace all occurrences of `SecureShare` with `Torch Secret` in page text content (service name references in the terms body)

    **client/src/pages/privacy.ts** — replace all occurrences of `SecureShare` with `Torch Secret` in page text content

    **client/public/robots.txt** — Line 6: `Sitemap: https://secureshare.example.com/sitemap.xml` → `Sitemap: https://torchsecret.com/sitemap.xml`

    **client/public/sitemap.xml** — Line 4: `<loc>https://secureshare.example.com/</loc>` → `<loc>https://torchsecret.com/</loc>`

    **client/public/site.webmanifest** — Lines 2-3: `"name": "SecureShare"` → `"name": "Torch Secret"`, `"short_name": "SecureShare"` → `"short_name": "Torch Secret"`

    After all edits, run grep audit to verify: `grep -r "SecureShare\|secureshare\.example\.com" client/ --include="*.{ts,html,json,xml,txt,webmanifest}"` — expect zero results.
  </action>
  <verify>
    Run: `grep -r "SecureShare\|secureshare\.example\.com" client/ --include="*.{ts,html,json,xml,txt,webmanifest}"` — must return zero results.
    Run: `grep -r "Torch Secret" client/index.html` — must show at least 6 matches (title, OG tags, JSON-LD, noscript).
    Run: `grep "torchsecret.com" client/public/sitemap.xml client/public/robots.txt client/index.html` — all three files must contain the new domain.
  </verify>
  <done>
    Zero "SecureShare" or "secureshare.example.com" occurrences remain in the client/ directory. Static files reference torchsecret.com directly. User-facing pages all read "Torch Secret".
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename brand in comment-only client source files (crypto, analytics, app entrypoints)</name>
  <files>
    client/src/analytics/posthog.ts
    client/src/api/client.ts
    client/src/app.ts
    client/src/crypto/encrypt.ts
    client/src/crypto/keys.ts
    client/src/crypto/encoding.ts
    client/src/crypto/decrypt.ts
    client/src/crypto/index.ts
    client/src/crypto/types.ts
    client/src/crypto/constants.ts
  </files>
  <action>
    These files contain only file-level comments (JSDoc or inline) referencing "SecureShare". Update the comment text only — do NOT alter any implementation code, function signatures, type definitions, or logic.

    **client/src/analytics/posthog.ts** — update file-level comment (line 2 or similar): `SecureShare` → `Torch Secret`

    **client/src/api/client.ts** — update file-level comment referencing `SecureShare` → `Torch Secret`

    **client/src/app.ts** — update file-level comment referencing `SecureShare` → `Torch Secret`

    **client/src/crypto/*.ts** (encrypt.ts, keys.ts, encoding.ts, decrypt.ts, index.ts, types.ts, constants.ts) — update file-level comments. These are JSDoc/comment-only changes; do not alter any implementation code whatsoever.

    After all edits: `grep -r "SecureShare\|secureshare\.example\.com" client/src/analytics/ client/src/api/ client/src/crypto/ client/src/app.ts` — expect zero results.
  </action>
  <verify>
    Run: `grep -r "SecureShare" client/src/analytics/ client/src/api/client.ts client/src/app.ts client/src/crypto/` — must return zero results.
    Run: `grep -r "Torch Secret" client/src/crypto/` — must show at least one match confirming the update landed.
  </verify>
  <done>
    All file-level comments in crypto/, analytics/, and client app entrypoints read "Torch Secret". No implementation code was modified — only comment strings changed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rename brand in server sources, CONTRIBUTING.md, SECURITY.md, issue template, scripts, INVARIANTS.md, PROJECT.md, and .env.example</name>
  <files>
    server/src/auth.ts
    server/src/services/notification.service.ts
    server/src/server.ts
    .github/ISSUE_TEMPLATE/feature-request.yml
    scripts/screenshots.ts
    .planning/INVARIANTS.md
    .planning/PROJECT.md
    .env.example
    CONTRIBUTING.md
    SECURITY.md
  </files>
  <action>
    Read each file and apply these targeted replacements:

    **server/src/auth.ts** — two email subject lines:
    - `'Reset your SecureShare password'` → `'Reset your Torch Secret password'`
    - `'Verify your SecureShare email'` → `'Verify your Torch Secret email'`
    Note: The "Workaround" comment in auth.ts (Better Auth verifications key mapping) is a legitimate workaround annotation — do NOT touch it. Only update the two brand-name email subject strings.

    **server/src/services/notification.service.ts** — Lines 24-26 (email subject and body):
    - Subject: `'Your SecureShare secret was viewed'` → `'Your Torch Secret secret was viewed'`
    - Body text: any occurrence of `SecureShare` → `Torch Secret`

    **server/src/server.ts** — logger startup message (Line 10 approx):
    - `'SecureShare server started'` → `'Torch Secret server started'`

    **scripts/screenshots.ts** — update the comment and any sample message string referencing `SecureShare` → `Torch Secret`

    **.github/ISSUE_TEMPLATE/feature-request.yml** — update the 1 occurrence of `SecureShare` in prose

    **.planning/INVARIANTS.md** — update title and brand references from "SecureShare" → "Torch Secret". This is an active planning document (referenced from CLAUDE.md and schema.ts), not a historical archive. Update all prose references but do NOT modify the enforcement table content (the technical rules stay the same — only the brand name in headings and prose changes).

    **.planning/PROJECT.md** — Line 20 reads: `SecureShare is a production-ready, zero-knowledge web application...`. Update this sentence to read: `Torch Secret is a production-ready, zero-knowledge web application...`. Also update line 8 if it still contains `SecureShare` (verify on read — it may already be updated from earlier work). Do NOT modify any other content in PROJECT.md.

    **.env.example** — Line 31: `RESEND_FROM_EMAIL=SecureShare <noreply@yourdomain.com>` → `RESEND_FROM_EMAIL=Torch Secret <noreply@torchsecret.com>`. Also add `APP_URL` entry with comment after the existing URL-related env vars:
    ```
    # Base URL for the application (used in OAuth callbacks and email links)
    # Production: https://torchsecret.com
    # Local dev override: http://localhost:5173 (Vite dev server)
    APP_URL=https://torchsecret.com
    ```

    **CONTRIBUTING.md** — replace the 4 occurrences of `SecureShare` with `Torch Secret` in prose

    **SECURITY.md** — replace the 3 occurrences of `SecureShare` with `Torch Secret` in prose

    After all edits, run grep audit: `grep -r "SecureShare\|secureshare\.example\.com" server/ CONTRIBUTING.md SECURITY.md .env.example scripts/ .github/ISSUE_TEMPLATE/ .planning/INVARIANTS.md .planning/PROJECT.md --include="*.{ts,yml,yaml,md,example}"` — expect zero results.
  </action>
  <verify>
    Run: `grep -rn "SecureShare\|secureshare\.example\.com" server/ CONTRIBUTING.md SECURITY.md .env.example scripts/ .github/ISSUE_TEMPLATE/ .planning/INVARIANTS.md .planning/PROJECT.md` — must return zero results.
    Run: `grep "Torch Secret" server/src/auth.ts server/src/services/notification.service.ts` — must show updated email strings.
    Run: `grep "APP_URL" .env.example` — must return the new APP_URL entry.
    Run: `grep "Torch Secret" .planning/PROJECT.md` — must show the updated line 20 product description.
  </verify>
  <done>
    Zero "SecureShare" occurrences remain in server sources, scripts, template files, active planning docs, or developer-facing files. Email subjects read "Torch Secret". .env.example includes the APP_URL entry. PROJECT.md product description reads "Torch Secret".
  </done>
</task>

</tasks>

<verification>
Final audit after all three tasks complete:

1. Run comprehensive grep across all non-archive source files:
   `grep -r "SecureShare\|secureshare\.example\.com" . --include="*.{ts,tsx,js,html,json,yml,yaml,xml,txt,md,webmanifest,example}" --exclude-dir=".planning/milestones" --exclude-dir=".git" --exclude-dir="node_modules"`

   Expected: zero results (except inside `.planning/milestones/` which is excluded, and `.planning/phases/*/PLAN.md` history files which are expected to reference the old name as context).

2. Run: `grep -r "torchsecret.com" client/public/ client/index.html` — all three static files must contain the new domain.

3. Run: `grep "Torch Secret" server/src/auth.ts` — must show two email subject matches.

4. Run: `grep "Torch Secret" .planning/PROJECT.md` — must show updated product description.
</verification>

<success_criteria>
- "SecureShare" → "Torch Secret" in all HTML titles, OG tags, header wordmark, email subjects, in-app text, and file-level comments in active source files
- "secureshare.example.com" → "torchsecret.com" in all static files (index.html, sitemap.xml, robots.txt) and template literals
- INVARIANTS.md and PROJECT.md titles and prose updated to Torch Secret
- .env.example includes APP_URL entry
- Post-rename grep audit returns zero results across active source files
</success_criteria>

<output>
After completion, create `.planning/phases/31-rebrand-tech-debt/31-01-SUMMARY.md`
</output>
