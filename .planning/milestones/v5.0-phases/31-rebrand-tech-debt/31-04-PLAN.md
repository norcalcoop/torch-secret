---
phase: 31-rebrand-tech-debt
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/create.ts
autonomous: true
requirements:
  - BRAND-03
gap_closure: true

must_haves:
  truths:
    - "All text in the protection panel is visible when light theme is active"
    - "Protection panel tab labels, note text, strength labels, entropy line, and state label use semantic color tokens"
    - "Dark theme appearance is unchanged"
  artifacts:
    - path: "client/src/pages/create.ts"
      provides: "Protection panel with semantic color tokens replacing hardcoded text-white/* and border-white/* classes"
      contains: "text-text-secondary"
  key_links:
    - from: "client/src/pages/create.ts"
      to: "client/src/styles.css"
      via: "Tailwind utility classes referencing --ds-color-text-* CSS custom properties"
      pattern: "text-text-(primary|secondary|muted)"
---

<objective>
Fix light-theme invisible text in the protection panel by replacing all hardcoded `text-white/*` and `border-white/*` Tailwind classes with semantic design-token classes.

Purpose: The protection panel (`createProtectionPanel` function in `create.ts`) uses hardcoded white opacity classes (`text-white/60`, `text-white/40`, `text-white`, `border-white/10`). In dark mode the deep purple surface makes white text readable; in light mode the surface is near-white so white text is invisible. This is a Phase 28 regression surfaced in Phase 31 UAT.

Output: `client/src/pages/create.ts` with all nine affected lines updated to semantic token classes. No other files change.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded white color classes with semantic tokens in createProtectionPanel</name>
  <files>client/src/pages/create.ts</files>
  <action>
Make the following nine targeted class string substitutions inside `client/src/pages/create.ts`. Each change is a single class replacement; do not touch surrounding classes or logic.

Line 275 — checkbox label in `createCheckbox` helper:
  Before: `'text-xs font-mono text-white/60 cursor-pointer select-none'`
  After:  `'text-xs font-mono text-text-secondary cursor-pointer select-none'`

Line 290 — tab strip border:
  Before: `'flex border-b border-white/10'`
  After:  `'flex border-b border-border'`

Line 301 — inactive tab class string (two color classes):
  Before: `'...text-white/40 border-b-2 border-transparent -mb-px transition-colors duration-150 cursor-pointer hover:text-white/70...'`
  After:  `'...text-text-muted border-b-2 border-transparent -mb-px transition-colors duration-150 cursor-pointer hover:text-text-secondary...'`

Line 303 — active tab class string (two color classes):
  Before: `'...text-white border-b-2 border-b-accent -mb-px transition-colors duration-150 cursor-pointer hover:text-white/70...'`
  After:  `'...text-text-primary border-b-2 border-b-accent -mb-px transition-colors duration-150 cursor-pointer hover:text-text-secondary...'`

Line 356 — "No protection" panel note:
  Before: `'text-xs font-mono text-white/40'`
  After:  `'text-xs font-mono text-text-muted'`

Line 371 — strength tier label:
  Before: `'text-xs font-medium text-white/60'`
  After:  `'text-xs font-medium text-text-secondary'`

Line 436 — entropy line:
  Before: `'text-xs font-mono text-white/40'`
  After:  `'text-xs font-mono text-text-muted'`

Line 559 — gen state label (initial assignment):
  Before: `'text-xs font-mono text-white/40 mt-1'`
  After:  `'text-xs font-mono text-text-muted mt-1'`

Line 779 — gen state label inside `refreshCombinedField` (reset branch):
  Before: `'text-xs font-mono text-white/40 mt-1'`
  After:  `'text-xs font-mono text-text-muted mt-1'`

Semantic token reference (from `client/src/styles.css`):
- `text-text-primary`   → `--ds-color-text-primary`   (#12161f light / #f0f0ff dark)
- `text-text-secondary` → `--ds-color-text-secondary`  (#353a48 light / #c8c8e0 dark)
- `text-text-muted`     → `--ds-color-text-muted`      (#5d6372 light / muted dark)
- `border-border`       → `--ds-color-border`           (#d5d7de light / dark equivalent)

Do not change any logic, event listeners, other class strings, or any other file.
  </action>
  <verify>
1. Run `npm run lint` from `/Users/ourcomputer/Github-Repos/secureshare` — must pass with no errors.
2. Run `npm run format:check` — must pass.
3. Run `npx vitest run client/src` from the project root — all client unit tests must pass.
4. Search the file for remaining hardcoded `text-white/` and `border-white/` patterns inside `createProtectionPanel` (roughly lines 250–800):
   `grep -n 'text-white/\|border-white/' client/src/pages/create.ts`
   Expected: zero matches inside the protection panel function. (Other uses of `text-white` or `border-white` outside that function, if any, are out of scope.)
  </verify>
  <done>
- All nine class strings updated.
- `grep -n 'text-white/\|border-white/' client/src/pages/create.ts` returns no hits within the protection panel.
- `npm run lint` and `npm run format:check` pass cleanly.
- Client unit tests pass.
  </done>
</task>

</tasks>

<verification>
After the task completes:

1. Build the client: `npm run build:client` — must complete without errors.
2. Start dev server: `npm run dev:client` — open the app and switch to light theme via the theme toggle.
3. Navigate to the create page. Click through all four protection tabs (No protection, Generate password, Custom password, Passphrase). Verify every label, note, and helper text is legible (dark text on light surface — not invisible white-on-white).
4. Switch back to dark theme and verify the protection panel still looks correct (no color regressions).
</verification>

<success_criteria>
- All text within the protection panel is readable in both light and dark themes.
- No `text-white/*` or `border-white/*` classes remain inside `createProtectionPanel` in `client/src/pages/create.ts`.
- Lint, format check, and client unit tests pass.
- Production build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/31-rebrand-tech-debt/31-04-SUMMARY.md` following the standard summary template.
</output>
