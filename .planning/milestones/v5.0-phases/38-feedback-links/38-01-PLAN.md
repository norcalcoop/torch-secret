---
phase: 38-feedback-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/feedback-link.ts
  - client/src/pages/confirmation.ts
  - client/src/pages/reveal.ts
  - client/src/pages/confirmation.test.ts
  - client/src/pages/reveal.test.ts
autonomous: false
requirements: [FBCK-01, FBCK-02]

must_haves:
  truths:
    - "After creating a secret, the confirmation page shows a visible 'Share feedback' link"
    - "After viewing a secret, the post-reveal page shows a visible 'Share feedback' link"
    - "Both links open in a new tab (target=_blank) with rel=noopener noreferrer"
    - "Both links use the same Tally.so URL constant — no divergence between pages"
  artifacts:
    - path: "client/src/components/feedback-link.ts"
      provides: "createFeedbackLink(url) factory — returns HTMLAnchorElement"
      exports: ["createFeedbackLink"]
    - path: "client/src/pages/confirmation.ts"
      provides: "renderConfirmationPage with feedback link as last child of wrapper"
      contains: "createFeedbackLink"
    - path: "client/src/pages/reveal.ts"
      provides: "renderRevealedSecret with feedback link as second child of actions row"
      contains: "createFeedbackLink"
    - path: "client/src/pages/confirmation.test.ts"
      provides: "DOM assertions for FBCK-01"
    - path: "client/src/pages/reveal.test.ts"
      provides: "DOM assertions for FBCK-02"
  key_links:
    - from: "client/src/pages/confirmation.ts"
      to: "client/src/components/feedback-link.ts"
      via: "ES import createFeedbackLink"
      pattern: "import.*createFeedbackLink.*feedback-link"
    - from: "client/src/pages/reveal.ts"
      to: "client/src/components/feedback-link.ts"
      via: "ES import createFeedbackLink"
      pattern: "import.*createFeedbackLink.*feedback-link"
    - from: "feedback-link anchor"
      to: "tally.so"
      via: "href attribute = TALLY_FEEDBACK_URL constant"
      pattern: "tally\\.so/r/"
---

<objective>
Add a "Share feedback" link to the confirmation page (post-creation) and the revealed secret page (post-reveal). Both are the highest-intent moments in the product flow. Each link opens the Tally.so feedback form in a new tab with proper security attributes.

Purpose: Give the team a direct signal channel at the two moments users have just completed their primary action — creating a secret and viewing one.
Output: `createFeedbackLink` shared component + wired into both page modules + unit tests confirming DOM attributes.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-feedback-links/38-RESEARCH.md

@client/src/pages/confirmation.ts
@client/src/pages/reveal.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From client/src/pages/confirmation.ts (lines 283-294):
```typescript
// "Create another" button is the last child of wrapper, then container.appendChild(wrapper)
const createAnotherButton = document.createElement('button');
createAnotherButton.type = 'button';
createAnotherButton.className = '...';
createAnotherButton.textContent = 'Create Another Secret';
createAnotherButton.addEventListener('click', () => { navigate('/'); });
wrapper.appendChild(createAnotherButton);
// Phase 38: feedback link goes HERE, after createAnotherButton, before container.appendChild(wrapper)
container.appendChild(wrapper);
focusPageHeading();
```

From client/src/pages/reveal.ts (lines 400-421):
```typescript
// renderRevealedSecret — actions row, currently one child: newSecretLink
const actions = document.createElement('div');
actions.className = 'flex flex-col sm:flex-row items-center gap-4';
const newSecretLink = document.createElement('a');
// ...
actions.appendChild(newSecretLink);
// Phase 38: feedback link goes HERE as second child of actions
wrapper.appendChild(heading);
wrapper.appendChild(badge);
wrapper.appendChild(terminal);
wrapper.appendChild(actions);
container.appendChild(wrapper);
captureSecretViewed();
```

Existing anchor pattern (from confirmation.ts):
```typescript
// Project convention: textContent only (NEVER innerHTML), Tailwind utility classes
const ctaLink = document.createElement('a');
ctaLink.href = '/register';
ctaLink.className = '...';
ctaLink.textContent = "Sign up — it's free";
```

External link pattern to follow:
```typescript
const feedbackLink = document.createElement('a');
feedbackLink.href = TALLY_FEEDBACK_URL;   // named constant
feedbackLink.target = '_blank';
feedbackLink.rel = 'noopener noreferrer';
feedbackLink.textContent = 'Share feedback';
feedbackLink.className = 'inline-block py-2 text-sm text-text-muted hover:text-text-secondary ' +
  'focus:ring-2 focus:ring-accent focus:outline-hidden rounded transition-colors';
```

Component naming pattern (from client/src/components/):
```
copy-button.ts     → export function createCopyButton(...)
share-button.ts    → export function createShareButton(...)
feedback-link.ts   → export function createFeedbackLink(url: string)
```

Test file pattern (from client/src/pages/register.test.ts):
```typescript
// @vitest-environment happy-dom (implicit via vitest.config.ts client project)
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
vi.mock('../router.js', () => ({ navigate: vi.fn() }));
// ... other mocks ...
import { renderXxxPage } from './xxx.js';
describe('...', () => {
  let container: HTMLElement;
  beforeEach(() => { container = document.createElement('div'); document.body.appendChild(container); });
  afterEach(() => { document.body.removeChild(container); });
  it('...', async () => { await renderXxxPage(container); const el = container.querySelector('a[href*="tally.so"]'); ... });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared feedback-link component and write failing tests</name>
  <files>client/src/components/feedback-link.ts, client/src/pages/confirmation.test.ts, client/src/pages/reveal.test.ts</files>
  <behavior>
    - confirmation.test.ts: renderConfirmationPage() produces an anchor with href containing "tally.so/r/", target="_blank", rel="noopener noreferrer", textContent="Share feedback"
    - reveal.test.ts: renderRevealedSecret() produces an anchor with href containing "tally.so/r/", target="_blank", rel="noopener noreferrer", textContent="Share feedback"
    - feedback-link.ts: createFeedbackLink('https://tally.so/r/TEST') returns HTMLAnchorElement with correct href, target, rel, textContent
  </behavior>
  <action>
    **Step 1 — Create the shared component (non-test code first, since it has no dependencies):**

    Create `client/src/components/feedback-link.ts`:
    - Export a named constant `TALLY_FEEDBACK_URL = 'https://tally.so/r/TODO'` at the top of the file — easy to find-and-replace when the real Tally form is created.
    - Export `createFeedbackLink(url: string): HTMLAnchorElement` factory function.
    - The anchor must have: `href = url`, `target = '_blank'`, `rel = 'noopener noreferrer'`, `textContent = 'Share feedback'`.
    - Apply Tailwind classes: `'inline-block py-2 text-sm text-text-muted hover:text-text-secondary focus:ring-2 focus:ring-accent focus:outline-hidden rounded transition-colors'`.
    - Do NOT use `innerHTML` — use `textContent` only.
    - Do NOT add a click handler — it is a plain anchor, semantically correct for external navigation.
    - Do NOT add an icon in this task (keep minimal; icon is discretionary and omitted to avoid adding Lucide import complexity).

    **Step 2 — Write failing test for FBCK-01 (`confirmation.test.ts`):**

    Create `client/src/pages/confirmation.test.ts`. Mock everything `confirmation.ts` imports:
    - `vi.mock('../router.js', () => ({ navigate: vi.fn(), updatePageMeta: vi.fn(), focusPageHeading: vi.fn() }))`
    - `vi.mock('../analytics/posthog.js', () => ({ captureConversionPromptShown: vi.fn(), captureConversionPromptClicked: vi.fn() }))`
    - `vi.mock('../components/icons.js', () => ({ createIcon: vi.fn(() => document.createElement('span')) }))`
    - `vi.mock('../components/copy-button.js', () => ({ createCopyButton: vi.fn(() => document.createElement('button')) }))`
    - `vi.mock('../components/share-button.js', () => ({ createShareButton: vi.fn(() => null) }))`

    Import `renderConfirmationPage` after mocks. Write one `describe` block "confirmation page — feedback link (FBCK-01)" with:
    - `it('renders an anchor linking to tally.so', ...)`: calls `renderConfirmationPage(container, 'https://example.com/secret#key', new Date(Date.now() + 3600000).toISOString())`, queries `container.querySelector('a[href*="tally.so"]')`, expects it not to be null.
    - `it('opens in a new tab', ...)`: same render call, asserts `anchor.target === '_blank'`.
    - `it('has rel=noopener noreferrer', ...)`: same render, asserts `anchor.rel === 'noopener noreferrer'`.
    - `it('has text content "Share feedback"', ...)`: same render, asserts `anchor.textContent === 'Share feedback'`.

    Run `npx vitest run client/src/pages/confirmation.test.ts -x` — expect RED (component not yet wired into confirmation.ts).

    **Step 3 — Write failing test for FBCK-02 (`reveal.test.ts`):**

    Create `client/src/pages/reveal.test.ts`. Because `renderRevealedSecret` is not currently exported, Task 2 will add the `export` keyword. Write the test file now so it is ready when Task 2 makes it GREEN.

    Mocks needed:
    - `vi.mock('../api/client.js', () => ({ getSecret: vi.fn(), getSecretMeta: vi.fn(), verifySecretPassword: vi.fn(), ApiError: class ApiError extends Error {} }))`
    - `vi.mock('../analytics/posthog.js', () => ({ captureSecretViewed: vi.fn() }))`
    - `vi.mock('../components/terminal-block.js', () => ({ createTerminalBlock: vi.fn(() => document.createElement('div')) }))`
    - `vi.mock('../components/icons.js', () => ({ createIcon: vi.fn(() => document.createElement('span')) }))`
    - `vi.mock('../components/loading-spinner.js', () => ({ createLoadingSpinner: vi.fn(() => document.createElement('div')) }))`
    - `vi.mock('./error.js', () => ({ renderErrorPage: vi.fn() }))`
    - `vi.mock('../router.js', () => ({ navigate: vi.fn() }))`
    - `vi.mock('../crypto/index.js', () => ({ decrypt: vi.fn() }))`

    Import `renderRevealedSecret` (named export — will be added in Task 2). Write one `describe` block "reveal page — feedback link (FBCK-02)" with 4 parallel `it` assertions (same pattern as confirmation.test.ts, but calling `renderRevealedSecret(container, 'hello world')` directly).

    Run `npx vitest run client/src/pages/reveal.test.ts -x` — expect RED (export + wiring not yet done).
  </action>
  <verify>
    <automated>MISSING — tests run RED intentionally in this step; Task 2 makes them GREEN. Confirm RED with: npx vitest run client/src/pages/confirmation.test.ts client/src/pages/reveal.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Three files exist: feedback-link.ts (exports TALLY_FEEDBACK_URL + createFeedbackLink), confirmation.test.ts (4 assertions), reveal.test.ts (4 assertions). Tests run but fail (RED) because confirmation.ts and reveal.ts do not yet import createFeedbackLink, and renderRevealedSecret is not yet exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire createFeedbackLink into confirmation.ts and reveal.ts — make tests GREEN</name>
  <files>client/src/pages/confirmation.ts, client/src/pages/reveal.ts</files>
  <behavior>
    - confirmation.ts: feedback link is the last visual element in wrapper (after createAnotherButton, before container.appendChild(wrapper))
    - reveal.ts: renderRevealedSecret is exported (for test isolation) and feedback link is second child of actions row (after newSecretLink)
    - Both pages import TALLY_FEEDBACK_URL from components/feedback-link.ts and call createFeedbackLink(TALLY_FEEDBACK_URL)
    - npx vitest run client/src/pages/confirmation.test.ts client/src/pages/reveal.test.ts passes (4+4=8 GREEN)
    - npm run test:run passes (full suite green, no regressions)
  </behavior>
  <action>
    **Edit `client/src/pages/confirmation.ts`:**

    1. Add import at top of file (alongside existing lucide/component imports):
       ```typescript
       import { createFeedbackLink, TALLY_FEEDBACK_URL } from '../components/feedback-link.js';
       ```

    2. In `renderConfirmationPage()`, after the `wrapper.appendChild(createAnotherButton)` line (line 292) and BEFORE `container.appendChild(wrapper)`:
       ```typescript
       // -- Feedback link (Phase 38 -- opens Tally.so form in new tab) --
       const feedbackLink = createFeedbackLink(TALLY_FEEDBACK_URL);
       wrapper.appendChild(feedbackLink);
       ```

    **Edit `client/src/pages/reveal.ts`:**

    1. Add import at top of file:
       ```typescript
       import { createFeedbackLink, TALLY_FEEDBACK_URL } from '../components/feedback-link.js';
       ```

    2. Add `export` keyword to the `renderRevealedSecret` function declaration (change `function renderRevealedSecret` to `export function renderRevealedSecret`) to enable direct test isolation. Add a JSDoc comment `/** @internal — exported for test isolation only. */` above the export.

    3. In `renderRevealedSecret()`, after `actions.appendChild(newSecretLink)` (line 414) and BEFORE `wrapper.appendChild(actions)`:
       ```typescript
       // -- Feedback link (Phase 38 -- opens Tally.so form in new tab) --
       const feedbackLink = createFeedbackLink(TALLY_FEEDBACK_URL);
       actions.appendChild(feedbackLink);
       ```

    **Security invariants to verify during implementation:**
    - `TALLY_FEEDBACK_URL` must be a static string with no `?` query parameters — no userId, secretId, or any identifying data appended. ZK invariant requires the link to be completely anonymous.
    - Do NOT pre-populate the Tally URL with `window.location.href` or any URL params.

    After both edits, run `npx vitest run client/src/pages/confirmation.test.ts client/src/pages/reveal.test.ts -x` to confirm GREEN. Then run `npm run test:run` for full suite.

    Finally run `npm run lint` and `npm run format:check` to verify TypeScript + ESLint clean.
  </action>
  <verify>
    <automated>npx vitest run client/src/pages/confirmation.test.ts client/src/pages/reveal.test.ts --reporter=verbose && npm run test:run</automated>
  </verify>
  <done>
    - 8 new tests pass (4 per page): href contains "tally.so/r/", target="_blank", rel="noopener noreferrer", textContent="Share feedback"
    - Full test suite passes (no regressions against 353 pre-existing tests)
    - ESLint clean, TypeScript compiles without errors
    - Both page files import createFeedbackLink from feedback-link.ts
    - TALLY_FEEDBACK_URL constant defined in feedback-link.ts (value "https://tally.so/r/TODO") — ready for swap when real form is created
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — feedback links visible on confirmation and post-reveal pages</name>
  <what-built>
    Feedback links on confirmation and post-reveal pages. Both open Tally.so in a new tab. Shared createFeedbackLink component. TALLY_FEEDBACK_URL constant placeholder for easy swap.
  </what-built>
  <action>
    Human verification of visual placement and link behavior. Follow the steps in how-to-verify.
  </action>
  <how-to-verify>
    1. Start the dev server: run `npm run dev:client` and `npm run dev:server` in two terminals
    2. Navigate to http://torchsecret.localhost:1355/create
    3. Create a secret (type anything, click "Create Secure Link")
    4. On the confirmation page: confirm a "Share feedback" link is visible below the "Create Another Secret" button
    5. Right-click the "Share feedback" link and open in new tab — confirm it opens https://tally.so/r/TODO (will 404 since TODO is a placeholder — that is expected)
    6. Navigate to a reveal URL with a valid secret (create another, copy the link, visit it)
    7. Click "Reveal Secret"
    8. On the post-reveal page: confirm a "Share feedback" link is visible in the actions row alongside "Create a New Secret"
    9. Click "Share feedback" — confirms it opens a new tab (URL will be tally.so/r/TODO placeholder)
    10. When ready to use a real form: update TALLY_FEEDBACK_URL in client/src/components/feedback-link.ts from 'https://tally.so/r/TODO' to the actual Tally.so share URL from the Tally "Share" tab
  </how-to-verify>
  <verify>Human confirms feedback links visible and functional on both pages.</verify>
  <done>User types "approved" — feedback links visually confirmed on confirmation page and post-reveal page.</done>
  <resume-signal>Type "approved" to confirm feedback links visible on both pages. Or describe any visual/functional issues found.</resume-signal>
</task>

</tasks>

<verification>
Full phase gate before /gsd:verify-work:
- `npm run test:run` — all tests pass (353 pre-existing + 8 new = 361 total)
- `npm run lint` — ESLint clean
- `npm run format:check` — Prettier clean
- `npm run build:client` — Vite builds without TypeScript errors
- FBCK-01: confirmation page renders anchor with href containing "tally.so/r/", target="_blank", rel="noopener noreferrer"
- FBCK-02: reveal page renders anchor with href containing "tally.so/r/", target="_blank", rel="noopener noreferrer"
- ZK invariant: TALLY_FEEDBACK_URL is a static string with no query parameters — confirmed by grep: `grep -n "TALLY_FEEDBACK_URL" client/src/components/feedback-link.ts` shows no `?` character in the value
</verification>

<success_criteria>
- Feedback link appears on confirmation page (post-creation) — visible below "Create Another Secret" button
- Feedback link appears on post-reveal page — visible in actions row alongside "Create a New Secret" link
- Both links open in new tab with `target="_blank"` and `rel="noopener noreferrer"`
- TALLY_FEEDBACK_URL is a named constant in feedback-link.ts (currently "https://tally.so/r/TODO", easy to update when real form is created)
- 8 new unit tests pass asserting href, target, rel, textContent for each page
- Zero regressions against existing test suite
- TypeScript, ESLint, and Prettier all clean
</success_criteria>

<output>
After completion, create `.planning/phases/38-feedback-links/38-01-SUMMARY.md` with:
- What was built (feedback-link component, wiring in both pages)
- TALLY_FEEDBACK_URL value and how to update it
- Test counts (pre-existing + new)
- Any deviations from plan
</output>
