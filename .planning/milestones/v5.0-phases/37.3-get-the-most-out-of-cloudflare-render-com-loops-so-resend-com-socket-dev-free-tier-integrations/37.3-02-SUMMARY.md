---
phase: 37.3-free-tier-integrations
plan: "02"
subsystem: email
tags: [loops, socket-dev, subscribers, double-opt-in, supply-chain, tdd]

# Dependency graph
requires:
  - phase: 37.3-free-tier-integrations-01
    provides: render.yaml env var parity, CI re-enabled
  - phase: 37-email-onboarding
    provides: loops singleton (config/loops.ts), onboarding.service.ts fire-and-forget pattern
  - phase: 36-email-capture
    provides: confirmSubscriber() in subscribers.service.ts, Resend fire-and-forget pattern

provides:
  - confirmSubscriber() fires loops.sendEvent('subscribed') with source:'email-capture' after DB update (fire-and-forget)
  - socket.yml at repo root with version:2 format and githubApp enabled
  - README security section bullet documenting Socket.dev dependency scanning

affects:
  - 37.3-06-socket-dev-github-app (reads socket.yml)
  - any future plan touching subscribers.service.ts

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Parallel fire-and-forget pattern: Resend Audience sync + Loops sendEvent both fire after DB update, neither blocks the confirm response"
    - "ZK-safe Loops event: only email + contactProperties.source — no userId, no secretId"
    - "ESLint disableTypeChecked for Cloudflare Workers dir: workers/**/*.ts excluded from projectService type checking"

key-files:
  created:
    - socket.yml
    - .planning/phases/37.3-.../37.3-02-SUMMARY.md
  modified:
    - server/src/services/subscribers.service.ts
    - server/src/routes/__tests__/subscribers.test.ts
    - eslint.config.ts
    - README.md

key-decisions:
  - "Loops sendEvent fires after Resend contacts.create — two parallel fire-and-forget calls, matching existing pattern; neither blocks confirm flow"
  - "socket.yml uses version:2 format (v1 issueRules deprecated); no issueRules added preemptively — add if false positives arise after GitHub App install (Plan 06)"
  - "ESLint workers/**/*.ts override added with disableTypeChecked — Cloudflare Worker globals (ScheduledController, ExecutionContext) not in root tsconfig; avoids projectService error blocking all commits"

patterns-established:
  - "Parallel fire-and-forget after DB update: void service.action().catch(err => console.error(...))"
  - "ZK-safe Loops events: contactProperties contains only categorization data (source), never user/secret identifiers"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-02-28
---

# Phase 37.3 Plan 02: Loops Subscribed Event + Socket.dev Config Summary

**Loops 'subscribed' event wired into double opt-in confirm flow (fire-and-forget alongside Resend Audience sync) plus socket.yml v2 config file and README security note**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-02-28T14:17:29Z
- **Completed:** 2026-02-28T14:20:13Z
- **Tasks:** 2
- **Files modified:** 5 (subscribers.service.ts, subscribers.test.ts, eslint.config.ts, socket.yml, README.md)

## Accomplishments

- `confirmSubscriber()` now fires `loops.sendEvent({ email, eventName: 'subscribed', contactProperties: { source: 'email-capture' } })` fire-and-forget after the existing Resend Audience sync — Loops can now distinguish confirmed double opt-in subscribers from pending form submitters
- `socket.yml` created at repo root with version:2 format and all githubApp settings enabled — ready for Plan 06 GitHub App installation
- README Features section updated with supply chain security bullet mentioning Socket.dev PR scanning
- All 18 subscriber tests pass including 2 new Loops test cases (TDD: RED commit then GREEN commit)
- Fixed pre-existing ESLint blocking issue: `workers/**/*.ts` needed `disableTypeChecked` override since Cloudflare Worker globals are not in root tsconfig

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Loops subscribed event — failing tests** - `34ee6d6` (test)
2. **Task 1 GREEN: Loops subscribed event — implementation** - `aa9277c` (feat)
3. **Task 2: socket.yml + README security note** - `45bf43e` (chore)

## Files Created/Modified

- `server/src/services/subscribers.service.ts` - Added `import { loops }` + `loops.sendEvent()` fire-and-forget after Resend call in `confirmSubscriber()`
- `server/src/routes/__tests__/subscribers.test.ts` - Added vi.mock for loops, loopsSendEvent() helper, 2 new test cases
- `eslint.config.ts` - Added `workers/**/*.ts` override with `disableTypeChecked` (Rule 3 auto-fix)
- `socket.yml` - Created Socket.dev v2 config at repo root with githubApp settings
- `README.md` - Added supply chain security bullet in Features section

## Decisions Made

- Loops `sendEvent` fires after `resend.contacts.create` — both are fire-and-forget with separate `.catch()` handlers. The two are intentionally parallel: a Loops outage never affects Resend sync and vice versa.
- `socket.yml` uses version:2 format only — no `issueRules` added preemptively. Per plan: add specific rules only if false positives arise after GitHub App install in Plan 06.
- ESLint override for `workers/**/*.ts` uses `disableTypeChecked` (same pattern as `*.config.ts` override) — correct fix for Cloudflare Worker globals that have their own tsconfig outside the root project service scope.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] ESLint projectService error blocking all commits for workers/keep-alive/src/index.ts**
- **Found during:** Task 1 (first commit attempt)
- **Issue:** `workers/keep-alive/src/index.ts` (created in Plan 01) uses Cloudflare Worker types (`ScheduledController`, `ExecutionContext`) not in root `tsconfig.json`. ESLint `projectService: true` tried to type-check it and failed with "was not found by the project service" — blocking lint-staged pre-commit hook for every subsequent commit.
- **Fix:** Added `{ files: ['workers/**/*.ts'], extends: [tseslint.configs.disableTypeChecked] }` override to `eslint.config.ts`, matching the existing pattern for `*.config.ts` files.
- **Files modified:** `eslint.config.ts`
- **Verification:** `npx eslint workers/keep-alive/src/index.ts` returns 0 errors; all subsequent commits succeed.
- **Committed in:** `34ee6d6` (included in Task 1 RED commit)

---

**Total deviations:** 1 auto-fixed (Rule 3 — blocking issue)
**Impact on plan:** Fix was necessary — without it, no commits in this plan or any future plan could succeed. No scope creep.

## Issues Encountered

None beyond the ESLint blocking issue documented above.

## User Setup Required

None — socket.yml is a config file read by the Socket.dev GitHub App when installed in Plan 06. No manual configuration needed at this step.

## Next Phase Readiness

- Plan 03 and beyond: Loops now receives 'subscribed' events from confirmed double opt-in subscribers; can build segmented campaigns targeting only confirmed users
- Plan 06: socket.yml is ready — GitHub App installation will immediately read this config for PR scan settings
- ESLint now correctly handles `workers/**/*.ts` — no future commit blocking from the keep-alive worker file

---
*Phase: 37.3-free-tier-integrations*
*Completed: 2026-02-28*

## Self-Check: PASSED

- FOUND: server/src/services/subscribers.service.ts
- FOUND: socket.yml
- FOUND: README.md
- FOUND: 37.3-02-SUMMARY.md
- FOUND commit: 34ee6d6 (test RED)
- FOUND commit: aa9277c (feat GREEN)
- FOUND commit: 45bf43e (chore Task 2)
