---
phase: 37.3-free-tier-integrations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - render.yaml
  - .github/workflows/ci.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - "render.yaml envVars section contains every key that appears in .env.example (full parity)"
    - "CI workflow triggers on push to main and on pull_request — not just workflow_dispatch"
    - "autoDeployTrigger: checksPass in render.yaml is active (CI re-enable makes this gate live)"
  artifacts:
    - path: "render.yaml"
      provides: "Full env var parity with .env.example including LOOPS_API_KEY, APP_URL, BETTER_AUTH_TRUSTED_ORIGINS, STRIPE_*, RESEND_AUDIENCE_ID, IP_HASH_SALT"
    - path: ".github/workflows/ci.yml"
      provides: "CI auto-triggers: push(main) + pull_request + workflow_dispatch"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "render.yaml autoDeployTrigger: checksPass"
      via: "CI passing → Render deploys to production"
      pattern: "autoDeployTrigger"
---

<objective>
Bring render.yaml into full parity with .env.example (all env var keys documented as sync:false stubs), and re-enable GitHub Actions CI auto-triggers so Render's production deploy gate becomes active.

Purpose: render.yaml is missing ~10 env var stubs added in Phases 34–37. Without them, the Render dashboard silently omits required runtime secrets when operators follow render.yaml as documentation. CI re-enablement is a one-line change but has critical downstream effect: it activates Render's autoDeployTrigger:checksPass gate, meaning production deploys only happen after CI passes.

Output: Updated render.yaml and ci.yml; no code logic changes.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: render.yaml env var parity</name>
  <files>render.yaml</files>
  <action>
Add the following sync:false stubs to the web service envVars section of render.yaml, after the existing VITE_POSTHOG_HOST stub and before the Redis keyvalue service definition. Do NOT add values — stubs serve as documentation reminders for the Render dashboard.

Add in this order:

```yaml
      # === Application URL ===
      # Set to your Render service URL, e.g. https://torchsecret.com (no trailing slash)
      - key: APP_URL
        sync: false
      # Comma-separated trusted origins for CSRF protection — same as APP_URL for single-domain deploys
      - key: BETTER_AUTH_TRUSTED_ORIGINS
        sync: false

      # === Loops email onboarding (Phase 37) ===
      - key: LOOPS_API_KEY
        sync: false

      # === Stripe Billing (Phase 34) ===
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: STRIPE_PRO_PRICE_ID
        sync: false

      # === Email Capture (Phase 36) ===
      - key: RESEND_AUDIENCE_ID
        sync: false
      # Salt for SHA-256 IP hashing — generate: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
      - key: IP_HASH_SALT
        sync: false

      # === PostHog REST API — used for scripted dashboard setup (Phase 37.1), not required at runtime ===
      # - key: POSTHOG_PERSONAL_API_KEY
      #   sync: false
      # - key: POSTHOG_PROJECT_ID
      #   sync: false
```

IMPORTANT: The POSTHOG_PERSONAL_API_KEY and POSTHOG_PROJECT_ID stubs are commented out (not active stubs) — they are NOT runtime requirements; the comment explains this. Use commented YAML lines (leading `# `) as shown.

ANTI-PATTERN: Do NOT put sync:false inside an envVarGroups section. The existing render.yaml uses inline envVars on the web service — preserve that pattern. sync:false inside envVarGroups is silently ignored by Render.
  </action>
  <verify>
    <automated>
# Check that all .env.example keys (required, non-optional) appear in render.yaml
# Required keys from .env.example: BETTER_AUTH_SECRET, BETTER_AUTH_URL, APP_URL, BETTER_AUTH_TRUSTED_ORIGINS,
# RESEND_API_KEY, RESEND_FROM_EMAIL, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRO_PRICE_ID,
# RESEND_AUDIENCE_ID, IP_HASH_SALT, LOOPS_API_KEY
grep -c "APP_URL\|BETTER_AUTH_TRUSTED_ORIGINS\|LOOPS_API_KEY\|STRIPE_SECRET_KEY\|STRIPE_WEBHOOK_SECRET\|STRIPE_PRO_PRICE_ID\|RESEND_AUDIENCE_ID\|IP_HASH_SALT" /Users/ourcomputer/Github-Repos/secureshare/render.yaml
# Should print 8 (or higher — each key on its own line)
    </automated>
  </verify>
  <done>render.yaml web service envVars includes all 8 missing stubs (APP_URL, BETTER_AUTH_TRUSTED_ORIGINS, LOOPS_API_KEY, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRO_PRICE_ID, RESEND_AUDIENCE_ID, IP_HASH_SALT) with sync:false and descriptive comments. PostHog REST API stubs are commented out with runtime note.</done>
</task>

<task type="auto">
  <name>Task 2: Re-enable GitHub Actions CI auto-triggers</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Replace the current `on:` block in .github/workflows/ci.yml:

Current:
```yaml
on:
  workflow_dispatch: # temporarily disabled auto-triggers; re-enable push/pull_request when ready
```

Replace with:
```yaml
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
```

This is a one-line structural change — no other modifications to ci.yml. The GitHub Secrets (INFISICAL_CLIENT_ID, INFISICAL_CLIENT_SECRET) are already configured in the repo — the re-enable is safe.

Context: autoDeployTrigger:checksPass in render.yaml means Render will now deploy to production only after all CI checks pass on the main branch. This is the production deploy safety gate. workflow_dispatch is kept alongside the new triggers so manual runs remain available (Claude's Discretion: keep it).
  </action>
  <verify>
    <automated>grep -A4 "^on:" /Users/ourcomputer/Github-Repos/secureshare/.github/workflows/ci.yml</automated>
  </verify>
  <done>ci.yml on: block contains push (branches: [main]), pull_request, and workflow_dispatch triggers. No other lines changed.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -c "sync: false" render.yaml` — should be 10+ (existing 6 + 8 new, POSTHOG commented out)
2. `grep -A4 "^on:" .github/workflows/ci.yml` — shows push/pull_request/workflow_dispatch
3. `npm run lint` passes — ci.yml and render.yaml are not TypeScript files but a quick lint confirms no cross-file issues
</verification>

<success_criteria>
- render.yaml documents all required runtime secrets as sync:false stubs — operators following render.yaml for a new deploy will not silently miss Stripe, Loops, Resend Audience, or IP hash salt
- CI triggers on push to main and on PRs; Render's autoDeployTrigger:checksPass gate is now active
- No application code was changed — this is pure configuration
</success_criteria>

<output>
After completion, create `.planning/phases/37.3-get-the-most-out-of-cloudflare-render-com-loops-so-resend-com-socket-dev-free-tier-integrations/37.3-01-SUMMARY.md`
</output>
