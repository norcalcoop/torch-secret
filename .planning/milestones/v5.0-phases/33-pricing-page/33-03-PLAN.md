---
phase: 33-pricing-page
plan: 03
type: execute
wave: 3
depends_on:
  - 33-01
  - 33-02
files_modified: []
autonomous: false

requirements:
  - PRICE-01
  - PRICE-02
  - PRICE-03
  - PRICE-04
  - PRICE-05

must_haves:
  truths:
    - "User sees Free vs Pro tier cards at /pricing (not 404)"
    - "Billing toggle is visible above cards; annual is selected by default with 22% savings badge"
    - "Toggling to Monthly updates Pro card price; toggling back to Annual restores it"
    - "Pro card has visually distinct treatment (accent border) and Recommended badge"
    - "FAQ accordion shows 6 items; items open and close on click"
    - "curl http://localhost:3000/pricing output contains FAQPage"
    - "curl -I http://localhost:3000/pricing does NOT show X-Robots-Tag: noindex"
    - "Free CTA 'Start for free' navigates to /create; Pro CTA 'Get Pro' navigates to /register?plan=pro"
  artifacts:
    - path: "client/src/pages/pricing.ts"
      provides: "Complete pricing page module"
    - path: "client/index.html"
      provides: "FAQPage JSON-LD in <head>"
    - path: "client/src/router.ts"
      provides: "/pricing route wired to renderPricingPage"
  key_links: []
---

<objective>
Human UAT verification of the complete pricing page at `/pricing`.

Purpose: Confirms all five PRICE requirements are met from the user's perspective — visual design, toggle behavior, card content, FAQ accordion, and JSON-LD curl-verifiability.

Output: Verified pricing page. No files modified by this plan.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-pricing-page/33-01-SUMMARY.md
@.planning/phases/33-pricing-page/33-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-UAT checks</name>
  <files><!-- no files modified — verification only --></files>
  <action>
Run automated verification before the human checkpoint:

1. ESLint must pass: `npm run lint` — no errors on pricing.ts, router.ts, or index.html
2. TypeScript must compile: `npm run build:client` — no type errors
3. Verify pricing page is wired in router: `grep -c "renderPricingPage" client/src/router.ts` — must return 1
4. Verify noindex removed: `grep -A8 "path === '/pricing'" client/src/router.ts | grep -c noindex` — must return 0
5. Verify FAQPage JSON-LD in index.html: `grep -c "FAQPage" client/index.html` — must return 1
6. Verify two JSON-LD blocks: `grep -c "application/ld+json" client/index.html` — must return 2
7. Verify two CSP nonces: `grep -c "__CSP_NONCE__" client/index.html` — must return 2
8. Verify no innerHTML in pricing.ts: `grep -c innerHTML client/src/pages/pricing.ts` — must return 0
9. Start dev servers for human testing:
   - `npm run dev:server` (requires PostgreSQL running — if not available, test against built client with `npm run build:client && npm run preview:client`)
   - `npm run dev:client`
10. Curl check (requires running server): `curl -s http://localhost:3000/pricing | grep -c "FAQPage"` — must return 1
11. Noindex check: `curl -sI http://localhost:3000/pricing | grep -i x-robots` — must produce no output
  </action>
  <verify>
- `npm run lint` exits 0
- `npm run build:client` exits 0
- `grep -c "renderPricingPage" client/src/router.ts` returns 1
- `grep -c "FAQPage" client/index.html` returns 1
- `grep -c "application/ld+json" client/index.html` returns 2
- `curl -s http://localhost:3000/pricing | grep -c "FAQPage"` returns 1
- `curl -sI http://localhost:3000/pricing | grep -i x-robots` returns no output
  </verify>
  <done>
All automated checks pass. Dev servers running. FAQPage JSON-LD present at /pricing via curl. No noindex header on /pricing. Ready for human visual verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — /pricing page, all PRICE requirements</name>
  <files></files>
  <action>
Open http://localhost:5173/pricing and verify the following flows.

**1. Page loads correctly (PRICE-01)**
- Navigate to http://localhost:5173/pricing
- Verify: Page shows "Simple, transparent pricing" h1 (not a 404 error page)
- Verify: Two cards are visible (side by side on desktop, stacked on mobile)

**2. Billing toggle behavior (PRICE-02)**
- Verify: Page loads with "Annual" active and "22% savings" badge visible
- Verify: Pro card shows "$65" and "/year"
- Click the toggle to switch to Monthly
- Verify: "22% savings" badge disappears or is hidden
- Verify: Pro card price updates to "$7" and "/month"
- Click toggle back to Annual
- Verify: Price reverts to "$65/year" and savings badge reappears

**3. Free tier card (PRICE-01)**
- Verify: Card labeled "Free", price "$0/forever"
- Verify: Feature list shows items (some with checkmarks, some with muted indicators)
- Verify: "Start for free" button is visible
- Click "Start for free" — verify SPA navigation to /create (no full page reload)

**4. Pro tier card (PRICE-03)**
- Verify: "Recommended" badge is visible at the top of the Pro card
- Verify: Pro card has a visually distinct border (accent-colored) compared to Free card
- Verify: "Get Pro" button is visible and NOT greyed out/disabled

**5. FAQ accordion (PRICE-04)**
- Verify: "Frequently asked questions" heading is visible
- Verify: 6 FAQ items are listed (closed by default)
- Click "Can I cancel my Pro subscription at any time?" — verify answer text appears
- Click again — verify item closes
- Verify topics visible: cancellation, refunds, billing cycle, free trial, payment methods, zero-knowledge security

**6. Mobile check (at 390px width or DevTools mobile emulation)**
- Verify: Cards stack vertically
- Verify: No content clipped by bottom tab bar (scroll to bottom, FAQ items still reachable)

**7. Curl verification (PRICE-05)**
Already confirmed in Task 1 automated checks. Optionally re-run:
`curl -s http://localhost:3000/pricing | grep "FAQPage"`
Should return the FAQPage JSON-LD block.
  </action>
  <verify>
Human types "approved" after verifying all flows above, or describes specific issues found.
  </verify>
  <done>
Human has confirmed: /pricing shows real pricing page (not 404); billing toggle works; Pro card has Recommended badge and visual distinction; FAQ accordion with 6 items; FAQPage JSON-LD curl-verifiable; no noindex on /pricing.
  </done>
</task>

</tasks>

<verification>
Phase 33 complete when:
1. /pricing shows Free vs Pro tier cards (not 404) — PRICE-01
2. Billing toggle visible, annual default, 22% savings label, prices update correctly — PRICE-02
3. Pro card has Recommended badge and accent border — PRICE-03
4. FAQ section with 6 items covering required topics — PRICE-04
5. curl confirms FAQPage JSON-LD in initial HTML response; no noindex header — PRICE-05
6. Human has typed "approved"
</verification>

<success_criteria>
Human confirms all 5 PRICE requirements are visually and functionally correct. Phase 33 is complete and Phase 34 (Stripe Pro Billing) can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/33-pricing-page/33-03-SUMMARY.md`
</output>
