---
phase: 43-verify-phase-37-email-onboarding-sequence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/37-email-onboarding-sequence/37-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ESEQ-01
  - ESEQ-02
  - ESEQ-03
  - ESEQ-04

must_haves:
  truths:
    - "37-VERIFICATION.md exists in the Phase 37 directory with status: passed"
    - "Each ESEQ requirement (ESEQ-01 through ESEQ-04) has a row in the VERIFICATION.md Requirements Coverage table marked SATISFIED with direct file evidence"
    - "All 10 Phase 37 tests are GREEN at verification time (live run, not historical count)"
    - "REQUIREMENTS.md marks ESEQ-01 through ESEQ-04 as [x] Complete"
  artifacts:
    - path: ".planning/phases/37-email-onboarding-sequence/37-VERIFICATION.md"
      provides: "Phase 37 verification report with status: passed"
      contains: "ESEQ-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ESEQ requirements marked complete"
      contains: "[x] **ESEQ-01**"
  key_links:
    - from: "37-VERIFICATION.md"
      to: "server/src/services/onboarding.service.ts"
      via: "Read tool — direct file evidence for ESEQ-01/02"
      pattern: "enrollInOnboardingSequence"
    - from: "37-VERIFICATION.md"
      to: "server/src/auth.ts"
      via: "Read tool + grep — databaseHooks hook evidence"
      pattern: "databaseHooks"
    - from: "37-VERIFICATION.md"
      to: "client/src/pages/register.ts"
      via: "Read tool + grep — ESEQ-04 checkbox evidence"
      pattern: "marketing-consent"
---

<objective>
Run gsd-verifier against Phase 37 to close the ESEQ-01 through ESEQ-04 orphaned requirement gap — producing 37-VERIFICATION.md and marking the requirements complete in REQUIREMENTS.md.

Purpose: Phase 37 (Email Onboarding Sequence) was fully implemented and human-verified in February 2026, but its VERIFICATION.md was never written. The v5.0 milestone audit classified ESEQ-01 through ESEQ-04 as "orphaned" because the verification document is missing. This phase closes that gap by independently verifying each Phase 37 artifact exists and is correctly wired, running the 10 Phase 37 tests live, and producing a VERIFICATION.md that follows the standard format established in Phases 36 and 38.

Output: `.planning/phases/37-email-onboarding-sequence/37-VERIFICATION.md` (status: passed), REQUIREMENTS.md updated to mark ESEQ-01/02/03/04 as [x].
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INVARIANTS.md
@.planning/phases/37-email-onboarding-sequence/37-01-PLAN.md
@.planning/phases/37-email-onboarding-sequence/37-02-PLAN.md
@.planning/phases/37-email-onboarding-sequence/37-01-SUMMARY.md
@.planning/phases/37-email-onboarding-sequence/37-02-SUMMARY.md
@.planning/phases/37-email-onboarding-sequence/37-03-SUMMARY.md
@.planning/phases/38-feedback-links/38-VERIFICATION.md

<interfaces>
<!-- Standard VERIFICATION.md format. Derived from 38-VERIFICATION.md (most recent prior example). -->

## VERIFICATION.md Frontmatter
```yaml
---
phase: 37-email-onboarding-sequence
verified: [ISO8601 timestamp at verification time]
status: passed   # or failed
score: N/N must-haves verified
re_verification: false
human_verification:
  - test: "Loops loop published and active with 'registered' trigger"
    expected: "3-email sequence active in Loops.so dashboard"
    why_human: "External platform state — Loops has no programmatic loop-status API. Verified by user on 2026-02-27 per 37-03-SUMMARY.md."
---
```

## VERIFICATION.md Section Structure (required sections, in order)
1. Header: Phase Goal, Verified timestamp, Status, Re-verification note
2. ## Goal Achievement
   - ### Observable Truths table: | # | Truth | Status | Evidence |
   - ### Required Artifacts table: | Artifact | Expected | Status | Details |
   - ### Key Link Verification table: | From | To | Via | Status | Details |
   - ### Requirements Coverage table: | Requirement | Source Plan | Description | Status | Evidence |
   - ### Anti-Patterns Found
   - ### Test Results (live test output block)
   - ### Human Verification (Previously Completed)
3. ## ZK Invariant Verification

## Phase 37 Artifacts to Verify (from 37-01/02-PLAN.md must_haves)

From Plan 01 must_haves.artifacts:
- drizzle/0006_add_marketing_consent.sql — contains "marketing_consent"
- server/src/db/schema.ts — contains "marketingConsent"
- server/src/auth.ts — contains "additionalFields" AND "databaseHooks"
- client/src/pages/register.ts — contains "marketing-consent"
- server/src/services/onboarding.service.test.ts — contains "enrollInOnboardingSequence"
- server/src/services/billing.service.test.ts — contains "activatePro"
- client/src/pages/register.test.ts — contains "marketing-consent"

From Plan 02 must_haves.artifacts:
- server/src/config/loops.ts — contains "LoopsClient"
- server/src/services/onboarding.service.ts — contains "enrollInOnboardingSequence"
- server/src/auth.ts — contains "databaseHooks"
- server/src/services/billing.service.ts — contains "updateContact"

## Key Links to Verify (from 37-02-PLAN.md must_haves.key_links)
- auth.ts databaseHooks.user.create.after → onboarding.service.ts via "enrollInOnboardingSequence"
- onboarding.service.ts → config/loops.ts via loops.sendEvent
- billing.service.ts activatePro → config/loops.ts via loops.updateContact

## Critical Patterns (per 43-RESEARCH.md pitfall warnings)
- databaseHooks hook is NON-async (void + .catch() pattern) — do NOT flag as incorrect
- Correct pattern: `after: (user) => { void enrollInOnboardingSequence({...}).catch(...) }`
- Marketing consent checkbox is in register.ts (NOT login.ts)
- Test count: run npm run test:run live and report CURRENT count, not the historical "328" from SUMMARY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 37 artifacts via direct file inspection and live test run</name>
  <files></files>
  <action>
This task independently verifies every Phase 37 artifact using Read and Grep tools — NEVER relying solely on SUMMARY file claims as evidence. Each artifact must be confirmed present on disk with file evidence.

**Step 1: Verify all Phase 37 source artifacts using Read + Grep**

For each artifact below, use the Read tool to open the file and then use Grep to confirm the `contains` pattern. Record: file found (Y/N), pattern found (Y/N), line number(s).

Artifacts to verify:

a) `drizzle/0006_add_marketing_consent.sql` — contains "marketing_consent"
b) `server/src/db/schema.ts` — contains "marketingConsent"
c) `server/src/auth.ts` — contains "additionalFields" AND "databaseHooks"
d) `client/src/pages/register.ts` — contains "marketing-consent" checkbox that is `checked = false` by default
e) `server/src/config/loops.ts` — contains "LoopsClient" singleton
f) `server/src/services/onboarding.service.ts` — contains "enrollInOnboardingSequence" exported function that calls `loops.sendEvent`
g) `server/src/services/billing.service.ts` — contains "updateContact" called in activatePro()
h) `server/src/services/onboarding.service.test.ts` — contains "enrollInOnboardingSequence" (test file exists)
i) `server/src/services/billing.service.test.ts` — contains "activatePro" (test file exists)
j) `client/src/pages/register.test.ts` — contains "marketing-consent" (test file exists)

**Step 2: Verify Key Links via Grep**

For each key link, use Grep to confirm the wiring pattern exists:

a) `auth.ts` → `onboarding.service.ts`: grep for "enrollInOnboardingSequence" in server/src/auth.ts — must find import AND call site
b) `auth.ts` databaseHooks hook non-async pattern: grep for "databaseHooks" in server/src/auth.ts — must find it; the hook function should be `(user) =>` NOT `async (user) =>` (non-async is correct)
c) `onboarding.service.ts` → `loops.ts`: grep for "sendEvent" in server/src/services/onboarding.service.ts — must find call with eventName 'registered'
d) `billing.service.ts` → `loops.ts`: grep for "updateContact" in server/src/services/billing.service.ts — must find call in activatePro
e) `register.ts` → `authClient.signUp.email()`: grep for "marketingConsent" in client/src/pages/register.ts — must confirm it is passed to signUp call

**Step 3: ZK Invariant check**

Grep for "userId" OR "user_id" in server/src/services/onboarding.service.ts — expected: zero matches.
Confirm onboarding.service.ts uses only email + non-identifying properties (firstName, marketingConsent, subscriptionTier).

**Step 4: Run Phase 37 tests live**

Run the Phase 37 test files directly:
```bash
cd /Users/ourcomputer/Github-Repos/secureshare && npx vitest run server/src/services/onboarding.service.test.ts server/src/services/billing.service.test.ts client/src/pages/register.test.ts --project server --project client 2>&1 | tail -30
```

Capture the actual output — count of tests passed, any failures. Report the real live count.

Then run the full suite to confirm no regressions:
```bash
cd /Users/ourcomputer/Github-Repos/secureshare && npm run test:run 2>&1 | tail -20
```

Report the CURRENT total test count (will be higher than the historical "328" from Phase 37 SUMMARY due to later phases adding tests).

**Step 5: Verify git commits from SUMMARY files**

Check that the two key commits from 37-02-SUMMARY.md exist in git history:
```bash
cd /Users/ourcomputer/Github-Repos/secureshare && git show --oneline -1 814ee1d 2>/dev/null && git show --oneline -1 9b2b25d 2>/dev/null
```

Note: "FOUND" or "NOT FOUND" — if not found, mark as "unable to verify via git log" but do not mark artifact as failed (commit SHAs may have been rebased; artifact existence on disk is the authoritative evidence).

**Record all findings** — you will use them to write 37-VERIFICATION.md in Task 2.
  </action>
  <verify>
    <automated>cd /Users/ourcomputer/Github-Repos/secureshare && npx vitest run server/src/services/onboarding.service.test.ts server/src/services/billing.service.test.ts client/src/pages/register.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>
    - All 10 Phase 37 tests GREEN (6 onboarding service + 1 billing service + 3 register)
    - All 10 artifacts confirmed present on disk with file evidence (line numbers recorded)
    - All 5 key links confirmed wired via grep
    - ZK invariant confirmed: no userId in onboarding.service.ts
    - Full test suite count recorded (current, not historical)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 37-VERIFICATION.md and update REQUIREMENTS.md</name>
  <files>
    .planning/phases/37-email-onboarding-sequence/37-VERIFICATION.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
Using the findings from Task 1, write the VERIFICATION.md and update REQUIREMENTS.md.

**1. Write `.planning/phases/37-email-onboarding-sequence/37-VERIFICATION.md`**

Follow the standard format exactly (see 38-VERIFICATION.md as the canonical reference). The document must have:

**Frontmatter:**
```yaml
---
phase: 37-email-onboarding-sequence
verified: [current ISO8601 timestamp]
status: passed
score: N/N must-haves verified
re_verification: false
human_verification:
  - test: "Loops loop published and active with 'registered' trigger"
    expected: "3-email sequence (welcome, day-3, day-7) active in Loops.so dashboard; welcome email personalized with {{firstName}}; CTA links correct"
    why_human: "External platform state — Loops.so has no programmatic loop-status API. Verified live by user on 2026-02-27 per 37-03-SUMMARY.md. Re-verification requires browser access to Loops dashboard."
---
```

**Phase header section:**
```
# Phase 37: Email Onboarding Sequence Verification Report

**Phase Goal:** Every new account holder automatically receives a timed 3-email onboarding sequence (welcome, key features, upgrade prompt) triggered by a single Loops.so 'registered' event fired after registration.
**Verified:** [timestamp]
**Status:** passed
**Re-verification:** No — initial verification (Phase 37 was executed 2026-02-27; VERIFICATION.md was not produced at that time)
```

**## Goal Achievement section:**

### Observable Truths table — 5 truths derived from Phase 37 must_haves:

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | New registration fires Loops sendEvent('registered') with email, firstName, marketingConsent, subscriptionTier | VERIFIED | [file + line evidence from Task 1] |
| 2 | Welcome email fires for ALL registrations regardless of marketingConsent value (unconditional) | VERIFIED | [evidence: no audience filter on node 1; onboarding.service.ts fires sendEvent for all users regardless of consent] |
| 3 | If Loops API fails, registration still succeeds (fire-and-forget via void + .catch()) | VERIFIED | [evidence: auth.ts hook pattern — non-async, void + .catch()] |
| 4 | activatePro() syncs subscriptionTier: 'pro' to Loops contact to suppress day-7 upgrade email for paying users | VERIFIED | [evidence: billing.service.ts updateContact call] |
| 5 | Registration form has marketing consent checkbox (id='marketing-consent', unchecked by default) | VERIFIED | [evidence: register.ts lines found in Task 1] |

### Required Artifacts table — use exact file + pattern evidence from Task 1 for every row:

Include all 10 artifacts from Phase 37 Plans 01 and 02. Format:
| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
For each: path, what it provides, VERIFIED or NOT FOUND, actual line numbers where key patterns were found.

### Key Link Verification table — use grep evidence from Task 1:

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
Include all 5 key links. For each: actual line reference confirming the wiring exists.

### Requirements Coverage table:

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| ESEQ-01 | 37-01-PLAN.md, 37-02-PLAN.md | New account holder automatically receives welcome email immediately after registration | SATISFIED | [onboarding.service.ts sendEvent('registered') fires unconditionally; databaseHooks.user.create.after wired] |
| ESEQ-02 | 37-02-PLAN.md | New account holder receives key features email on day 3 (marketing consent required) | SATISFIED | [marketingConsent contact property passed to Loops; day-3 filter enforced in Loops UI — human-verified 2026-02-27] |
| ESEQ-03 | 37-02-PLAN.md | New account holder receives upgrade prompt email on day 7 linking to live Stripe Checkout (marketing consent required; skipped if already Pro) | SATISFIED | [activatePro() syncs subscriptionTier: 'pro' to Loops contact; day-7 dual filter enforced in Loops UI — human-verified 2026-02-27] |
| ESEQ-04 | 37-01-PLAN.md | Marketing consent opt-in checkbox added to registration form (gates emails 2-3 per GDPR) | SATISFIED | [register.ts marketing-consent checkbox; checked=false by default; signUp.email() passes marketingConsent value] |

### Anti-Patterns Found:

Perform a quick check:
```bash
grep -n "TODO\|FIXME\|placeholder\|stub\|// TODO" /Users/ourcomputer/Github-Repos/secureshare/server/src/services/onboarding.service.ts /Users/ourcomputer/Github-Repos/secureshare/server/src/config/loops.ts 2>/dev/null
```
Report findings (likely none). If any found, note them.

### Test Results:

Include the ACTUAL live test output block from Task 1 Step 4 (the targeted Phase 37 tests run). Report current total test count from full suite.

### Human Verification (Previously Completed):

Document the Loops loop verification as previously completed — do NOT re-trigger it. Format:

```
Per 37-03-SUMMARY.md, a human completed the Loops dashboard configuration and end-to-end verification on 2026-02-27:

| Item | Result |
|------|--------|
| Loops loop Active with trigger: 'registered' | CONFIRMED |
| 7-node structure (welcome → timer → filter → day-3 → timer → filter → day-7) | CONFIRMED |
| Welcome email delivered to Mailinator within ~30s | CONFIRMED |
| Subject "Hey Torchtest, welcome to Torch Secret" with firstName personalization | CONFIRMED |
| From address: hello@torchsecret.com | CONFIRMED |
| Day-7 CTA links to https://torchsecret.com/pricing | CONFIRMED |
| Day-3 node: marketingConsent=true audience filter | CONFIRMED |
| Day-7 node: marketingConsent=true AND subscriptionTier!=pro dual filter | CONFIRMED |

These items cannot be re-verified programmatically (external platform state). The code evidence (correct contact properties sent on every registration, correct Pro tier sync on upgrade) is confirmed above. Loops UI configuration was a one-time setup step.
```

**## ZK Invariant Verification section:**

Include findings from Task 1 Step 3 (grep for userId in onboarding.service.ts). Confirm: onboarding.service.ts passes only email + non-identifying properties; no userId in Loops calls or error logs; billing.service.ts uses stripeCustomerId lookup to get email (same pattern as getOrCreateStripeCustomer — userId not in scope).

---

**2. Update `.planning/REQUIREMENTS.md`**

Read REQUIREMENTS.md. Find the Email Onboarding Sequence (ESEQ) section. Change each `[ ]` to `[x]` for ESEQ-01, ESEQ-02, ESEQ-03, and ESEQ-04:

```markdown
- [x] **ESEQ-01**: New account holder automatically receives welcome email immediately after registration
- [x] **ESEQ-02**: New account holder receives key features email on day 3 (marketing consent required)
- [x] **ESEQ-03**: New account holder receives upgrade prompt email on day 7 linking to live Stripe Checkout (marketing consent required; skipped if already Pro)
- [x] **ESEQ-04**: Marketing consent opt-in checkbox added to registration form (gates emails 2-3 per GDPR)
```

Also find the Traceability table and update the four ESEQ rows from `Pending` to `Complete`:

```markdown
| ESEQ-01 | Phase 43 | Complete |
| ESEQ-02 | Phase 43 | Complete |
| ESEQ-03 | Phase 43 | Complete |
| ESEQ-04 | Phase 43 | Complete |
```

Also update the last-updated note at the bottom of REQUIREMENTS.md to reflect the current date.
  </action>
  <verify>
    <automated>grep -c "\[x\].*ESEQ" /Users/ourcomputer/Github-Repos/secureshare/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - .planning/phases/37-email-onboarding-sequence/37-VERIFICATION.md written with status: passed
    - VERIFICATION.md has 5 Observable Truths (all VERIFIED), 10 Required Artifacts (all VERIFIED), 5 Key Links (all WIRED), 4 Requirements Coverage rows (all SATISFIED)
    - VERIFICATION.md includes live test output (current count, not historical 328)
    - VERIFICATION.md includes Human Verification (Previously Completed) section documenting Loops loop verification from 37-03-SUMMARY.md
    - REQUIREMENTS.md: ESEQ-01 through ESEQ-04 all changed from [ ] to [x]
    - REQUIREMENTS.md Traceability table: ESEQ rows updated from Pending to Complete
    - grep -c "\[x\].*ESEQ" REQUIREMENTS.md returns 4
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run:

```bash
cd /Users/ourcomputer/Github-Repos/secureshare && grep -c "\[x\].*ESEQ" .planning/REQUIREMENTS.md && echo "---" && head -10 .planning/phases/37-email-onboarding-sequence/37-VERIFICATION.md
```

Expected:
- First command: `4` (all four ESEQ requirements marked complete)
- Second command: frontmatter with `status: passed`
</verification>

<success_criteria>
- 37-VERIFICATION.md exists at `.planning/phases/37-email-onboarding-sequence/37-VERIFICATION.md` with `status: passed`
- VERIFICATION.md score shows all must-haves verified (e.g., 5/5 or 7/7 depending on final truth count)
- Every artifact row in VERIFICATION.md has "VERIFIED" status with direct file evidence (line numbers, not just SUMMARY references)
- All 10 Phase 37 tests GREEN in live run (onboarding.service.test.ts x6, billing.service.test.ts x1, register.test.ts x3)
- Full test suite continues to pass (no regressions from Phase 43 activity — Phase 43 writes no production code)
- REQUIREMENTS.md: 4 ESEQ requirements marked [x] (was [ ]) — confirmed by `grep -c "\[x\].*ESEQ"` returning 4
- REQUIREMENTS.md Traceability table: ESEQ-01/02/03/04 rows show Phase 43 and Complete
</success_criteria>

<output>
After completion, create `.planning/phases/43-verify-phase-37-email-onboarding-sequence/43-01-SUMMARY.md`
</output>
