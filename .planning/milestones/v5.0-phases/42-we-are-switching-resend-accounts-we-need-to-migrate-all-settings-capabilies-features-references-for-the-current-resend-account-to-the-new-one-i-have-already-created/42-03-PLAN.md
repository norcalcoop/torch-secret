---
phase: 42-resend-account-migration
plan: 03
type: execute
wave: 3
depends_on: [42-02]
files_modified: []
autonomous: false
requirements: [RESEND-MIGRATE-03]

must_haves:
  truths:
    - "Dev server starts cleanly with new credentials (Zod startup validation passes)"
    - "A real email sent from dev reaches the inbox via the new Resend account"
    - "Old API key is revoked from the old Resend dashboard"
    - "Existing test suite still passes (no regressions from env-only change)"
  artifacts:
    - path: "(no code artifacts — pure credential verification)"
      provides: "Confirmed working end-to-end Resend delivery on new account"
  key_links:
    - from: "New RESEND_API_KEY in local .env"
      to: "server/src/services/email.ts Resend singleton"
      via: "env.RESEND_API_KEY injected at startup via infisical run --env=dev --"
      pattern: "new Resend(env.RESEND_API_KEY)"
    - from: "New RESEND_AUDIENCE_ID in Infisical"
      to: "subscribers.service.ts resend.contacts.create({ audienceId })"
      via: "env.RESEND_AUDIENCE_ID at runtime"
      pattern: "audienceId: env.RESEND_AUDIENCE_ID"
---

<objective>
Verify that the new Resend credentials work end-to-end, then revoke the old API key. This closes the migration.

Purpose: Credentials migration is not complete until a real email is delivered via the new account and the old key is safely decommissioned. Revoking the old key is a one-way action — confirm new credentials first.
Output: Working Resend delivery on new account; old API key revoked; test suite green; migration complete.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-we-are-switching-resend-accounts-we-need-to-migrate-all-settings-capabilies-features-references-for-the-current-resend-account-to-the-new-one-i-have-already-created/42-CONTEXT.md
@.planning/phases/42-we-are-switching-resend-accounts-we-need-to-migrate-all-settings-capabilies-features-references-for-the-current-resend-account-to-the-new-one-i-have-already-created/42-RESEARCH.md
@.planning/phases/42-we-are-switching-resend-accounts-we-need-to-migrate-all-settings-capabilies-features-references-for-the-current-resend-account-to-the-new-one-i-have-already-created/42-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke test — server startup and test suite</name>
  <files></files>
  <action>
    Run two automated checks to confirm the new credentials are correctly wired and no code regressions exist.

    **Check 1 — Zod startup validation (smoke test):**
    Start the dev server with Infisical-injected env vars. A clean startup proves all three Resend vars are present and non-empty (Zod `z.string().min(1)` validates at import time). A Zod parse error means a variable is missing or empty.

    ```bash
    # Start dev server with new dev credentials; Ctrl-C after confirming startup
    infisical run --env=dev -- npm run dev:server
    ```

    Watch for: clean startup log lines (Pino format). A Zod error like `"RESEND_API_KEY" must not be empty` means the credential update did not propagate correctly — go back to Plan 02 and re-verify Infisical.

    Note: `tsx watch` picks up env vars at process start. If the server was already running from an earlier session, stop it first and restart with the Infisical prefix.

    **Check 2 — Full test suite (no regressions):**
    This phase makes no code changes — tests are mocked and independent of real Resend credentials. The suite should be identical to pre-migration.

    ```bash
    npx vitest run
    ```

    All email-related tests use `vi.mock('../../services/email.js')` — they do not call real Resend API. The test run confirms no accidental file changes crept in. Expected: same pass count as before Phase 42 started (361+ tests, all green).
  </action>
  <verify>
    <automated>npx vitest run</automated>
    Automated: `npx vitest run` — all tests pass (same count as pre-migration).
    Smoke: dev server starts cleanly with `infisical run --env=dev -- npm run dev:server` — no Zod errors in startup output.
  </verify>
  <done>Dev server starts without Zod errors. Full test suite passes with same count as before Phase 42.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real email delivery verification</name>
  <files></files>
  <action>Human triggers a real transactional email from the dev server (password reset or registration flow) to verify the new Resend API key delivers email end-to-end. The Zod startup check in Task 1 only confirms the env vars are present — this step confirms the API key is actually valid with Resend and emails are delivered.</action>
  <what-built>Task 1 confirmed Zod startup validation passes and tests are green. Now verify that the new Resend account actually delivers emails end-to-end — the Zod check only confirms the vars are present, not that the API key is valid with Resend.</what-built>

  <how-to-verify>
    With the dev server running (`infisical run --env=dev -- npm run dev:server`), trigger a real transactional email using one of these methods (choose whichever is easiest):

    **Option A — Password reset email (simplest):**
    1. Visit `http://torchsecret.localhost:1355/forgot-password`
    2. Enter any email address you have access to
    3. Submit the form
    4. Check that inbox — a password reset email should arrive within 30-60 seconds
    5. The email is sent via `resend.emails.send()` using the new `RESEND_API_KEY` and `RESEND_FROM_EMAIL`

    **Option B — Email verification (register flow):**
    1. Visit `http://torchsecret.localhost:1355/register`
    2. Register a new test account with a real email address
    3. Check the inbox for the verification email

    **Confirming success:**
    - Email arrives in inbox within 60 seconds
    - Sender address matches the new `RESEND_FROM_EMAIL` value from Plan 01
    - No errors in the server log (Pino output in terminal where dev server is running)

    **If email does not arrive:**
    - Check the Pino log for `resend_` prefixed error events
    - Verify the API key is valid: log in to the new Resend dashboard → API Keys → check the key's "Last used" timestamp updates after the request
    - Check Resend dashboard → Logs to see if the send request was received and what status it returned
  </how-to-verify>

  <verify>Email arrives in inbox from the new Resend account within 60 seconds of triggering a password reset or registration in dev.</verify>
  <done>Real transactional email delivered to inbox via new Resend account credentials.</done>
  <resume-signal>Type "email delivered" after confirming the email arrived in your inbox, or describe the error if it failed.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Revoke old Resend API key</name>
  <files></files>
  <action>Human revokes the old Resend API key from the old account dashboard. This is a one-way action that must only happen after Task 1 and Task 2 confirm the new credentials are fully working. No CLI automation possible — API key revocation requires the Resend web dashboard.</action>
  <what-built>New credentials are confirmed working (Task 1 + Task 2). The old API key is now safe to revoke — revocation is a one-way action and should only happen after the new key is fully verified.</what-built>

  <how-to-verify>
    1. Log in to the OLD Resend account (the account associated with key `re_hNmZgKfp_...`)
    2. Navigate to: API Keys (left sidebar)
    3. Find the key that was used for Torch Secret (it starts with `re_hNmZgKfp_`)
    4. Click the delete/revoke button for that key
    5. Confirm the revocation when prompted

    **After revoking:**
    - The old key `re_hNmZgKfp_...` no longer appears in the old Resend account's API Keys list
    - No Infisical environment or local .env references the old key (all were updated in Plan 02)
    - The old Audience (`9ef8f5aa-97f3-4012-b26e-aad3f153cb7f`) can be left abandoned — it has only test contacts and is now orphaned from any code reference

    **Note:** You do not need to delete the old Audience or the old Resend account — simply revoking the API key is sufficient to complete the migration decommission.
  </how-to-verify>

  <verify>Old API key `re_hNmZgKfp_...` no longer appears in the old Resend account's API Keys list.</verify>
  <done>Old API key revoked. Migration fully decommissioned.</done>
  <resume-signal>Type "key revoked" after revoking the old API key from the old Resend account dashboard.</resume-signal>
</task>

</tasks>

<verification>
Migration complete when:
1. `npx vitest run` passes — all tests green, no regressions
2. Dev server starts cleanly with `infisical run --env=dev --` prefix — no Zod startup errors
3. Real email delivered to inbox from new Resend account (password reset or registration flow)
4. Old API key `re_hNmZgKfp_...` revoked from old Resend account dashboard
</verification>

<success_criteria>
- New Resend account delivers transactional emails end-to-end in dev
- Old API key revoked (decommissioned)
- All four Infisical environments (dev, staging, prod) and local .env hold only new account credentials
- No code files were modified during Phase 42 (pure credentials migration)
- Test suite passes with same count as before Phase 42
</success_criteria>

<output>
After completion, create `.planning/phases/42-we-are-switching-resend-accounts-we-need-to-migrate-all-settings-capabilies-features-references-for-the-current-resend-account-to-the-new-one-i-have-already-created/42-03-SUMMARY.md`
</output>
