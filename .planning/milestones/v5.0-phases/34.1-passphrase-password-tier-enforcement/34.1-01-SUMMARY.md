---
phase: 34.1-passphrase-password-tier-enforcement
plan: "01"
subsystem: api
tags: [zod, drizzle, tier-enforcement, protection-type, secrets, integration-tests]

# Dependency graph
requires:
  - phase: 34-stripe-pro-billing
    provides: subscriptionTier column on users table and Pro/free tier distinction
provides:
  - protection_type enum in CreateSecretSchema (none/passphrase/password) with default 'none'
  - Server-side tier enforcement guard in POST /api/secrets: anonymous blocked from passphrase/password, free blocked from password, Pro allowed all
  - 6 integration tests covering all tier x protection_type combinations
affects: [34.1-02, create-page, passphrase-ui, password-ui]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Tier enforcement guard pattern: protectionType !== 'none' → check userId → check subscriptionTier from DB (avoid unnecessary DB round-trip for passphrase-only)"
    - "Pro user elevation in tests: create user via createUserAndSignIn, then direct Drizzle update sets subscriptionTier='pro' before test request"

key-files:
  created: []
  modified:
    - shared/types/api.ts
    - server/src/routes/secrets.ts
    - server/src/routes/__tests__/secrets.test.ts

key-decisions:
  - "Anonymous users blocked from passphrase AND password (403 passphrase_not_allowed) — no DB lookup needed, just check !userId"
  - "Free users blocked from password type (403 pro_required) via DB lookup — passphrase allowed without DB round-trip (only userId check)"
  - "protection_type defaults to 'none' via Zod .default() — backwards-compatible, existing clients unaffected"
  - "error codes: passphrase_not_allowed for anonymous, pro_required for free-tier password attempt — distinct codes for client-side messaging"

patterns-established:
  - "Tier cap guard block placed after expiresIn caps, before createSecret() call — all server-side guards co-located in POST handler"

requirements-completed: [TIER-01, TIER-02]

# Metrics
duration: 2min
completed: 2026-02-26
---

# Phase 34.1 Plan 01: Passphrase/Password Tier Enforcement Summary

**Server-side protection_type tier guard: anonymous blocked from passphrases, free users blocked from custom passwords, Pro users allowed all — enforced via Zod enum + route handler with targeted DB lookup**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-26T01:25:06Z
- **Completed:** 2026-02-26T01:27:12Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added `protection_type: z.enum(['none', 'passphrase', 'password']).optional().default('none')` to `CreateSecretSchema` — backwards-compatible, existing API callers unaffected
- Implemented tier enforcement guard in POST /api/secrets: anonymous → 403 `passphrase_not_allowed`; free + password → 403 `pro_required`; Pro + any → allowed
- 6 new integration tests pass covering all tier × protection_type combinations; all 271 existing tests continue to pass

## Task Commits

Each task was committed atomically:

1. **Task 1: Add protection_type to CreateSecretSchema and enforce tier caps** - `6b82c4f` (feat)
2. **Task 2: Add integration tests for protection_type tier enforcement** - `f313bb4` (test)

**Plan metadata:** (docs commit — see final commit)

## Files Created/Modified
- `shared/types/api.ts` - Added `protection_type` field to `CreateSecretSchema` with JSDoc comment
- `server/src/routes/secrets.ts` - Added `db`, `users`, `eq` imports; added tier enforcement guard block between expiresIn caps and createSecret() call
- `server/src/routes/__tests__/secrets.test.ts` - Appended 99-line describe block with 6 new tests

## Decisions Made
- Anonymous users get `passphrase_not_allowed` for both passphrase and password types — simpler for client to handle, consistent message, no DB lookup needed
- Free users with `protection_type: 'password'` trigger a DB lookup for subscriptionTier — avoids the lookup for `passphrase` (free users allowed)
- `protection_type` added as optional with `.default('none')` — Zod handles defaulting so existing callers that omit the field continue to work without any change

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Tier enforcement is complete and tested at the API layer
- Ready for Phase 34.1 Plan 02 (client-side UI enforcement — passphrase/password controls gated by tier in create page)
- The `protection_type` field is now available in `CreateSecretRequest` type for client usage

---
*Phase: 34.1-passphrase-password-tier-enforcement*
*Completed: 2026-02-26*

## Self-Check: PASSED

- FOUND: shared/types/api.ts
- FOUND: server/src/routes/secrets.ts
- FOUND: server/src/routes/__tests__/secrets.test.ts
- FOUND: .planning/phases/34.1-passphrase-password-tier-enforcement/34.1-01-SUMMARY.md
- FOUND commit: 6b82c4f (feat — Task 1)
- FOUND commit: f313bb4 (test — Task 2)
