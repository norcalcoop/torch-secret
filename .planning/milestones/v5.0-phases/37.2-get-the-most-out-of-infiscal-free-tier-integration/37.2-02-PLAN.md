---
phase: 37.2-get-the-most-out-of-infiscal-free-tier-integration
plan: 02
type: execute
wave: 2
depends_on: ["37.2-01"]
files_modified:
  - .infisical.json
  - package.json
  - .github/workflows/ci.yml
  - .env.example
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - ".infisical.json exists and is committed (contains workspaceId, no secrets)"
    - "npm run dev:server and npm run dev:client are prefixed with infisical run --env=dev --"
    - "npm run staging:up script exists"
    - "CI test and e2e jobs use Infisical secrets-action instead of hardcoded placeholder env vars"
    - ".env.example has keys and comments only — no secret values, and BETTER_AUTH_TRUSTED_ORIGINS is documented"
    - "README local dev section replaced with Infisical onboarding steps"
  artifacts:
    - path: ".infisical.json"
      provides: "Infisical project binding — eliminates --projectId flag from all scripts"
      contains: "workspaceId"
    - path: "package.json"
      provides: "Updated dev:server, dev:client, and new staging:up scripts"
    - path: ".github/workflows/ci.yml"
      provides: "CI test + e2e jobs using Infisical secrets-action with Universal Auth"
    - path: ".env.example"
      provides: "Documentation-only file: all key names + comments, no values, BETTER_AUTH_TRUSTED_ORIGINS added"
    - path: "README.md"
      provides: "Updated local dev setup section with Infisical CLI install + login + npm run dev"
  key_links:
    - from: ".infisical.json"
      to: "infisical run --env=dev --"
      via: "workspaceId resolves the correct project; --env selects environment"
      pattern: "workspaceId in .infisical.json"
    - from: "Infisical secrets-action step"
      to: "subsequent CI steps"
      via: "secrets injected as env vars before setup-node/npm ci steps"
      pattern: "uses: Infisical/secrets-action@v1.0.9"
    - from: "job-level env block in CI"
      to: "CI-specific overrides"
      via: "job env: takes precedence over step-injected vars"
      pattern: "DATABASE_URL: postgresql://secureshare:secureshare@localhost:5432/secureshare"
---

<objective>
Wire the Infisical project into the codebase: create .infisical.json, prefix npm dev scripts, add staging:up, replace CI hardcoded placeholder env vars with Infisical secrets-action (preserving CI-specific overrides), strip values from .env.example, and update README. No application logic changes.

Purpose: After this plan, every environment (local dev, staging, CI, production) pulls secrets from the single Infisical source of truth. The developer workflow becomes: install CLI + login + npm run dev.
Output: Five files modified; codebase is ready for smoke-test wiring in Plan 03.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-CONTEXT.md
@.planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-RESEARCH.md
@.planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-01-SUMMARY.md

<interfaces>
<!-- Current package.json scripts (before migration): -->
```json
"dev:server": "tsx watch server/src/server.ts",
"dev:client": "portless torchsecret sh -c 'npx vite --port $PORT --host $HOST'"
```

<!-- Current ci.yml test job env block (12 hardcoded vars): -->
```yaml
env:
  DATABASE_URL: postgresql://secureshare:secureshare@localhost:5432/secureshare
  NODE_ENV: test
  BETTER_AUTH_SECRET: ci-test-secret-placeholder-must-be-at-least-32-chars
  BETTER_AUTH_URL: http://localhost:3000
  APP_URL: http://localhost:3000
  RESEND_API_KEY: re_placeholder
  RESEND_FROM_EMAIL: "Torch Secret <noreply@torchsecret.com>"
  STRIPE_SECRET_KEY: sk_test_placeholder
  STRIPE_WEBHOOK_SECRET: whsec_placeholder
  STRIPE_PRO_PRICE_ID: price_placeholder
  RESEND_AUDIENCE_ID: aud_placeholder
  IP_HASH_SALT: ci-test-ip-hash-salt-placeholder
  LOOPS_API_KEY: loops_placeholder
```

<!-- Current ci.yml e2e job env block (11 hardcoded vars — missing LOOPS_API_KEY): -->
```yaml
env:
  DATABASE_URL: postgresql://secureshare:secureshare@localhost:5432/secureshare
  REDIS_URL: redis://localhost:6379
  NODE_ENV: test
  BETTER_AUTH_SECRET: ci-test-secret-placeholder-must-be-at-least-32-chars
  BETTER_AUTH_URL: http://localhost:3000
  APP_URL: http://localhost:3000
  RESEND_API_KEY: re_placeholder
  RESEND_FROM_EMAIL: "Torch Secret <noreply@torchsecret.com>"
  STRIPE_SECRET_KEY: sk_test_placeholder
  STRIPE_WEBHOOK_SECRET: whsec_placeholder
  STRIPE_PRO_PRICE_ID: price_placeholder
  RESEND_AUDIENCE_ID: aud_placeholder
  IP_HASH_SALT: ci-test-ip-hash-salt-placeholder
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .infisical.json and update package.json scripts</name>
  <files>.infisical.json, package.json</files>
  <action>
**1. Create `.infisical.json` in the repo root.**

Read the workspaceId and project-slug from `37.2-01-SUMMARY.md`. Create the file:

```json
{
  "workspaceId": "<workspaceId-from-01-SUMMARY>",
  "defaultEnvironment": "dev",
  "gitBranchToEnvironmentMapping": null
}
```

This file is safe to commit — it contains no secrets. It binds all `infisical run` calls to the correct project without requiring `--projectId` flags.

**2. Update `package.json` scripts.**

Modify exactly these three script values (leave all other scripts unchanged):

```json
"dev:server": "infisical run --env=dev -- tsx watch server/src/server.ts",
"dev:client": "infisical run --env=dev -- portless torchsecret sh -c 'npx vite --port $PORT --host $HOST'",
"staging:up": "infisical run --env=staging -- docker-compose up",
```

Insert `staging:up` after `dev:client` (before `build:client`). The double dash `--` separates Infisical flags from the wrapped command — do NOT use `--command=` syntax.

**Verify no other scripts are changed** — build:client, test, lint, format, etc. must remain identical.
  </action>
  <verify>
```bash
# Verify .infisical.json exists and has workspaceId
cat /Users/ourcomputer/Github-Repos/secureshare/.infisical.json | grep workspaceId

# Verify scripts contain infisical run prefix
grep "infisical run" /Users/ourcomputer/Github-Repos/secureshare/package.json

# Verify staging:up exists
grep "staging:up" /Users/ourcomputer/Github-Repos/secureshare/package.json

# Run tests to confirm no regressions
cd /Users/ourcomputer/Github-Repos/secureshare && npx vitest run --reporter=dot
```
  </verify>
  <done>
- `.infisical.json` exists at repo root with correct workspaceId and `defaultEnvironment: "dev"`
- `dev:server` is `"infisical run --env=dev -- tsx watch server/src/server.ts"`
- `dev:client` is `"infisical run --env=dev -- portless torchsecret sh -c 'npx vite --port $PORT --host $HOST'"`
- `staging:up` is `"infisical run --env=staging -- docker-compose up"`
- All 347 tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ci.yml, strip .env.example values, update README</name>
  <files>.github/workflows/ci.yml, .env.example, README.md</files>
  <action>
**1. Update `.github/workflows/ci.yml`**

Modify the `test` job and `e2e` job. Leave `lint` and `docker-build` jobs completely unchanged.

For the **`test` job:**
- Keep the job-level `env:` block but reduce it to CI-specific infrastructure overrides only (these must NOT come from Infisical because CI uses its own postgres service container):
  ```yaml
  env:
    DATABASE_URL: postgresql://secureshare:secureshare@localhost:5432/secureshare
    NODE_ENV: test
    BETTER_AUTH_URL: http://localhost:3000
    APP_URL: http://localhost:3000
    RESEND_FROM_EMAIL: "Torch Secret <noreply@torchsecret.com>"
  ```
  Remove these from job-level env (Infisical will inject them): BETTER_AUTH_SECRET, RESEND_API_KEY, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRO_PRICE_ID, RESEND_AUDIENCE_ID, IP_HASH_SALT, LOOPS_API_KEY

- Add the Infisical secrets-action step as the **first step after `actions/checkout@v4`** (before `actions/setup-node@v4`). Read project-slug from `37.2-01-SUMMARY.md`:
  ```yaml
  - name: Import secrets from Infisical
    uses: Infisical/secrets-action@v1.0.9
    with:
      method: "universal"
      client-id: ${{ secrets.INFISICAL_CLIENT_ID }}
      client-secret: ${{ secrets.INFISICAL_CLIENT_SECRET }}
      project-slug: "<project-slug-from-01-SUMMARY>"
      env-slug: "dev"
      domain: "https://app.infisical.com"
      export-type: "env"
  ```

For the **`e2e` job:**
- Keep the job-level `env:` block with CI-specific overrides only:
  ```yaml
  env:
    DATABASE_URL: postgresql://secureshare:secureshare@localhost:5432/secureshare
    REDIS_URL: redis://localhost:6379
    NODE_ENV: test
    BETTER_AUTH_URL: http://localhost:3000
    APP_URL: http://localhost:3000
    RESEND_FROM_EMAIL: "Torch Secret <noreply@torchsecret.com>"
  ```
  Same removals as test job. LOOPS_API_KEY is automatically fixed — Infisical dev env includes it.

- Add identical Infisical secrets-action step as first step after `actions/checkout@v4`.

**Important:** `RESEND_FROM_EMAIL` stays in the job-level env block (it is config, not a secret, and is specific to CI invocations).

**2. Strip values from `.env.example`**

Transform `.env.example` to a pure documentation file — key names, comments, and empty values only. Rules:
- All lines that currently have a real or placeholder value should become `KEY=` (key only, no value)
- Comment lines (starting with `#`) stay unchanged
- Blank lines stay unchanged
- Add `BETTER_AUTH_TRUSTED_ORIGINS=` entry (with a comment) after the `APP_URL=` line — this key is missing from .env.example but is required by the app (documented in CONTEXT.md and RESEARCH.md as a known gap)
- Add `LOOPS_API_KEY=` entry with a comment after `RESEND_AUDIENCE_ID=` if not already present

The result must have NO values after `=` on any non-commented line.

Verify: `grep -E '^[A-Z_]+=.' /Users/ourcomputer/Github-Repos/secureshare/.env.example` should return empty (no keys with values).

**3. Update README.md local dev section**

Find the existing local development / setup section (look for "Prerequisites" or ".env" file instructions). Replace the content of that section with the Infisical onboarding workflow:

```markdown
## Local Development

### Prerequisites
- Node.js 24.x
- PostgreSQL 17+ running locally
- [Infisical CLI](https://infisical.com/docs/cli/overview) installed

### Setup

1. **Install Infisical CLI:**
   ```bash
   brew install infisical/get-cli/infisical   # macOS
   ```
   See [Infisical CLI docs](https://infisical.com/docs/cli/overview) for Linux installation.

2. **Authenticate with Infisical:**
   ```bash
   infisical login
   ```
   This opens a browser. Authenticate with your personal account. Session persists in `~/.infisical/` — only needed once per machine.

3. **Request project access** from the project owner (needed to read secrets from the `torch-secret` Infisical project).

4. **Start development servers:**
   ```bash
   npm run dev:server   # Terminal 1 — Express server on :3000, secrets injected by Infisical
   npm run dev:client   # Terminal 2 — Vite dev server via portless
   ```

No `.env` file needed. Secrets are pulled automatically from the `dev` environment in Infisical.

### Docker Staging

To run the full stack with Docker Compose (staging mode):

```bash
npm run staging:up   # Wraps: infisical run --env=staging -- docker-compose up
```

Requires `infisical login` completed. Pulls secrets from Infisical's `staging` environment.
```

Keep all other README sections intact. Only replace/update the local development setup section.
  </action>
  <verify>
```bash
# Verify .env.example has no values
grep -E '^[A-Z_]+=.' /Users/ourcomputer/Github-Repos/secureshare/.env.example

# Verify BETTER_AUTH_TRUSTED_ORIGINS is in .env.example
grep "BETTER_AUTH_TRUSTED_ORIGINS" /Users/ourcomputer/Github-Repos/secureshare/.env.example

# Verify secrets-action appears in ci.yml twice (test + e2e jobs)
grep -c "secrets-action" /Users/ourcomputer/Github-Repos/secureshare/.github/workflows/ci.yml

# Verify CI-specific overrides remain (DATABASE_URL still in job env)
grep "DATABASE_URL: postgresql://secureshare" /Users/ourcomputer/Github-Repos/secureshare/.github/workflows/ci.yml

# Verify README has infisical login
grep "infisical login" /Users/ourcomputer/Github-Repos/secureshare/README.md

# Run tests — no regressions
cd /Users/ourcomputer/Github-Repos/secureshare && npx vitest run --reporter=dot
```
  </verify>
  <done>
- `grep -E '^[A-Z_]+=.' .env.example` returns empty (no key=value lines)
- `BETTER_AUTH_TRUSTED_ORIGINS=` line exists in .env.example with a comment explaining it
- ci.yml test job: secrets-action step is present; BETTER_AUTH_SECRET, STRIPE_*, RESEND_API_KEY, IP_HASH_SALT, LOOPS_API_KEY are removed from job-level env
- ci.yml e2e job: same pattern; LOOPS_API_KEY gap is fixed via Infisical injection
- DATABASE_URL (CI postgres service URL) and NODE_ENV remain in job-level env for both jobs
- README shows `infisical login` and `npm run dev:server` in setup steps
- All 347 tests pass
  </done>
</task>

</tasks>

<verification>
```bash
# Full verification suite
cd /Users/ourcomputer/Github-Repos/secureshare

# 1. .infisical.json exists with workspaceId
cat .infisical.json | grep workspaceId

# 2. Scripts have infisical prefix
grep "infisical run" package.json

# 3. .env.example is clean (no values)
grep -E '^[A-Z_]+=.' .env.example

# 4. BETTER_AUTH_TRUSTED_ORIGINS documented
grep "BETTER_AUTH_TRUSTED_ORIGINS" .env.example

# 5. CI has secrets-action (twice)
grep -c "secrets-action" .github/workflows/ci.yml

# 6. All tests pass
npx vitest run --reporter=dot
```
</verification>

<success_criteria>
- `.infisical.json` committed with correct workspaceId
- `dev:server` and `dev:client` scripts have `infisical run --env=dev --` prefix
- `staging:up` script exists
- CI test and e2e jobs use Infisical secrets-action; only CI-specific infra vars remain in job-level env
- `.env.example` is documentation-only (no values)
- `BETTER_AUTH_TRUSTED_ORIGINS` is documented in `.env.example`
- README local dev section uses Infisical onboarding steps
- All 347 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-02-SUMMARY.md` with:
- List of files modified
- Final ci.yml structure (test job env keys kept vs removed)
- Confirmation that tests pass
</output>
