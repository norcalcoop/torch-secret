---
phase: 41-update-readme-and-stale-documentation-for-torch-secret-v5-0-launch
plan: 03
type: execute
wave: 2
depends_on: [41-01, 41-02]
files_modified:
  - screenshots/homepage-dark.png
  - screenshots/homepage-light.png
  - screenshots/create-flow.png
  - screenshots/reveal-flow.png
  - screenshots/dashboard.png
  - README.md
autonomous: false
requirements:
  - README_SCREENSHOTS

must_haves:
  truths:
    - screenshots/ directory exists at the repo root
    - At least 3 screenshot PNG files exist in screenshots/ covering the marketing homepage, create flow, and reveal/dashboard flow
    - README.md embeds the screenshots with descriptive alt text using relative paths (screenshots/filename.png)
    - Screenshot filenames in README.md img references match the actual files in screenshots/
  artifacts:
    - screenshots/
    - README.md
  key_links:
    - README.md screenshots section references files that exist in screenshots/
---

<objective>
Take fresh browser screenshots of the Torch Secret v5.0 UI and embed them in README.md.

Purpose: README has no screenshots. New visitors, potential contributors, and anyone evaluating the project cannot see what the product looks like. Screenshots communicate the v5.0 design (glassmorphism, dark/light themes, marketing homepage) in seconds.
Output: screenshots/ directory with 3-5 PNG captures; README.md updated with an embedded Screenshots section.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

### Current README.md state

README.md currently has no screenshots section and no screenshots/ directory. The file ends with:

```
## Contributing

We welcome contributions. See [CONTRIBUTING.md](CONTRIBUTING.md) for setup instructions, coding conventions, and the pull request process.
```

### Screenshot target pages (Claude's Discretion — recommended set)

Five pages that best communicate the v5.0 product value:
1. **homepage-dark.png** — Marketing homepage at `/` in dark mode (hero, tagline, CTA)
2. **homepage-light.png** — Marketing homepage at `/` in light mode (shows the light theme)
3. **create-flow.png** — Create-secret page at `/create` showing the form
4. **reveal-flow.png** — Reveal page showing the terminal-style secret display after viewing
5. **dashboard.png** — Secret dashboard at `/dashboard` (authenticated view)

Alternatively, if the dashboard requires auth setup that is cumbersome during screenshot capture, substitute a confirmation page screenshot.

### App must be running for screenshots

The frontend runs at `http://torchsecret.localhost:1355` via portless. Screenshots require:
- `npm run dev:server` running (Terminal 1)
- `npm run dev:client` running (Terminal 2)
- Both started with `infisical run --env=dev --` prefix (or via the npm scripts which already include this)

The `gsd-uat-browser` skill (agent-browser + portless) is available for automated screenshot capture if preferred.

### README insertion point

Add a "## Screenshots" section immediately before the existing "## Contributing" section. Use relative paths to reference screenshots.
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Capture screenshots with app running</name>
  <files>screenshots/homepage-dark.png, screenshots/homepage-light.png, screenshots/create-flow.png, screenshots/reveal-flow.png, screenshots/dashboard.png</files>
  <action>
Start the development servers and capture screenshots of the v5.0 UI. Steps:

1. Ensure `npm run dev:server` and `npm run dev:client` are both running (open two terminal windows).

2. Create the `screenshots/` directory at the repo root:
   ```bash
   mkdir -p /Users/ourcomputer/Github-Repos/secureshare/screenshots
   ```

3. Navigate to `http://torchsecret.localhost:1355` and capture:
   - **homepage-dark.png** — homepage in dark mode (set via toggle or localStorage)
   - **homepage-light.png** — homepage in light mode
   - **create-flow.png** — `/create` page showing the secret-creation form
   - **reveal-flow.png** — the reveal page after viewing a secret (terminal-style display); create a test secret first, then reveal it
   - **dashboard.png** — `/dashboard` if you can log in; otherwise substitute with the confirmation page after creating a secret

4. Save all screenshots as PNG files named exactly: `homepage-dark.png`, `homepage-light.png`, `create-flow.png`, `reveal-flow.png`, `dashboard.png` (or whatever subset you capture — at least 3 are required).

5. Place all files in the `screenshots/` directory at the repo root.

When done, confirm by running:
```bash
ls /Users/ourcomputer/Github-Repos/secureshare/screenshots/
```

Confirm with: "Screenshots captured: [list the filenames you saved]"
  </action>
  <verify>
    <automated>ls /Users/ourcomputer/Github-Repos/secureshare/screenshots/ 2>/dev/null | wc -l</automated>
  </verify>
  <done>screenshots/ directory exists with at least 3 PNG files named according to the pages captured</done>
</task>

<task type="auto">
  <name>Task 2: Embed screenshots in README.md</name>
  <files>README.md</files>
  <action>
Add a "## Screenshots" section to README.md immediately before the existing "## Contributing" section.

Read the actual filenames in `screenshots/` first, then embed only the files that exist. Use this template, adjusting for the actual filenames:

```markdown
## Screenshots

![Torch Secret marketing homepage in dark mode — hero section with zero-knowledge tagline and create CTA](screenshots/homepage-dark.png)

![Torch Secret marketing homepage in light mode](screenshots/homepage-light.png)

![Create a secret form — paste sensitive text, set expiration and optional protection](screenshots/create-flow.png)

![Secret reveal page — terminal-style one-time display after the secret is viewed](screenshots/reveal-flow.png)

![Secret dashboard — authenticated view of created secrets with expiration status](screenshots/dashboard.png)

---
```

Rules:
- Only embed files that actually exist in screenshots/ (check with ls first)
- Each image must have descriptive alt text (describe what is visible, not just the page name)
- Use relative paths: `screenshots/filename.png` (not absolute paths)
- Add a `---` horizontal rule after the screenshots block, before the Contributing section
- Do not modify any other README.md content — only insert this new section

After editing, verify the filenames in the README match the actual files:
```bash
grep "screenshots/" README.md
ls screenshots/
```
  </action>
  <verify>
    <automated>grep -c "screenshots/" /Users/ourcomputer/Github-Repos/secureshare/README.md</automated>
  </verify>
  <done>README.md contains a Screenshots section with at least 3 embedded images, each with descriptive alt text, using relative paths that match files in screenshots/</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls screenshots/` shows at least 3 PNG files
2. `grep -c "screenshots/" README.md` returns ≥ 3
3. Each filename in README.md img references exists as an actual file in screenshots/
4. `grep "## Screenshots" README.md` returns 1 result
5. The Screenshots section appears before the Contributing section in README.md
</verification>

<success_criteria>
- screenshots/ directory exists at repo root with at least 3 PNG captures of the v5.0 UI
- README.md Screenshots section is embedded with descriptive alt text and correct relative paths
- All referenced screenshot filenames resolve to actual files (no broken image links)
</success_criteria>

<output>
After completion, create `.planning/phases/41-update-readme-and-stale-documentation-for-torch-secret-v5-0-launch/41-03-SUMMARY.md`
</output>
