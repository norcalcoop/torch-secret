---
phase: 41-update-readme-and-stale-documentation-for-torch-secret-v5-0-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - package.json
autonomous: true
requirements:
  - CHANGELOG_V4
  - CHANGELOG_V5
  - PKG_VERSION

must_haves:
  truths:
    - CHANGELOG.md contains a [4.0.0] section with Added/Changed subsections covering auth, dashboard, billing, analytics, email, and rate-limiting features from Phases 21-30
    - CHANGELOG.md contains a [5.0.0] section with Added/Changed subsections covering rebrand, marketing homepage, OAuth, Infisical, Loops, SEO pages, and security hardening from Phases 31-41
    - CHANGELOG.md [Unreleased] section exists and is empty (no content under it)
    - CHANGELOG.md comparison links at the bottom include [5.0.0], [4.0.0], [3.0.0], [2.0.0], [1.0.0] references
    - package.json version field is "5.0.0"
  artifacts:
    - CHANGELOG.md
    - package.json
  key_links:
    - CHANGELOG.md [4.0.0] entry immediately follows [Unreleased] section
    - CHANGELOG.md [5.0.0] entry is between [Unreleased] and [4.0.0]
---

<objective>
Back-fill the CHANGELOG with v4.0 and v5.0 milestone entries and bump package.json to 5.0.0.

Purpose: The current CHANGELOG only covers v1.0-v3.0. Versions 4.0 and 5.0 shipped across 20 phases without CHANGELOG entries. Contributors and deployers need an accurate record.
Output: CHANGELOG.md with five complete version entries (v1.0–v5.0), correct comparison links; package.json at 5.0.0.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

### Current CHANGELOG state

CHANGELOG.md currently contains entries for [3.0.0], [2.0.0], and [1.0.0] only. The [Unreleased] section is empty. Comparison links at the bottom only reference v1.0–v3.0 using `norcalcoop/secureshare`.

### v4.0 feature set (Phases 21-30, sourced from STATE.md + ROADMAP.md)

**Added:**
- User accounts: email/password registration, login, session management, email verification (Better Auth 1.x)
- Secret dashboard: authenticated users can view, track, and manage all their created secrets
- EFF Diceware passphrase generator for secret protection (client-side, word list bundled)
- Optional password or passphrase protection for secrets (Argon2id server-side hashing, OWASP parameters)
- Passphrase/password tier enforcement: anonymous users blocked; free users limited to generated passphrases; Pro users get custom passwords
- Privacy-safe PostHog analytics with URL fragment stripping and zero-knowledge-safe event design
- Email notification: secret-viewed alert sent to creator (ZK-safe — no secretId in email body)
- Stripe Pro billing: subscription checkout, Customer Portal, webhook lifecycle handler, 30-day expiration unlock
- Rate-limit conversion prompts: anonymous users see upgrade CTA after hitting limits
- Legal pages: `/privacy` and `/terms` with server-side noindex enforcement
- Feedback links on confirmation and reveal pages (Tally.so)

**Changed:**
- Package version bumped to 4.0.0

### v5.0 feature set (Phases 31-41, sourced from STATE.md + ROADMAP.md)

**Added:**
- Google OAuth and GitHub OAuth sign-in (Better Auth social providers; two separate GitHub apps for dev + prod)
- Marketing homepage at `/` with hero, zero-knowledge proof points, use-case sections, and GDPR email capture
- Pricing page at `/pricing` with Free vs Pro tier cards, billing toggle, FAQ accordion, and FAQPage JSON-LD
- SEO content pages (Express SSR): `/vs/*`, `/alternatives/*`, `/use/*` — server-rendered, visible to AI crawlers without JavaScript
- GDPR-compliant email capture: homepage form, double opt-in, Resend Audiences sync, one-click unsubscribe
- Loops.so email onboarding sequence: welcome on registration, day-3 key features email, day-7 upgrade prompt (marketing consent gated)
- PostHog enrichment: `checkout_initiated`, `subscription_activated`, `dashboard_viewed` events; launch dashboard, conversion funnels, and cohorts configured
- Security hardening: Argon2id concurrency cap via p-limit, tighter rate limiting on password-verify endpoint, PostgreSQL pool hardening (max 10, statement timeout), payload size cap (100KB Zod + 413 error handler)
- Infisical secrets management: all secrets in Infisical Cloud; no `.env` files required for team development
- Supply chain security scanning via Socket.dev on every pull request

**Changed:**
- App renamed from SecureShare to Torch Secret across all user-facing surfaces, canonical URLs, metadata, and source code
- All canonical URLs, sitemap entries, JSON-LD `@id`/`url` fields, and OG image URLs updated to `torchsecret.com`
- Create-secret form moved from `/` to `/create`; `/` now serves the marketing homepage
- `.env.example` stripped to keys-only reference; all values managed via Infisical

**Note on CHANGELOG comparison links:** The bottom links use `norcalcoop/secureshare` as the repo URL. Plan 02 will update all 10 occurrences across 5 files to the new repo URL. For now, add the v4.0 and v5.0 links following the same `norcalcoop/secureshare` pattern — Plan 02 will sweep all of them in a single grep pass.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Back-fill CHANGELOG.md with v4.0 and v5.0 entries</name>
  <files>CHANGELOG.md</files>
  <action>
Rewrite CHANGELOG.md to add [5.0.0] and [4.0.0] entries between [Unreleased] and [3.0.0].

The final file structure must be:

```
## [Unreleased]

## [5.0.0] - 2026-03-02

### Added

- Google OAuth and GitHub OAuth sign-in (Better Auth social providers; two separate GitHub OAuth Apps for dev and prod environments)
- Marketing homepage at `/` with hero section, zero-knowledge proof points, use-case sections, and GDPR-compliant email capture form
- Pricing page at `/pricing` with Free vs Pro tier cards, monthly/annual billing toggle, FAQ accordion, and FAQPage JSON-LD in `<head>`
- SEO content pages (Express SSR): `/vs/*`, `/alternatives/*`, `/use/*` — server-rendered so content is visible to AI crawlers and Googlebot without JavaScript execution
- GDPR-compliant email capture: homepage form with explicit consent checkbox, double opt-in confirmation, Resend Audiences sync, one-click unsubscribe
- Loops.so email onboarding sequence: welcome email on registration, day-3 key features email, day-7 upgrade prompt (marketing consent gated)
- PostHog enrichment: `checkout_initiated`, `subscription_activated`, `dashboard_viewed` events; launch dashboard, conversion funnels, and user cohorts configured in PostHog Cloud
- Security hardening: Argon2id concurrency cap via p-limit (max 4 concurrent), tighter rate limiting on password-verify endpoint (5 req/min), PostgreSQL pool hardening (max 10 connections, statement timeout 10s), payload size cap enforcement (100KB)
- Infisical secrets management: all secrets stored in Infisical Cloud; no `.env` file required for team development
- Supply chain security scanning via Socket.dev on every pull request

### Changed

- App renamed from SecureShare to Torch Secret across all user-facing surfaces, canonical URLs, structured data, and source code
- All canonical URLs, sitemap entries, JSON-LD `@id`/`url` fields, and OG image URLs updated to `torchsecret.com`
- Create-secret form moved from `/` to `/create`; `/` now serves the marketing homepage
- `.env.example` stripped to keys-only reference; all values managed via Infisical
- Package version bumped to 5.0.0

## [4.0.0] - 2026-02-22

### Added

- User accounts with email/password registration, login, session management, and email verification (Better Auth 1.x)
- Secret dashboard: authenticated users can view, track, and manage all their created secrets with expiration status
- EFF Diceware passphrase generator for secret protection (client-side generation, word list bundled, offline-capable)
- Optional password or passphrase protection for secrets (Argon2id server-side hashing, OWASP-recommended parameters)
- Passphrase/password tier enforcement: anonymous users blocked from all protection options; free users limited to generated passphrases; Pro users unlock custom passwords
- Privacy-safe PostHog analytics with URL fragment stripping before every event — no plaintext secrets or keys can appear in analytics data
- Email notification: optional secret-viewed alert sent to the creator (zero-knowledge safe — no secretId in email body)
- Stripe Pro billing: subscription checkout, Customer Portal, webhook lifecycle handler (`customer.subscription.created/deleted`), 30-day expiration unlock for Pro subscribers
- Rate-limit conversion prompts: anonymous users see account upgrade CTA after hitting rate limits
- Legal pages: `/privacy` and `/terms` with server-side `noindex` header enforcement
- Feedback links on confirmation and reveal pages (Tally.so embedded link)

### Changed

- Package version bumped to 4.0.0

## [3.0.0] - 2026-02-18
... (existing content unchanged)
```

Rules:
- Keep all existing content for [3.0.0], [2.0.0], [1.0.0] exactly as-is — do not modify those sections.
- Keep the [Unreleased] heading but leave it empty (no content under it).
- Update the comparison links at the bottom of the file to add v5.0 and v4.0 links. The new links section must be:

```
[Unreleased]: https://github.com/norcalcoop/secureshare/compare/v5.0...HEAD
[5.0.0]: https://github.com/norcalcoop/secureshare/compare/v4.0...v5.0
[4.0.0]: https://github.com/norcalcoop/secureshare/compare/v3.0...v4.0
[3.0.0]: https://github.com/norcalcoop/secureshare/compare/v2.0...v3.0
[2.0.0]: https://github.com/norcalcoop/secureshare/compare/v1.0...v2.0
[1.0.0]: https://github.com/norcalcoop/secureshare/releases/tag/v1.0
```

Note: The `norcalcoop/secureshare` repo URL is stale and will be updated by Plan 02 in a sweep across all 5 affected files. Do not try to determine the new URL here — write the existing pattern and Plan 02 handles the rename.
  </action>
  <verify>
    <automated>grep -c "\[5\.0\.0\]" /Users/ourcomputer/Github-Repos/secureshare/CHANGELOG.md && grep -c "\[4\.0\.0\]" /Users/ourcomputer/Github-Repos/secureshare/CHANGELOG.md && grep -c "Infisical" /Users/ourcomputer/Github-Repos/secureshare/CHANGELOG.md && grep -c "Torch Secret" /Users/ourcomputer/Github-Repos/secureshare/CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md contains [5.0.0] and [4.0.0] sections with feature-grouped entries; [Unreleased] is empty; comparison links include all five versions</done>
</task>

<task type="auto">
  <name>Task 2: Bump package.json version to 5.0.0</name>
  <files>package.json</files>
  <action>
In package.json, change the `"version"` field from `"4.0.0"` to `"5.0.0"`. This is a single-line change. Do not modify any other fields.
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('/Users/ourcomputer/Github-Repos/secureshare/package.json','utf8')); console.log(p.version); if(p.version !== '5.0.0') process.exit(1);"</automated>
  </verify>
  <done>package.json version is "5.0.0"</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "\[5\.0\.0\]" CHANGELOG.md` returns 2 (heading + comparison link)
2. `grep -c "\[4\.0\.0\]" CHANGELOG.md` returns 2 (heading + comparison link)
3. `grep "^\[Unreleased\]" CHANGELOG.md` returns exactly one hit; nothing between it and the [5.0.0] heading
4. `node -e "console.log(require('./package.json').version)"` prints `5.0.0`
5. `wc -l CHANGELOG.md` is substantially larger than before (was 77 lines; now 120+ lines)
</verification>

<success_criteria>
- CHANGELOG.md back-filled with v4.0 and v5.0 milestone entries in feature-grouped format matching Keep a Changelog 1.1.0 style
- All five comparison links present at the bottom of CHANGELOG.md
- package.json version is 5.0.0
- No existing CHANGELOG content (v1.0-v3.0 entries) modified
</success_criteria>

<output>
After completion, create `.planning/phases/41-update-readme-and-stale-documentation-for-torch-secret-v5-0-launch/41-01-SUMMARY.md`
</output>
