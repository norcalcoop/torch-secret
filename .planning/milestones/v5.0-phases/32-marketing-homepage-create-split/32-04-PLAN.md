---
phase: 32-marketing-homepage-create-split
plan: 04
type: execute
wave: 2
depends_on:
  - 32-01
  - 32-02
  - 32-03
files_modified: []
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05

must_haves:
  truths:
    - "Navigating to / shows the marketing homepage with hero, use cases, and email capture — no secret textarea"
    - "Navigating to /create shows the fully functional create-secret form"
    - "Desktop nav shows Create a Secret, Pricing, and Dashboard/Login links"
    - "Mobile viewport shows a bottom tab bar with Home, Create, Pricing, Dashboard tabs"
    - "Email form submission with valid email + checked consent shows a toast; unchecked consent shows inline error"
    - "WebApplication JSON-LD block is present in page source at /"
  artifacts:
    - path: "client/src/pages/home.ts"
      provides: "Marketing homepage — rendered at /"
  key_links:
    - from: "browser /"
      to: "client/src/pages/home.ts"
      via: "SPA router dynamic import"
      pattern: "renderHomePage"
---

<objective>
Human verification of the complete Phase 32 implementation across all three plans. Confirms that the marketing homepage, /create route split, navigation overhaul, and mobile bottom tab bar all work correctly together in the browser.

Purpose: Catch any visual, functional, or interaction issues that automated checks cannot detect — especially mobile layout, CTA flow, and GDPR form behavior.
Output: User approval (or list of issues for gap closure).
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-marketing-homepage-create-split/32-01-SUMMARY.md
@.planning/phases/32-marketing-homepage-create-split/32-02-SUMMARY.md
@.planning/phases/32-marketing-homepage-create-split/32-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev servers and verify automated checks</name>
  <files></files>
  <action>
Run a final automated verification pass before the human checkpoint:

1. Run ESLint: `npm run lint` — must pass with no errors on changed files
2. Run TypeScript: `npm run build:client` — must compile without errors
3. Start servers for manual testing:
   - Terminal 1: `npm run dev:server` (requires PostgreSQL running)
   - Terminal 2: `npm run dev:client`
4. Confirm dev servers are reachable:
   - `curl -s http://localhost:5173/ | grep 'Torch Secret'` — should return content
   - `curl -s http://localhost:5173/ | grep 'application/ld+json'` — should return the JSON-LD block (HOME-05 verification)
5. Check NOINDEX protection (Pitfall 3 from RESEARCH.md):
   - `curl -sI http://localhost:5173/` — must NOT contain `x-robots-tag: noindex` header (homepage must be indexable)
  </action>
  <verify>
- `npm run lint` exits 0
- `npm run build:client` exits 0
- `curl http://localhost:5173/ | grep 'application/ld+json'` returns a match
- `curl -I http://localhost:5173/` does NOT contain `x-robots-tag: noindex`
  </verify>
  <done>
All automated checks pass. Dev servers running. JSON-LD present at /. No noindex header on /.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — homepage, /create split, nav, mobile tab bar, GDPR form</name>
  <files></files>
  <action>
Human verification of Phase 32 implementation. Open http://localhost:5173 and verify the following flows.

**Desktop verification:**
1. Navigate to `/` — confirm: hero section with H1 present; NO create textarea; CTA button routes to /create; 3 use-case cards visible; email capture form present with consent checkbox unchecked by default; desktop header shows Pricing, Dashboard/Login, Create a Secret, ThemeToggle.
2. Navigate to `/create` — confirm: secret creation textarea present; form functional; "Create a Secret" nav link hidden in header.
3. Click "Pricing" nav link — confirm SPA navigation (no hard reload), not-found page shows (expected).
4. Email capture form: (a) submit empty → inline error; (b) valid email + unchecked consent → inline error; (c) valid email + checked consent → toast success, form resets.
5. Theme toggle still works.

**Mobile verification (DevTools 375px or physical device):**
6. Bottom tab bar visible at viewport bottom with 4 tabs: Home, Create, Pricing, Dashboard.
7. Active tab highlighted on route change (tap Create → Create tab highlighted).
8. Content not clipped by tab bar (email form fully visible when scrolled to bottom).

**JSON-LD:**
9. `curl -s http://localhost:5173/ | grep 'application/ld+json'` returns match.
  </action>
  <verify>
Human types "approved" after verifying all flows above, or describes specific issues found.
  </verify>
  <done>
Human has confirmed: homepage renders at /; create form at /create; full nav visible; mobile tab bar works; GDPR form validates correctly; JSON-LD present at /.
  </done>
</task>

</tasks>

<verification>
Phase 32 complete when:
1. `/` shows marketing homepage (hero + use cases + email capture) — no create textarea
2. `/create` shows the fully functional create-secret form
3. Desktop nav has all four elements in correct order
4. Mobile bottom tab bar has 4 tabs, active highlighting works on navigation
5. GDPR email capture form: consent unchecked by default, inline error on unchecked, toast on valid submit
6. `curl / | grep application/ld+json` returns the WebApplication JSON-LD block
7. `curl -I /` has no x-robots-tag: noindex header
8. Human has typed "approved"
</verification>

<success_criteria>
All five HOME requirements satisfied:
- HOME-01: Marketing homepage at / with hero, ZK proof points, CTA
- HOME-02: Create form at /create (moved from /)
- HOME-03: Desktop nav has /create, /pricing, /dashboard links; mobile bottom tab bar present
- HOME-04: Email capture form with GDPR consent on homepage (UI only)
- HOME-05: WebApplication JSON-LD in page source at /
</success_criteria>

<output>
After completion, create `.planning/phases/32-marketing-homepage-create-split/32-04-SUMMARY.md`
</output>
