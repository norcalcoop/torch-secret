---
phase: 22-authentication
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/router.ts
autonomous: true
requirements:
  - AUTH-05
gap_closure: true

must_haves:
  truths:
    - "Navigating to /reset-password/ (trailing slash) renders the reset-password error card, not the 404 page"
    - "All other existing routes continue to work after the normalization change"
  artifacts:
    - path: "client/src/router.ts"
      provides: "handleRoute() with trailing-slash-normalized path"
      contains: "window.location.pathname.replace"
  key_links:
    - from: "client/src/router.ts"
      to: "handleRoute() path variable"
      via: "replace(/\\/$/, '') || '/'"
      pattern: "pathname\\.replace\\(/\\\\/$/"
---

<objective>
Fix the trailing-slash routing bug in the SPA router so that address-bar navigation to `/reset-password/` (or any route with a trailing slash) correctly matches the intended route instead of falling through to the 404 page.

Purpose: UAT test 7 failed because `handleRoute()` reads `window.location.pathname` raw. Exact-match comparisons like `path === '/reset-password'` silently fail when the browser appends a trailing slash, sending unauthenticated users (who typed the URL) to a 404 instead of the invalid-token error card.

Output: One-line change in `client/src/router.ts` at line 163. All tests pass. No new files.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/router.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize trailing slash in handleRoute()</name>
  <files>client/src/router.ts</files>
  <action>
    In `handleRoute()` at line 163, replace:

    ```ts
    const path = window.location.pathname;
    ```

    with:

    ```ts
    const path = window.location.pathname.replace(/\/$/, '') || '/';
    ```

    This strips any trailing slash before the if/else chain runs, so `/reset-password/` becomes `/reset-password` and matches the existing branch. The `|| '/'` guard preserves the root path: `''.replace(/\/$/, '')` would produce an empty string, which must remain `'/'` to match the homepage branch.

    Also update the `routechange` CustomEvent at the bottom of `handleRoute()` — it currently dispatches `{ detail: { path } }`. Since `path` is now the normalized value, the event already emits the clean path with no further change needed. Verify this is the case and leave it as-is.

    No other changes to `router.ts`.
  </action>
  <verify>
    1. Start the Vite dev server (`npm run dev:client`) and navigate the browser to `http://localhost:5173/reset-password/` (with trailing slash). Confirm the reset-password error card renders ("Invalid or expired reset link" or similar), NOT the 404 page.
    2. Also verify `/` still loads the create page and `/login/` loads the login page.
  </verify>
  <done>
    `handleRoute()` line 163 reads the normalized path. The reset-password route renders correctly when the URL has a trailing slash.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to confirm no regressions</name>
  <files></files>
  <action>
    Run the complete test suite once (non-watch mode):

    ```bash
    npm run test:run
    ```

    All pre-existing tests must pass. The one-line normalization change has no side-effects on server tests. Client router tests (if any) must also pass — the normalization is transparent to SPA navigate() calls because `navigate()` always pushes clean paths via `history.pushState`, so `window.location.pathname` never has a trailing slash in that code path.

    If any test fails, investigate before marking this task done.
  </action>
  <verify>
    `npm run test:run` exits with code 0. All test suites green. No new failures introduced.
  </verify>
  <done>
    Full test suite passes with no regressions. The router normalization fix is safe to ship.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `/reset-password/` (trailing slash, address-bar entry) renders the invalid-token error card with a link back to `/forgot-password`, not the 404 page.
- `/reset-password` (no trailing slash) continues to work identically.
- `npm run test:run` exits 0.
- No other routes are affected (verify `/`, `/login/`, `/register/` all route correctly).
</verification>

<success_criteria>
UAT test 7 passes: navigating to `/reset-password` without a `?token=` query param — whether via SPA navigation or direct address-bar entry with or without a trailing slash — shows the invalid-link error card with a link to `/forgot-password`, not the 404 page.
</success_criteria>

<output>
After completion, create `.planning/phases/22-authentication/22-07-SUMMARY.md` following the standard summary template.
</output>
