---
phase: 22-authentication
plan: 03
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - server/src/app.ts
  - server/src/middleware/require-auth.ts
  - server/src/routes/me.ts
autonomous: true
requirements: [AUTH-03, AUTH-05, AUTH-08]

must_haves:
  truths:
    - "POST /api/auth/sign-up/email returns 200 and a Set-Cookie header containing better-auth.session_token"
    - "GET /api/me with a valid session cookie returns 200 with the authenticated user's id, email, and name"
    - "GET /api/me without a session cookie returns 401 with { error: 'unauthorized' }"
    - "POST /api/auth/sign-up/email returns a response without hanging or timeout (confirms auth handler body-stream ordering is correct relative to express.json())"
  artifacts:
    - path: "server/src/app.ts"
      provides: "Updated Express app with auth handler mounted before JSON parser"
      contains: "toNodeHandler(auth)"
    - path: "server/src/middleware/require-auth.ts"
      provides: "Express middleware that validates Better Auth sessions"
      exports: ["requireAuth"]
    - path: "server/src/routes/me.ts"
      provides: "GET /api/me route returning current session user"
      exports: ["meRouter"]
  key_links:
    - from: "server/src/app.ts"
      to: "server/src/auth.ts"
      via: "toNodeHandler(auth) mounted at /api/auth/*splat BEFORE express.json()"
      pattern: "toNodeHandler.*auth"
    - from: "server/src/middleware/require-auth.ts"
      to: "server/src/auth.ts"
      via: "auth.api.getSession({ headers: fromNodeHeaders(req.headers) })"
      pattern: "auth\\.api\\.getSession"
    - from: "server/src/routes/me.ts"
      to: "server/src/middleware/require-auth.ts"
      via: "requireAuth middleware applied before the GET handler"
      pattern: "requireAuth"
    - from: "server/src/app.ts"
      to: "server/src/routes/me.ts"
      via: "app.use('/api/me', meRouter) BEFORE the /api catch-all; /api catch-all must be last /api/* handler"
      pattern: "app\\.use.*'/api/me'.*meRouter"
---

<objective>
Wire Better Auth into the Express application: mount the auth handler at /api/auth/*splat before express.json(), create the requireAuth middleware that validates sessions, and add a /api/me route that returns the authenticated user. This plan makes the server fully auth-capable.

Purpose: Without this wiring, no auth API calls work. All integration tests (Plan 06) depend on this.
Output: Updated app.ts, new require-auth.ts, new me.ts.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/INVARIANTS.md
@.planning/phases/22-authentication/22-RESEARCH.md
@.planning/phases/22-authentication/22-01-SUMMARY.md
@server/src/app.ts
@server/src/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update app.ts to mount Better Auth handler</name>
  <files>server/src/app.ts</files>
  <action>
    Update server/src/app.ts to:

    1. Add imports at the top of the file (after existing imports):
       import { toNodeHandler } from 'better-auth/node';
       import { auth } from './auth.js';
       import { meRouter } from './routes/me.js';

    2. Mount the Better Auth handler BEFORE app.use(express.json()):
       The current line 59 is: app.use(express.json({ limit: '100kb' }));
       Insert BEFORE it:

         // Better Auth handler -- MUST be before express.json() to avoid body-stream conflict.
         // Better Auth parses its own bodies internally; if express.json() runs first,
         // the stream is consumed and all auth requests hang indefinitely.
         // Express 5 wildcard syntax: /api/auth/*splat (not /api/auth/*)
         app.all('/api/auth/{*splat}', toNodeHandler(auth));

    3. Register the /api/me route AFTER the secrets router:
       After the line: app.use('/api/secrets', createSecretsRouter(redisClient));
       Add:
         app.use('/api/me', meRouter);

    4. Move the existing /api catch-all (app.use('/api', ...)) to AFTER app.use('/api/me', meRouter):
       The current app.ts has a catch-all that returns 404 for unknown /api/* routes. It must be
       the LAST /api/* handler — after meRouter — or it will intercept GET /api/me requests before
       they reach meRouter, returning 404 instead of the correct response.
       Locate the existing app.use('/api', ...) 404-returning catch-all and move it to after
       app.use('/api/me', meRouter). The resulting order for /api/* handlers must be:
         app.all('/api/auth/{*splat}', toNodeHandler(auth));   // step 2 above
         app.use('/api/secrets', createSecretsRouter(...));    // existing
         app.use('/api/me', meRouter);                         // step 3 above
         app.use('/api', ...404 catch-all...);                 // moved here — LAST /api handler

    5. Update the middleware order comment at the top of buildApp() to reflect the new order:
       Replace the existing numbered list with:
         * Middleware order is critical for correctness:
         * 1. trust proxy    -- enables correct req.ip behind reverse proxy
         * 2. httpsRedirect  -- redirect HTTP->HTTPS before any response
         * 3. cspNonce       -- generate nonce BEFORE helmet reads it
         * 4. helmet         -- set all security headers (CSP uses nonce from step 3)
         * 5. httpLogger     -- logging
         * 6. auth handler   -- /api/auth/*splat MUST be before express.json() (body-stream ordering)
         * 7. json parser    -- body parsing (for non-auth routes)
         * 8. routes         -- API endpoints (health, secrets, me)
         * 9. static assets + SPA catch-all (production only, when client/dist exists)
         * 10. errorHandler  -- MUST be last

    CRITICAL: Verify the actual insertion is BEFORE express.json() in the file — not after.
    Misplacing this causes silent hangs on all auth requests.

    ZERO-KNOWLEDGE INVARIANT: The auth handler mounts Better Auth at /api/auth/*. The auth
    handler will never see secretIds. Do not add any middleware to the auth handler that
    combines session userId with secretId. See .planning/INVARIANTS.md.
  </action>
  <verify>
    grep -n 'toNodeHandler\|express.json' server/src/app.ts
    The line with toNodeHandler must appear BEFORE the line with express.json.
    npx tsc --project server/tsconfig.json --noEmit exits 0.
  </verify>
  <done>
    app.ts imports toNodeHandler, auth, meRouter.
    app.all('/api/auth/{*splat}', toNodeHandler(auth)) appears before app.use(express.json(...)).
    app.use('/api/me', meRouter) appears after the secrets router.
    The existing /api catch-all is moved to AFTER app.use('/api/me', meRouter) — it is the last /api/* handler.
    Middleware order comment updated.
    tsc --noEmit passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create requireAuth middleware and /api/me route</name>
  <files>server/src/middleware/require-auth.ts, server/src/routes/me.ts</files>
  <action>
    1. Create server/src/middleware/require-auth.ts:

       import type { Request, Response, NextFunction } from 'express';
       import { fromNodeHeaders } from 'better-auth/node';
       import { auth } from '../auth.js';

       /**
        * Express middleware that validates a Better Auth session.
        *
        * Uses auth.api.getSession() with the incoming request headers.
        * On success: sets res.locals.user (AuthUser) and res.locals.session (AuthSessionData).
        * On failure: returns 401 JSON immediately.
        *
        * ZERO-KNOWLEDGE INVARIANT: res.locals.user contains userId.
        * Never augment res.locals or pass userId to any code that also reads secretId
        * from the request body or URL. See .planning/INVARIANTS.md.
        */
       export async function requireAuth(
         req: Request,
         res: Response,
         next: NextFunction,
       ): Promise<void> {
         const session = await auth.api.getSession({
           headers: fromNodeHeaders(req.headers),
         });

         if (!session) {
           res.status(401).json({ error: 'unauthorized', message: 'Authentication required.' });
           return;
         }

         res.locals.user = session.user;
         res.locals.session = session.session;
         next();
       }

    2. Create server/src/routes/me.ts:

       import { Router } from 'express';
       import { requireAuth } from '../middleware/require-auth.js';
       import type { AuthUser } from '../auth.js';

       export const meRouter = Router();

       /**
        * GET /api/me
        *
        * Returns the authenticated user's profile.
        * Requires a valid Better Auth session cookie.
        *
        * Response: { user: { id, email, name, emailVerified, image, createdAt } }
        *
        * ZERO-KNOWLEDGE INVARIANT: This endpoint returns userId (user.id).
        * It MUST NOT include secretId in the response body. The client uses
        * this endpoint to display user info only. See .planning/INVARIANTS.md.
        */
       meRouter.get('/', requireAuth, (_req, res) => {
         const user = res.locals.user as AuthUser;
         res.json({
           user: {
             id: user.id,
             email: user.email,
             name: user.name,
             emailVerified: user.emailVerified,
             image: user.image,
             createdAt: user.createdAt,
           },
         });
       });
  </action>
  <verify>
    npx tsc --project server/tsconfig.json --noEmit exits 0.
    grep 'requireAuth' server/src/middleware/require-auth.ts
    grep 'meRouter' server/src/routes/me.ts
    grep 'fromNodeHeaders' server/src/middleware/require-auth.ts
  </verify>
  <done>
    server/src/middleware/require-auth.ts exports requireAuth async middleware.
    server/src/routes/me.ts exports meRouter with GET / handler guarded by requireAuth.
    Both files include zero-knowledge invariant comments.
    tsc --noEmit passes.
  </done>
</task>

</tasks>

<verification>
1. grep -n 'toNodeHandler\|express.json' server/src/app.ts | head -5
   toNodeHandler line number must be LESS THAN express.json line number.
2. npx tsc --project server/tsconfig.json --noEmit exits 0.
3. Start dev server (npm run dev:server) and confirm it boots without errors.
4. curl -X POST http://localhost:3000/api/auth/sign-up/email \
     -H 'Content-Type: application/json' \
     -d '{"email":"test@example.com","password":"testpass123","name":"Test"}' \
   Returns 200 (not hanging — confirms auth handler is before json parser).
5. curl http://localhost:3000/api/me — returns 401 (unauthorized, no cookie).
</verification>

<success_criteria>
- app.ts mounts /api/auth/{*splat} before express.json()
- requireAuth middleware validates sessions via auth.api.getSession()
- GET /api/me returns 401 without session, 200 with valid session
- TypeScript compiles without errors
- Dev server starts without errors
</success_criteria>

<output>
After completion, create .planning/phases/22-authentication/22-03-SUMMARY.md
</output>
