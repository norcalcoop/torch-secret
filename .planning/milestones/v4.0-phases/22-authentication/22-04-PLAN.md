---
phase: 22-authentication
plan: 04
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - client/src/pages/login.ts
  - client/src/pages/register.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-06, AUTH-07]

must_haves:
  truths:
    - "User can fill in the register form (name, email, password) and submit — a POST to /api/auth/sign-up/email is made"
    - "User sees a 'check your email' message after registration (email verification required)"
    - "User can fill in the login form (email, password) and submit — a POST to /api/auth/sign-in/email is made"
    - "Login page has Google and GitHub OAuth buttons that call authClient.signIn.social({ provider: 'google'/'github' })"
    - "Both pages redirect to /dashboard on success, display inline error messages on failure"
  artifacts:
    - path: "client/src/pages/login.ts"
      provides: "Login page with email/password form and OAuth buttons"
      exports: ["renderLoginPage"]
    - path: "client/src/pages/register.ts"
      provides: "Registration form with email verification notice"
      exports: ["renderRegisterPage"]
  key_links:
    - from: "client/src/pages/login.ts"
      to: "client/src/api/auth-client.ts"
      via: "authClient.signIn.email() for password login, authClient.signIn.social() for OAuth"
      pattern: "signIn\\.email|signIn\\.social"
    - from: "client/src/pages/register.ts"
      to: "client/src/api/auth-client.ts"
      via: "authClient.signUp.email() to create the account"
      pattern: "signUp\\.email"
---

<objective>
Create the login and register pages using vanilla TypeScript with the Better Auth client. Both pages follow the established codebase design patterns and handle all states: loading, success (redirect/message), and error (inline display).

Purpose: AUTH-01 (register) and AUTH-03 (login) are the primary entry points for the auth system. OAuth buttons cover AUTH-06 and AUTH-07.
Output: client/src/pages/login.ts, client/src/pages/register.ts
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-authentication/22-RESEARCH.md
@.planning/phases/22-authentication/22-02-SUMMARY.md
@client/src/pages/create.ts
@client/src/api/auth-client.ts
@client/src/router.ts
@client/src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create login page</name>
  <files>client/src/pages/login.ts</files>
  <action>
    MANDATORY: Before writing any code in this task, invoke the `frontend-design` skill.

    Create client/src/pages/login.ts. Export renderLoginPage(container: HTMLElement): Promise<void>.

    Page structure:
    - Wrapper div, className 'space-y-6'
    - Header: h1 "Sign In", subtext "Welcome back"
    - Card: glassmorphism surface (bg-surface border border-surface-border rounded-xl p-6)
    - Form fields inside the card:
        - Email input (type="email", autocomplete="email", required)
        - Password input (type="password", autocomplete="current-password", required)
        - Submit button "Sign In" (full-width, primary variant)
        - Error message area (hidden by default, shown on failure)
    - OAuth divider: "or continue with" text
    - Google OAuth button: icon + "Continue with Google"
    - GitHub OAuth button: icon + "Continue with GitHub"
    - Footer link: "Don't have an account? Sign up" — navigates to /register

    Email/password login flow:
      const { data, error } = await authClient.signIn.email({
        email,
        password,
        callbackURL: '/dashboard',
      });
      On error: show error message. Common errors:
        - 'invalid_credentials' or 'INVALID_EMAIL_OR_PASSWORD' → "Invalid email or password"
        - 'email_not_verified' → "Please verify your email before signing in. Check your inbox."
      On success (data not null): navigate('/dashboard')

    OAuth flow (for both Google and GitHub buttons):
      await authClient.signIn.social({
        provider: 'google',  // or 'github'
        callbackURL: '/dashboard',
        errorCallbackURL: '/login?error=oauth',
      });
      Note: signIn.social triggers a full redirect — no return value to handle.
      On page load, check for ?error=oauth in the URL and display an error message if present.

    Loading state: disable submit button and show a spinner inside it while the API call is pending.
    Use the existing loading-spinner component pattern from the codebase if available.

    Accessibility:
    - Form has noValidate (client validation via required attrs + JS, not browser native popups)
    - Error message uses role="alert" for screen reader announcement
    - Buttons have descriptive aria-labels for OAuth buttons: "Sign in with Google", "Sign in with GitHub"

    Already-authenticated redirect:
      On mount, call const { data } = await authClient.getSession();
      If data?.session exists, navigate('/dashboard') immediately.
  </action>
  <verify>
    grep 'renderLoginPage' client/src/pages/login.ts
    grep 'signIn.email\|signIn\.social' client/src/pages/login.ts
    grep "navigate.*'/dashboard'" client/src/pages/login.ts
    grep "navigate.*'/register'" client/src/pages/login.ts
  </verify>
  <done>
    client/src/pages/login.ts exports renderLoginPage.
    Page has email/password form, Google button, GitHub button.
    Handles email_not_verified error with clear message.
    Redirects to /dashboard on success.
    Redirects to /dashboard if already authenticated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create register page</name>
  <files>client/src/pages/register.ts</files>
  <action>
    MANDATORY: Before writing any code in this task, invoke the `frontend-design` skill.

    Create client/src/pages/register.ts. Export renderRegisterPage(container: HTMLElement): Promise<void>.

    Page structure:
    - Wrapper div, className 'space-y-6'
    - Header: h1 "Create Account", subtext "Start sharing secrets securely"
    - Card: glassmorphism surface (bg-surface border border-surface-border rounded-xl p-6)
    - Form fields inside the card:
        - Name input (type="text", autocomplete="name", required, placeholder="Your name")
        - Email input (type="email", autocomplete="email", required)
        - Password input (type="password", autocomplete="new-password", required, minLength=8)
        - Password strength hint text: "At least 8 characters" (shown below password field)
        - Submit button "Create Account" (full-width, primary variant)
        - Error message area (hidden by default, shown on failure)
    - Success state: replace the form with a card showing:
        "Check your email — We sent a verification link to <email>. You must verify your email before you can sign in."
        with a subtext "Didn't receive it? Check your spam folder."
    - OAuth divider + Google/GitHub buttons (same as login page)
    - Footer link: "Already have an account? Sign in" — navigates to /login

    Registration flow:
      const { data, error } = await authClient.signUp.email({
        email,
        password,
        name,
        callbackURL: '/dashboard',
      });
      On error: show inline error message. Common errors:
        - 'user_already_exists' or 'USER_ALREADY_EXISTS' → "An account with this email already exists. Sign in instead."
        - Other errors → error.message or "Registration failed. Please try again."
      On success (data not null): show the "check your email" success state.
        Do NOT navigate to /dashboard — the user must verify their email first.
        requireEmailVerification: true means an unverified user cannot log in.

    OAuth flow: same as login page. Social sign-in does not require email verification.

    Already-authenticated redirect:
      On mount, call const { data } = await authClient.getSession();
      If data?.session exists, navigate('/dashboard') immediately.

    Client-side validation before submit:
    - Password length < 8 chars: show "Password must be at least 8 characters" without API call.
    - Empty name: show "Name is required" without API call.
  </action>
  <verify>
    grep 'renderRegisterPage' client/src/pages/register.ts
    grep 'signUp.email' client/src/pages/register.ts
    grep "navigate.*'/login'" client/src/pages/register.ts
    grep 'check your email\|verification\|verify' client/src/pages/register.ts -i
  </verify>
  <done>
    client/src/pages/register.ts exports renderRegisterPage.
    Page has name, email, password fields + validation.
    On success shows "check your email" state (does NOT navigate to dashboard).
    On user_already_exists error shows appropriate message.
    Has Google and GitHub OAuth buttons.
    Redirects to /dashboard if already authenticated.
  </done>
</task>

</tasks>

<verification>
1. grep 'renderLoginPage' client/src/pages/login.ts
2. grep 'renderRegisterPage' client/src/pages/register.ts
3. grep 'email_not_verified' client/src/pages/login.ts — should handle this error case
4. grep 'user_already_exists\|USER_ALREADY_EXISTS' client/src/pages/register.ts — should handle
5. npm run build:client exits 0 (run after Plan 05 adds router routes)
</verification>

<success_criteria>
- login.ts: renders email/password form + Google/GitHub OAuth buttons, handles all error cases, redirects to /dashboard on success
- register.ts: renders name/email/password form + OAuth buttons, shows email verification notice on success (no immediate redirect), handles user_already_exists error
- Both pages redirect already-authenticated users to /dashboard on mount
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create .planning/phases/22-authentication/22-04-SUMMARY.md
</output>
