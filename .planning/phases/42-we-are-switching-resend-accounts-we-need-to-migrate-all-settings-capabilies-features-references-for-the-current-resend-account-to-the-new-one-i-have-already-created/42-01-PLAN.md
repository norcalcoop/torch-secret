---
phase: 42-resend-account-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [RESEND-MIGRATE-01]

must_haves:
  truths:
    - "New Resend API key is retrieved from the new account dashboard"
    - "New Audience ID is available (Audience created in new account)"
    - "New default sender address is known (RESEND_FROM_EMAIL value confirmed)"
  artifacts:
    - path: "(no files — credentials held in user memory for Plan 02)"
      provides: "Three values required for Plan 02: RESEND_API_KEY, RESEND_AUDIENCE_ID, RESEND_FROM_EMAIL"
  key_links:
    - from: "Resend dashboard (new account)"
      to: "Plan 02 Infisical CLI commands"
      via: "User provides three values as Plan 02 input"
      pattern: "re_[A-Za-z0-9]+ (API key), UUID (Audience ID), email address (sender)"
---

<objective>
Retrieve all three Resend credentials from the new account dashboard so they are available for Plan 02 to update Infisical and the local .env file.

Purpose: The migration cannot proceed without the actual new credential values. This is the only step that requires opening the Resend web dashboard.
Output: Three values confirmed: new API key, new Audience UUID, new default sender address.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-we-are-switching-resend-accounts-we-need-to-migrate-all-settings-capabilies-features-references-for-the-current-resend-account-to-the-new-one-i-have-already-created/42-CONTEXT.md
@.planning/phases/42-we-are-switching-resend-accounts-we-need-to-migrate-all-settings-capabilies-features-references-for-the-current-resend-account-to-the-new-one-i-have-already-created/42-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Retrieve new Resend credentials from dashboard</name>
  <files></files>
  <action>Human retrieves three credential values from the new Resend account dashboard: API key, Audience ID (after creating a fresh Audience), and default sender address. No CLI automation possible — credentials exist only in the Resend web dashboard and are shown only once at creation time.</action>
  <what-built>This plan starts the Resend account migration. The new Resend account has already been created by you. Claude needs the actual credential values to update Infisical in Plan 02.</what-built>

  <how-to-verify>
    Log in to the new Resend account at https://resend.com/login and complete these three steps:

    **Step 1 — Get the API key:**
    - Navigate to: API Keys (left sidebar)
    - Create a new API key with "Sending access" permission (Full Access is also fine)
    - Copy the full key (starts with `re_`) — it is shown only once

    **Step 2 — Create a new Audience and get its ID:**
    - Navigate to: Audiences (left sidebar)
    - Click "Create Audience"
    - Name it: "Torch Secret" (or any descriptive name)
    - After creation, open the Audience and copy the UUID from the URL or the Audience detail page
    - The UUID looks like: `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`
    - Do NOT import any contacts from the old account — start fresh

    **Step 3 — Note the default sender address (RESEND_FROM_EMAIL):**
    - Navigate to: Domains (left sidebar)
    - Note the default Resend subdomain sender address for this account
    - It will look like: `onboarding@resend.dev` or a similar Resend-managed address
    - If the new account uses the same `onboarding@resend.dev` sender, RESEND_FROM_EMAIL does not need updating
    - If it is different, note the exact value (including the display name format if desired, e.g., `Torch Secret <onboarding@resend.dev>`)

    **When you have all three values, provide them in this format:**
    ```
    RESEND_API_KEY=re_xxxxxxxxxxxxx
    RESEND_AUDIENCE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
    RESEND_FROM_EMAIL=onboarding@resend.dev
    ```
    (Replace placeholder values with your actual new values)

    **Context for comparison (old values being replaced):**
    - Old API key: `re_hNmZgKfp_...` (to be revoked in Plan 03 after new key confirmed working)
    - Old Audience ID: `9ef8f5aa-97f3-4012-b26e-aad3f153cb7f` (abandon — test contacts only, no export needed)
    - Old sender: `onboarding@resend.dev` (check if new account uses same or different address)
  </how-to-verify>

  <verify>Human provides three values: RESEND_API_KEY (starts with re_), RESEND_AUDIENCE_ID (UUID format), RESEND_FROM_EMAIL (email address). Plan 02 cannot proceed until all three are received.</verify>
  <done>All three new Resend credential values received from user and available for Plan 02.</done>
  <resume-signal>Provide the three credential values: RESEND_API_KEY, RESEND_AUDIENCE_ID, RESEND_FROM_EMAIL. Claude will use these in Plan 02.</resume-signal>
</task>

</tasks>

<verification>
Human has provided all three new Resend credential values. Plan 02 can proceed with Infisical updates.
</verification>

<success_criteria>
All three values are available:
- RESEND_API_KEY starting with `re_`
- RESEND_AUDIENCE_ID as a UUID
- RESEND_FROM_EMAIL confirmed (same as old or new value noted)
</success_criteria>

<output>
After completion, create `.planning/phases/42-we-are-switching-resend-accounts-we-need-to-migrate-all-settings-capabilies-features-references-for-the-current-resend-account-to-the-new-one-i-have-already-created/42-01-SUMMARY.md`
</output>
