---
phase: 22-authentication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/api/auth-client.ts
  - client/src/pages/dashboard.ts
autonomous: true
requirements: [AUTH-05, AUTH-08]

must_haves:
  truths:
    - "Browser can import the Better Auth client and call signIn, signUp, signOut, getSession without module errors"
    - "Dashboard stub page renders the logged-in user's name and email and provides a working logout button"
  artifacts:
    - path: "client/src/api/auth-client.ts"
      provides: "Better Auth browser client"
      exports: ["authClient", "signIn", "signUp", "signOut", "getSession"]
    - path: "client/src/pages/dashboard.ts"
      provides: "Minimal authenticated dashboard page"
      exports: ["renderDashboardPage"]
  key_links:
    - from: "client/src/pages/dashboard.ts"
      to: "client/src/api/auth-client.ts"
      via: "authClient.getSession() to load user, authClient.signOut() for logout button"
      pattern: "authClient\\.getSession|authClient\\.signOut"
---

<objective>
Create the Better Auth browser client module and a minimal dashboard stub page that shows the logged-in user's name/email and a logout button. These two files are purely frontend and have no dependency on the server auth wiring (Plan 03), so they run in Wave 1 parallel with Plan 01.

Purpose: Unblocks frontend auth page plans (04, 05) and gives OAuth flows a landing destination.
Output: client/src/api/auth-client.ts, client/src/pages/dashboard.ts
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-authentication/22-RESEARCH.md
@client/src/router.ts
@client/src/pages/create.ts
@client/src/components/layout.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Better Auth browser client</name>
  <files>client/src/api/auth-client.ts</files>
  <action>
    MANDATORY: Before writing any code in this task, invoke the `frontend-design` skill.

    Create client/src/api/auth-client.ts:

    import { createAuthClient } from 'better-auth/client';

    /**
     * Better Auth browser client.
     *
     * baseURL is intentionally omitted — the Vite dev server proxies /api to :3000,
     * and in production the client and server share the same origin.
     * Better Auth infers the base URL from window.location in the browser.
     *
     * Usage:
     *   import { authClient } from './api/auth-client.js';
     *   const { data: session } = await authClient.getSession();
     */
    export const authClient = createAuthClient();

    export const { signIn, signUp, signOut, useSession } = authClient;

    Note: better-auth/client is the correct import for vanilla TypeScript projects.
    Do NOT use better-auth/react (this project has no React dependency).
  </action>
  <verify>
    npx tsc --project client/tsconfig.json --noEmit 2>/dev/null || true
    (Client tsconfig may not exist — if not, rely on Vite type check via npm run build:client)
    The file should import without TypeScript errors.
    Check: grep 'createAuthClient' client/src/api/auth-client.ts
  </verify>
  <done>
    client/src/api/auth-client.ts exists.
    Exports authClient, signIn, signUp, signOut, useSession.
    Uses better-auth/client (not better-auth/react).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create minimal dashboard stub page</name>
  <files>client/src/pages/dashboard.ts</files>
  <action>
    MANDATORY: Before writing any code in this task, invoke the `frontend-design` skill.

    Create client/src/pages/dashboard.ts — a minimal authenticated dashboard page sufficient
    to validate OAuth and session flows in Phase 22. The full dashboard (secret history, labels,
    deletion) is Phase 23.

    The page must:
    1. Call authClient.getSession() on mount.
    2. If no session (null or unauthenticated), redirect to /login via navigate('/login').
    3. If session exists, render:
       - An h1 heading "Dashboard"
       - The user's name and email in a card
       - A "Log out" button that calls authClient.signOut() then navigates to /

    Design guidance:
    - Follow the existing page structure from create.ts: wrapper div with className 'space-y-6',
      header section with h1 and subtext, then content.
    - Use the glassmorphism surface pattern from the codebase: card with bg-surface border
      border-surface-border rounded-xl p-6 for the user info card.
    - Keep the page minimal — this is a stub for auth validation. Phase 23 adds the full UI.
    - Import navigate from '../router.js' for redirects.
    - Import authClient from '../api/auth-client.js'.
    - Export renderDashboardPage(container: HTMLElement): Promise<void>

    Loading state: while getSession() is pending, show a simple "Loading..." text to avoid
    flash of unauthenticated content.

    Error handling: if getSession() throws, redirect to /login.

    Logout flow:
      const result = await authClient.signOut();
      if (!result.error) navigate('/');
      else navigate('/'); // navigate home regardless — session may already be gone
  </action>
  <verify>
    grep 'renderDashboardPage' client/src/pages/dashboard.ts
    grep 'authClient.getSession\|getSession' client/src/pages/dashboard.ts
    grep 'authClient.signOut\|signOut' client/src/pages/dashboard.ts
    grep "navigate.*'/login'" client/src/pages/dashboard.ts
  </verify>
  <done>
    client/src/pages/dashboard.ts exports renderDashboardPage.
    Page checks session on mount and redirects unauthenticated users to /login.
    Renders user name + email when authenticated.
    Logout button signs out and navigates to /.
  </done>
</task>

</tasks>

<verification>
1. grep 'createAuthClient' client/src/api/auth-client.ts — should find import from better-auth/client
2. grep 'renderDashboardPage' client/src/pages/dashboard.ts — should find export
3. grep 'getSession' client/src/pages/dashboard.ts — should find session check
4. npm run build:client exits 0 (once server Plans 01+03 are complete and env is wired — acceptable to defer build check to Plan 06)
</verification>

<success_criteria>
- client/src/api/auth-client.ts exports authClient using better-auth/client (not better-auth/react)
- client/src/pages/dashboard.ts exports renderDashboardPage
- Dashboard page redirects unauthenticated users to /login
- Dashboard page shows user name/email and a functional logout button
</success_criteria>

<output>
After completion, create .planning/phases/22-authentication/22-02-SUMMARY.md
</output>
