---
phase: 48-activate-custom-domain-sending
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - RSND-02
  - RSND-03

must_haves:
  truths:
    - "RESEND_FROM_EMAIL is set to noreply@torchsecret.com in Infisical staging environment"
    - "Staging server is restarted with the new env var injected"
    - "A subscriber confirmation email received in inbox shows From: noreply@torchsecret.com (not onboarding@resend.dev)"
    - "A secret-viewed notification email received in inbox shows From: noreply@torchsecret.com"
  artifacts:
    - path: "Infisical staging environment — RESEND_FROM_EMAIL secret"
      provides: "Env var driving all Resend email From addresses in staging"
      contains: "noreply@torchsecret.com"
  key_links:
    - from: "Infisical RESEND_FROM_EMAIL (staging)"
      to: "server/src/services/notification.service.ts (line 23)"
      via: "env.RESEND_FROM_EMAIL at runtime"
      pattern: "env.RESEND_FROM_EMAIL"
    - from: "Infisical RESEND_FROM_EMAIL (staging)"
      to: "server/src/services/subscribers.service.ts (line 114)"
      via: "env.RESEND_FROM_EMAIL at runtime"
      pattern: "env.RESEND_FROM_EMAIL"
---

<objective>
Update RESEND_FROM_EMAIL to noreply@torchsecret.com in the Infisical staging environment, restart the staging server to inject the new value, then verify that two Resend-powered email types (subscriber confirmation and secret-viewed notification) arrive in inbox with the correct From address.

Purpose: Staged rollout — surface any unexpected Resend API errors in staging before touching production traffic.
Output: Confirmed staging email delivery from noreply@torchsecret.com for both email types.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-activate-custom-domain-sending/48-CONTEXT.md
@.planning/phases/48-activate-custom-domain-sending/48-RESEARCH.md

<interfaces>
<!-- Key runtime paths for RESEND_FROM_EMAIL. No code changes needed — context only. -->

From server/src/services/notification.service.ts (line 23):
```typescript
const { error } = await resend.emails.send({
  from: env.RESEND_FROM_EMAIL,   // reads from env at call time
  to: userEmail,
  subject: 'Your Torch Secret secret was viewed',
  ...
});
```

From server/src/services/subscribers.service.ts (line 114):
```typescript
await resend.emails.send({
  from: env.RESEND_FROM_EMAIL,   // reads from env at call time
  to: email,
  subject: 'Confirm your Torch Secret subscription',
  ...
});
```

From package.json (staging start command):
```json
"staging:up": "infisical run --env=staging -- docker-compose up"
```
Infisical injects RESEND_FROM_EMAIL at process start. A new staging:up after the Infisical update picks up the new value automatically.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Update RESEND_FROM_EMAIL in Infisical staging and restart staging server</name>
  <action>
    1. Open Infisical dashboard → Project → staging environment
    2. Find the RESEND_FROM_EMAIL secret — note the current value before changing (for rollback reference)
    3. Edit: set value to `noreply@torchsecret.com`
    4. Save
    5. In the terminal, restart the staging server so the new env var is injected:
       ```
       npm run staging:up
       ```
       Wait for the server to be healthy (Docker Compose logs show no startup errors).

    IMPORTANT: Do not proceed to Task 2 until the staging server is fully restarted. Infisical injects secrets at process start — a running server will still use the old value until restarted.

    Rollback: If the Infisical dashboard shows an error or the server fails to start, revert RESEND_FROM_EMAIL to its previous value and report the error before continuing.
  </action>
  <verify>Staging server is running (docker-compose up shows no errors) and Infisical staging shows RESEND_FROM_EMAIL = noreply@torchsecret.com</verify>
  <done>Infisical staging RESEND_FROM_EMAIL = noreply@torchsecret.com and staging:up completed without errors</done>
  <resume-signal>Type "staging restarted" once the staging server is running with the updated env var</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify subscriber confirmation and secret-viewed notification emails in staging</name>
  <what-built>Staging server now serves RESEND_FROM_EMAIL=noreply@torchsecret.com. Two manual email triggers confirm end-to-end delivery from the new address.</what-built>
  <action>
    Trigger subscriber confirmation (no login required):
    ```bash
    curl -X POST https://staging.torchsecret.com/api/subscribers \
      -H 'Content-Type: application/json' \
      -d '{"email": "YOUR_TEST_EMAIL@example.com"}'
    ```
    Then trigger secret-viewed notification: log in to staging, create a secret with notification enabled, consume it from incognito. Check inbox for both emails.
  </action>
  <how-to-verify>
    **Trigger 1 — Subscriber confirmation (no login required):**
    ```bash
    curl -X POST https://staging.torchsecret.com/api/subscribers \
      -H 'Content-Type: application/json' \
      -d '{"email": "YOUR_TEST_EMAIL@example.com"}'
    ```
    Expected response: `{"message":"ok"}` or similar success.
    Check inbox: confirmation email should arrive within 60 seconds.
    Open the email: verify the From header shows `noreply@torchsecret.com` (NOT `onboarding@resend.dev`).

    **Trigger 2 — Secret-viewed notification:**
    1. Log in to staging as a test user with a notification email set (or create a secret with notification enabled).
    2. Create a secret with "notify me when viewed" enabled.
    3. Open the secret link in an incognito window to consume it.
    4. Check inbox: From header shows `noreply@torchsecret.com`.

    NOTE: If subscriber confirmation arrives from `onboarding@resend.dev`, the server was not restarted — go back to Task 1. If Resend returns a 403, the domain may have been de-verified — do not proceed to Plan 02 until resolved.
  </how-to-verify>
  <verify>Both emails received in inbox with From: noreply@torchsecret.com</verify>
  <done>Subscriber confirmation and secret-viewed notification both arrive from noreply@torchsecret.com in staging inbox</done>
  <resume-signal>Type "staging verified" if both emails show From: noreply@torchsecret.com, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
Staging environment is confirmed when:
- Infisical staging RESEND_FROM_EMAIL = `noreply@torchsecret.com`
- Staging server restarted after the change
- Subscriber confirmation email received from `noreply@torchsecret.com`
- Secret-viewed notification email received from `noreply@torchsecret.com`
- No Resend API 403 errors observed

If either email arrived from `onboarding@resend.dev`, the server was not restarted. Restart staging:up and re-trigger.
If Resend returns 403: domain verification may have lapsed — check Resend dashboard before proceeding to Plan 02.
</verification>

<success_criteria>
- RESEND_FROM_EMAIL is `noreply@torchsecret.com` in Infisical staging (RSND-02 staging half)
- Subscriber confirmation email From header = `noreply@torchsecret.com` in staging (RSND-03 staging)
- Secret-viewed notification email From header = `noreply@torchsecret.com` in staging (RSND-03 staging)
- Zero Resend 403 errors — confirms Resend still treats noreply@torchsecret.com as an authorized sender
</success_criteria>

<output>
After completion, create `.planning/phases/48-activate-custom-domain-sending/48-01-SUMMARY.md` documenting:
- Previous and new values of RESEND_FROM_EMAIL in Infisical staging
- Which email types were triggered and from which address they arrived
- Any errors or anomalies observed during the Infisical update or server restart
</output>
