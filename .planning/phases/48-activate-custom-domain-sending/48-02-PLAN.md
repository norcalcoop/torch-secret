---
phase: 48-activate-custom-domain-sending
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified: []
autonomous: false
requirements:
  - RSND-02
  - RSND-03
  - LOOP-03

must_haves:
  truths:
    - "RESEND_FROM_EMAIL is set to noreply@torchsecret.com in Infisical production environment"
    - "Production server is redeployed with the new env var injected"
    - "A subscriber confirmation email from production shows From: noreply@torchsecret.com"
    - "A secret-viewed notification email from production shows From: noreply@torchsecret.com"
    - "A Loops.so welcome email inspected via Gmail Show original shows dkim=pass header.i=@torchsecret.com and no via loops.so indicator"
  artifacts:
    - path: "Infisical production environment — RESEND_FROM_EMAIL secret"
      provides: "Env var driving all Resend email From addresses in production"
      contains: "noreply@torchsecret.com"
  key_links:
    - from: "Infisical RESEND_FROM_EMAIL (production)"
      to: "server/src/services/notification.service.ts"
      via: "env.RESEND_FROM_EMAIL injected by infisical run --env=production at Render deploy"
      pattern: "env.RESEND_FROM_EMAIL"
    - from: "Loops domain authentication (Phase 47 LOOP-01 + LOOP-02)"
      to: "Gmail Authentication-Results header"
      via: "DKIM torchsecret.com selectors pointing to Loops/SES DKIM endpoints"
      pattern: "dkim=pass header.i=@torchsecret.com"
    - from: "subscribers.service.ts confirmSubscriber()"
      to: "loops.sendEvent({ eventName: 'subscribed' })"
      via: "subscriber confirmation link click"
      pattern: "loops.sendEvent"
---

<objective>
Update RESEND_FROM_EMAIL to noreply@torchsecret.com in the Infisical production environment, redeploy production on Render.com, verify the two Resend email types in production, then trigger and inspect a Loops.so welcome email to confirm no "via loops.so" header indicators remain.

Purpose: Complete the env var rollout to production and confirm LOOP-03 — the Loops domain authentication from Phase 47 eliminates "via" indicators automatically once a real email is inspected.
Output: All three success criteria from the phase roadmap satisfied: both Resend email types confirmed from noreply@torchsecret.com, Loops welcome email DKIM-aligned on torchsecret.com.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-activate-custom-domain-sending/48-CONTEXT.md
@.planning/phases/48-activate-custom-domain-sending/48-RESEARCH.md
@.planning/phases/48-activate-custom-domain-sending/48-01-SUMMARY.md

<interfaces>
<!-- How to trigger each email type in production. No code changes. -->

**Subscriber confirmation trigger (no login needed) — also fires Loops welcome as a side effect:**
```bash
curl -X POST https://torchsecret.com/api/subscribers \
  -H 'Content-Type: application/json' \
  -d '{"email": "YOUR_TEST_EMAIL@example.com"}'
# Expected: {"message":"ok"}
# Then CLICK the confirmation link in the Resend email → triggers Loops 'subscribed' event
```

**Loops subscribed event — triggered by subscriber confirmation click:**
From server/src/services/subscribers.service.ts (line 171):
```typescript
void loops.sendEvent({
  email: subscriber.email,
  eventName: 'subscribed',
  contactProperties: { source: 'email-capture' },
}).catch(...)
```
CRITICAL: The Loops `subscribed` event fires from `confirmSubscriber()`, NOT from Better Auth user registration. To trigger the Loops welcome email: POST to /api/subscribers, receive the Resend confirmation email, click the confirmation link.

**Gmail raw header inspection:**
Gmail → open email → top-right three-dot menu → "Show original"
Success indicators in Authentication-Results:
  - `dkim=pass header.i=@torchsecret.com` — DKIM aligned on torchsecret.com
  - No `via loops.so` in Gmail From display
  - No `via amazonses.com` in Gmail From display
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Update RESEND_FROM_EMAIL in Infisical production and redeploy on Render</name>
  <action>
    Prerequisite: Plan 01 must be complete — staging verified before touching production (locked decision).

    1. Open Infisical dashboard → Project → production environment
    2. Find the RESEND_FROM_EMAIL secret — note the current value before changing (for rollback reference)
    3. Edit: set value to `noreply@torchsecret.com`
    4. Save
    5. Trigger a production redeploy on Render.com:
       - Go to Render.com → Service → Manual Deploy (or click "Deploy latest commit")
       - Wait for the deploy to complete and the service to show "Live"
       - Render triggers a fresh Infisical injection on each deploy — the new RESEND_FROM_EMAIL value will be active after the deploy completes

    Rollback: If the Render deploy fails or the service goes unhealthy, revert RESEND_FROM_EMAIL to its previous value in Infisical and redeploy. The prior value was the one noted in step 2.
  </action>
  <verify>Render.com service shows "Live" after the deploy that follows the Infisical update</verify>
  <done>Infisical production RESEND_FROM_EMAIL = noreply@torchsecret.com and Render deploy completed with service Live</done>
  <resume-signal>Type "production redeployed" once Render shows the service as Live with the new deploy</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify Resend emails in production and inspect Loops welcome email headers</name>
  <what-built>Production now serves RESEND_FROM_EMAIL=noreply@torchsecret.com. All three email types are triggered and confirmed in this single step.</what-built>
  <action>
    Trigger subscriber confirmation (fires Resend confirmation email and, after link click, the Loops welcome email):
    ```bash
    curl -X POST https://torchsecret.com/api/subscribers \
      -H 'Content-Type: application/json' \
      -d '{"email": "YOUR_TEST_EMAIL@example.com"}'
    ```
    Click the confirmation link in the received email. Then trigger secret-viewed notification: log in, create a secret with notification enabled, consume from incognito. Inspect Loops welcome email via Gmail "Show original".
  </action>
  <how-to-verify>
    **Step 1 — Subscriber confirmation email (triggers Loops welcome as a side effect):**
    ```bash
    curl -X POST https://torchsecret.com/api/subscribers \
      -H 'Content-Type: application/json' \
      -d '{"email": "YOUR_TEST_EMAIL@example.com"}'
    ```
    - Check inbox for the Resend confirmation email
    - Verify From: `noreply@torchsecret.com`
    - CLICK THE CONFIRMATION LINK in the email — this triggers the Loops `subscribed` event and fires the Loops welcome email to the same address

    **Step 2 — Inspect Loops welcome email raw headers:**
    After clicking the confirmation link, check inbox for the Loops welcome email (From: hello@torchsecret.com).
    - Open the Loops welcome email
    - Gmail → three-dot menu → "Show original"
    - Find `Authentication-Results` in the raw headers — should show:
      ```
      dkim=pass header.i=@torchsecret.com ...
      ```
    - Confirm the Gmail From display shows `hello@torchsecret.com` with no `via loops.so` or `via amazonses.com` label

    **Step 3 — Secret-viewed notification:**
    1. Log in to production as a test user
    2. Create a secret with notification email enabled
    3. Open the secret link in incognito to consume it
    4. Check inbox: From header = `noreply@torchsecret.com`

    NOTE: If the Loops welcome email does not arrive within 5 minutes, check the Loops.so dashboard → Contacts to confirm the contact was created and the `subscribed` event fired.
  </how-to-verify>
  <verify>All three emails received: subscriber confirmation From=noreply@torchsecret.com, Loops welcome dkim=pass on torchsecret.com, notification From=noreply@torchsecret.com</verify>
  <done>All three email types confirmed from correct From addresses; Loops Gmail "Show original" shows dkim=pass header.i=@torchsecret.com; no "via loops.so" indicator present</done>
  <resume-signal>Type "production verified" if all three email types confirmed with correct headers. Or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
Production environment and LOOP-03 are confirmed when:
- Infisical production RESEND_FROM_EMAIL = `noreply@torchsecret.com`
- Render.com redeploy complete — service shows Live
- Subscriber confirmation email received from `noreply@torchsecret.com` in production
- Secret-viewed notification email received from `noreply@torchsecret.com` in production
- Loops welcome email received with Gmail "Show original" showing:
  - `dkim=pass header.i=@torchsecret.com`
  - No `via loops.so` in Gmail From display
  - No `via amazonses.com` in Gmail From display

All four Phase 48 success criteria from ROADMAP.md satisfied:
1. RESEND_FROM_EMAIL = noreply@torchsecret.com in both staging and production Infisical (RSND-02 complete)
2. Secret-viewed notification from noreply@torchsecret.com (RSND-03)
3. Subscriber confirmation from noreply@torchsecret.com (RSND-03)
4. Loops welcome email — no "via" third-party header indicators (LOOP-03)
</verification>

<success_criteria>
- RESEND_FROM_EMAIL = `noreply@torchsecret.com` in Infisical production (RSND-02 complete)
- Subscriber confirmation and secret-viewed notification arrive from `noreply@torchsecret.com` in production (RSND-03 complete)
- Loops welcome email Gmail "Show original" shows `dkim=pass header.i=@torchsecret.com` with no "via loops.so" (LOOP-03 complete)
- All three requirements RSND-02, RSND-03, LOOP-03 satisfied — Phase 48 complete
</success_criteria>

<output>
After completion, create `.planning/phases/48-activate-custom-domain-sending/48-02-SUMMARY.md` documenting:
- Previous and new values of RESEND_FROM_EMAIL in Infisical production
- Render.com deploy confirmation (deploy ID or timestamp if available)
- From headers observed for subscriber confirmation and secret-viewed notification in production
- Loops welcome email raw header excerpt (Authentication-Results line confirming dkim=pass)
- Final status of all three requirements: RSND-02, RSND-03, LOOP-03
</output>
