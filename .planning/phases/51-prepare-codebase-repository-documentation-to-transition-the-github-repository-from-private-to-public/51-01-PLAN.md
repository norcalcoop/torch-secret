---
phase: 51-prepare-codebase-repository-documentation-to-transition-the-github-repository-from-private-to-public
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "git ls-files .planning/ | wc -l outputs 0 (directory fully untracked)"
    - "git ls-files CLAUDE.md outputs nothing (file untracked)"
    - "docs/hybrid-users.md, docs/secret-share-prd.md, and docs/screenshots/ are absent from git index and disk"
    - ".gitignore contains CLAUDE.md and .planning/ entries"
  artifacts:
    - path: ".gitignore"
      provides: "Prevents CLAUDE.md and .planning/ from ever being re-committed"
      contains: "CLAUDE.md"
  key_links:
    - from: ".gitignore"
      to: ".planning/"
      via: "gitignore entry prevents re-staging after git rm --cached"
      pattern: "^\\.planning/"
    - from: ".gitignore"
      to: "CLAUDE.md"
      via: "gitignore entry prevents re-staging after git rm --cached"
      pattern: "^CLAUDE\\.md"
---

<objective>
Delete legacy docs files from the old SecureShare brand, untrack .planning/ and CLAUDE.md from git while keeping them on disk, and update .gitignore with the two new entries.

Purpose: Remove internal artifacts and pre-brand-migration docs from the public git history going forward. The files remain functional on disk — local GSD workflow and Claude Code continue working unchanged.
Output: Cleaned git index, updated .gitignore, legacy docs deleted from git and disk.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-prepare-codebase-repository-documentation-to-transition-the-github-repository-from-private-to-public/51-CONTEXT.md
@.planning/phases/51-prepare-codebase-repository-documentation-to-transition-the-github-repository-from-private-to-public/51-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy docs files from git and disk</name>
  <files>docs/hybrid-users.md, docs/secret-share-prd.md, docs/screenshots/create-dark.png, docs/screenshots/phase11-error-page.png, docs/screenshots/phase11-homepage.png, docs/screenshots/reveal-dark.png</files>
  <action>
Run `git rm -r docs/screenshots/ docs/hybrid-users.md docs/secret-share-prd.md` to remove all six files atomically from both the git index and disk.

These are internal planning artifacts and screenshots from the pre-rebrand SecureShare era. They have no public value and reference the old brand. Using `git rm -r` for the screenshots directory handles all four PNG files at once without needing to list them individually.

After the deletion, verify with `git ls-files docs/ | grep -E 'hybrid-users|secret-share-prd|screenshots'` — this must return no output. The `docs/cached-frolicking-lemon.md` file is already gitignored (via `cached-*.md` pattern) and not tracked — no action needed for it.

Do NOT remove the `!docs/screenshots/*.png` exception line from `.gitignore` — it becomes a harmless no-op after the directory is gone, and removing it could cause confusion if the directory is ever recreated.

Stage the deletions but do not commit yet — the next task will commit everything together.
  </action>
  <verify>
    <automated>cd /Users/ourcomputer/Github-Repos/secureshare && git ls-files docs/ | grep -E 'hybrid-users|secret-share-prd|screenshots'</automated>
  </verify>
  <done>The six legacy files are absent from git index and disk. `git ls-files docs/` shows only `docs/cached-frolicking-lemon.md` is no longer tracked (it was already gitignored). No phantom index entries remain.</done>
</task>

<task type="auto">
  <name>Task 2: Untrack .planning/ and CLAUDE.md, update .gitignore, commit all changes</name>
  <files>.gitignore</files>
  <action>
Step 1 — Add two entries to `.gitignore`. Insert after the existing `.claude.local.md` line (line 7):

```
CLAUDE.md
.planning/
```

Note: The existing `.planning/security/` entry lower in the file becomes redundant but is harmless — leave it in place. The broader `.planning/` rule supersedes it.

Step 2 — Untrack the files from git index (files remain on disk):

```bash
git rm -r --cached .planning/
git rm --cached CLAUDE.md
```

This produces a large removal (570+ files from .planning/) — that is expected and correct. The files continue to exist on disk and remain functional for local GSD workflow.

Step 3 — Stage the `.gitignore` change (the doc deletions from Task 1 are already staged):

```bash
git add .gitignore
```

Step 4 — Commit all staged changes (docs deletion + gitignore update + untracking) in a single commit:

```bash
git commit -m "chore: remove legacy docs, untrack .planning/ and CLAUDE.md from public repo"
```

Using one commit keeps the history clean for public viewers: the intent is clear, and the large index removal is bounded to a single commit.

Verify .planning/ and CLAUDE.md are still present on disk after the commit:

```bash
ls .planning/STATE.md CLAUDE.md
```

Both must exist on disk. If either is missing, the `--cached` flag was omitted (files were deleted from disk too) — stop and investigate before proceeding.
  </action>
  <verify>
    <automated>cd /Users/ourcomputer/Github-Repos/secureshare && git ls-files .planning/ | wc -l | tr -d ' ' | grep -x '0' && git ls-files CLAUDE.md | wc -l | tr -d ' ' | grep -x '0' && ls .planning/STATE.md CLAUDE.md > /dev/null 2>&1 && echo "PASS"</automated>
  </verify>
  <done>
    - `git ls-files .planning/ | wc -l` outputs 0
    - `git ls-files CLAUDE.md` outputs nothing
    - `.planning/STATE.md` exists on disk (local GSD workflow intact)
    - `CLAUDE.md` exists on disk (Claude Code will still find it)
    - `.gitignore` contains `CLAUDE.md` and `.planning/` entries
    - All changes are committed to the branch
  </done>
</task>

</tasks>

<verification>
After both tasks are complete:

1. `git ls-files .planning/ | wc -l` must output `0`
2. `git ls-files CLAUDE.md` must return nothing
3. `git ls-files docs/ | grep -E 'hybrid-users|secret-share-prd|screenshots'` must return nothing
4. `ls .planning/STATE.md CLAUDE.md` must succeed (files still on disk)
5. `grep -n 'CLAUDE.md' .gitignore` must show the new entry
6. `grep -n '\.planning/' .gitignore` must show the new entry
</verification>

<success_criteria>
All internal planning artifacts and legacy brand docs are removed from the public git index. .planning/ and CLAUDE.md remain fully functional on disk. .gitignore prevents accidental re-staging. Changes committed in one clean commit.
</success_criteria>

<output>
After completion, create `.planning/phases/51-prepare-codebase-repository-documentation-to-transition-the-github-repository-from-private-to-public/51-01-SUMMARY.md`
</output>
