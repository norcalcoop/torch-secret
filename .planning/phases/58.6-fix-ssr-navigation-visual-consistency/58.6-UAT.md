---
status: resolved
phase: 58.6-fix-ssr-navigation-visual-consistency
source: [58.6-01-SUMMARY.md]
started: 2026-03-08T00:00:00Z
updated: 2026-03-09T00:00:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Theme Dropdown on SSR Pages
expected: Visit an SSR page (e.g., http://torchsecret.localhost:1355/vs/onetimesecret). The header theme toggle should be a dropdown (not a moon-cycle button). Clicking or hovering the toggle should reveal three options: Light, Dark, System.
result: issue
reported: "the header theme toggle does not display the system icon when I select the system theme"
severity: major

### 2. Theme Dropdown Active State
expected: With the dropdown open on an SSR page, the currently active theme option (whichever of Light/Dark/System matches your current setting) should be visually marked as active/highlighted. Selecting a different option should apply the theme and update which option is marked active.
result: issue
reported: "the header icon never changes"
severity: major

### 3. Dark Dot-Grid in Dark Mode on SSR Pages
expected: With dark mode active (either system preference or manually set to Dark), visit an SSR page. The dot-grid background should appear dark (subtle purple-tinted dots on a dark background) — not bright white dots. This should be correct immediately on page load before JavaScript runs.
result: pass

### 4. Terminal Color Tokens on SSR Pages
expected: On an SSR page that contains a terminal/code block, the terminal should render with correct green-tinted colors: dark green text, a slightly lighter green header bar, and a dark green background — matching what the SPA pages show.
result: skipped
reason: No SSR pages currently render terminal/code blocks; tokens are defined but unused in page content

## Summary

total: 4
passed: 1
issues: 2
pending: 0
skipped: 1

## Gaps

- truth: "Theme toggle dropdown on SSR pages shows the correct icon for the active theme (including the monitor/system icon when System is selected)"
  status: resolved
  reason: "User reported: the header theme toggle does not display the system icon when I select the system theme"
  severity: major
  test: 1
  root_cause: "themeDropdownScript IIFE never updates the <summary> icon. The <span class=\"ssr-theme-summary-icon\"> is hardcoded with MOON_SVG at server render time (layout.ts:115) and no JavaScript ever replaces it. Additionally, the load-time block only sets .active class on menu buttons — it never inspects localStorage to select the correct icon on page load."
  artifacts:
    - path: "server/src/routes/seo/templates/layout.ts"
      issue: "Line 115: <span class=\"ssr-theme-summary-icon\"> hardcoded with MOON_SVG at render time; never updated by script"
    - path: "server/src/routes/seo/templates/layout.ts"
      issue: "Lines 250–273: themeDropdownScript IIFE — icon-update pathway entirely absent from both load-time block (256–257) and click handler (259–268)"
  missing:
    - "Icon-update helper inside IIFE: read localStorage pref → map light→SUN_SVG, dark→MOON_SVG, system→MONITOR_SVG → replace .ssr-theme-summary-icon innerHTML. SVG strings must be embedded as JS string literals inside the <script> tag."
    - "Call the helper once at load time (after the .active class loop) to show correct icon on page load"
    - "Call the helper once in the click handler (before details.open = false) to update icon immediately on selection"
  debug_session: ""

- truth: "The header summary icon updates to reflect the currently selected theme (moon for Dark, sun for Light, monitor for System)"
  status: resolved
  reason: "User reported: the header icon never changes"
  severity: major
  test: 2
  root_cause: "Same root cause as gap 1 — the IIFE click handler (lines 259–268) applies theme to <html> and sets .active on menu buttons but never touches .ssr-theme-summary-icon innerHTML. Both gaps share a single missing icon-update helper."
  artifacts:
    - path: "server/src/routes/seo/templates/layout.ts"
      issue: "Lines 259–268: click handler applies theme but never updates summary icon"
  missing:
    - "Call the icon-update helper inside the click handler (same fix as gap 1)"
  debug_session: ""
