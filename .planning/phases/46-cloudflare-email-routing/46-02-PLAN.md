---
phase: 46-cloudflare-email-routing
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified: []
autonomous: false
requirements:
  - EROT-02

must_haves:
  truths:
    - "An email sent from an external account to hello@torchsecret.com arrives in torch-secret@gmail.com inbox"
    - "An email sent from an external account to security@torchsecret.com arrives in torch-secret@gmail.com inbox"
    - "Cloudflare MX DNS records are live and resolvable from public DNS"
  artifacts:
    - path: "Gmail inbox (torch-secret@gmail.com)"
      provides: "2 delivered test messages confirming end-to-end forwarding works"
  key_links:
    - from: "External sender (non-torchsecret.com account)"
      to: "hello@torchsecret.com"
      via: "Cloudflare Email Routing forwarding pipeline"
      pattern: "Message appears in torch-secret@gmail.com inbox, not spam"
    - from: "External sender"
      to: "security@torchsecret.com"
      via: "Cloudflare Email Routing forwarding pipeline"
      pattern: "Message appears in torch-secret@gmail.com inbox, not spam"
---

<objective>
Verify that the Cloudflare Email Routing configuration from Plan 01 is working end-to-end: send real test emails to hello@torchsecret.com and security@torchsecret.com from an external account and confirm both arrive in torch-secret@gmail.com.

Purpose: EROT-02 requires live delivery verification, not just dashboard status confirmation. This plan provides the observable proof that the routing pipeline works.
Output: Confirmed end-to-end email delivery for 2 of 7 addresses (hello + security); remaining 5 confirmed by Active status per CONTEXT.md decisions.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-cloudflare-email-routing/46-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Verify DNS propagation and send test emails to confirm end-to-end delivery</name>
  <action>Human action required. Claude cannot send external emails or access Gmail. This task verifies Plan 01 configuration is live by running DNS checks and sending real test emails.

  STEP 1 — Confirm DNS is live (run in terminal):
    dig MX torchsecret.com +short
  Expected: all 3 Cloudflare MX records (amir/linda/isaac.mx.cloudflare.net at priorities 13/24/86).
  If empty: wait 5 minutes and retry before sending test emails.

    dig TXT torchsecret.com +short
  Expected: contains "v=spf1 include:_spf.mx.cloudflare.net ~all"

  STEP 2 — Send test email to hello@torchsecret.com:
  From any non-torchsecret.com email account (personal Gmail, Fastmail, iCloud — any external account):
  1. Compose a new email
  2. To: hello@torchsecret.com
  3. Subject: "Test forwarding — hello address"
  4. Body: any text (e.g., "Testing Cloudflare Email Routing for hello address.")
  5. Send

  STEP 3 — Send test email to security@torchsecret.com:
  From the same external account:
  1. Compose a new email
  2. To: security@torchsecret.com
  3. Subject: "Test forwarding — security address"
  4. Body: any text
  5. Send

  STEP 4 — Confirm delivery in torch-secret@gmail.com:
  Open torch-secret@gmail.com:
  1. Check INBOX first — both test emails should appear within 1-3 minutes (the Gmail "Never send to Spam" filter from Plan 01 Task 2 should keep them out of spam)
  2. If not in inbox after 3 minutes, check the SPAM folder
  3. Confirm both emails arrived — the "To" or "Delivered-To" header should show the @torchsecret.com address

  STEP 5 — Confirm Active status for all 7 rules:
  Return to Cloudflare dashboard → Email → Email Routing → Routing rules.
  Confirm all 7 rules (hello, contact, admin, info, support, security, privacy) show "Active" status.

  If test emails land in spam (not inbox): the Gmail filter from Plan 01 Task 2 may not have been saved correctly. Verify the filter exists in Gmail Settings → Filters. If missing, recreate it (see Plan 01 Task 2) and mark the emails as "Not spam."

  If test emails do not arrive at all (not in inbox or spam):
  1. Confirm all 7 rules show Active (not Pending) in Cloudflare dashboard
  2. If rules show Pending, the torch-secret@gmail.com destination was not verified — return to Destination addresses tab and complete verification
  3. Check that torch-secret@gmail.com has no delivery issues (storage quota, account restrictions)
  4. Wait 5 minutes for MX DNS to fully propagate and retry the send</action>
  <verify>
    <automated>dig MX torchsecret.com +short</automated>
    Expected: 3 lines with amir.mx.cloudflare.net (priority 13), linda.mx.cloudflare.net (priority 24), isaac.mx.cloudflare.net (priority 86).
    Manual: test email to hello@torchsecret.com appeared in torch-secret@gmail.com; test email to security@torchsecret.com appeared in torch-secret@gmail.com; all 7 rules show Active in Cloudflare dashboard.
  </verify>
  <done>Both test emails (hello@torchsecret.com and security@torchsecret.com) delivered to torch-secret@gmail.com inbox. All 7 routing rules show Active status. dig MX torchsecret.com returns all 3 Cloudflare MX records.</done>
</task>

</tasks>

<verification>
Final state check — run after delivery is confirmed:

```bash
# All 3 Cloudflare MX records must be present
dig MX torchsecret.com +short
# Expected:
# 13 amir.mx.cloudflare.net.
# 24 linda.mx.cloudflare.net.
# 86 isaac.mx.cloudflare.net.

# Cloudflare SPF record must be present
dig TXT torchsecret.com +short
# Expected (contains):
# "v=spf1 include:_spf.mx.cloudflare.net ~all"
```

Manual confirmation:
- hello@torchsecret.com test email delivered to torch-secret@gmail.com inbox
- security@torchsecret.com test email delivered to torch-secret@gmail.com inbox
- Cloudflare dashboard: all 7 rules Active
</verification>

<success_criteria>
- `dig MX torchsecret.com +short` returns all 3 Cloudflare MX records (amir/linda/isaac.mx.cloudflare.net at priorities 13/24/86)
- Test email sent to hello@torchsecret.com from external account arrives in torch-secret@gmail.com
- Test email sent to security@torchsecret.com from external account arrives in torch-secret@gmail.com
- All 7 routing rules show Active status in Cloudflare Email Routing dashboard
- Phase 46 success criteria from ROADMAP.md are fully satisfied: inbound routing is live and verified for business addresses that Phase 49 will rely on for alias verification emails
</success_criteria>

<output>
After completion, create `.planning/phases/46-cloudflare-email-routing/46-02-SUMMARY.md` with:
- DNS verification output (paste dig MX and dig TXT results)
- Confirmation that hello@torchsecret.com test email arrived (timestamp, subject)
- Confirmation that security@torchsecret.com test email arrived (timestamp, subject)
- Final Active status for all 7 routing rules
- Note for Phase 49: forwarding is now live — Gmail alias verification emails will route correctly
</output>
