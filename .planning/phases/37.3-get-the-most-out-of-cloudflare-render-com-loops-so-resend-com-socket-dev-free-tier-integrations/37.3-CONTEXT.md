# Phase 37.3: Free Tier Integrations - Context

**Gathered:** 2026-02-28
**Status:** Ready for planning

<domain>
## Phase Boundary

Configure and fully utilize the free tiers of five services already partially in use: Cloudflare, Render.com, Loops.so, Resend.com, and Socket.dev. No new application features except account deletion, which is included here because the Loops GDPR deletion call requires an actual deletion endpoint to exist.

Each service has a different maturity level going in:
- **Resend.com** — Most complete: Audience sync is wired (confirm → add, unsubscribe → mark). Only gap is render.yaml parity.
- **Loops.so** — Two gaps: confirmed subscribers are not synced to Loops; account deletion (new) needs a deleteContact call.
- **Render.com** — Configuration gap: render.yaml is missing ~10 env var stubs vs .env.example.
- **Cloudflare** — Proxied (orange cloud) but no page rules, caching config, security settings, or Worker in the repo.
- **Socket.dev** — Zero integration. GitHub app not installed, no documentation.

</domain>

<decisions>
## Implementation Decisions

### Cloudflare — caching strategy
- Cache `/assets/*` for 1 year (Vite-hashed filenames make this safe — content changes = new filename)
- HTML responses (`/`, `/create`, `/reveal/*`, etc.) cached short or bypassed — they are the SPA entry point and must stay fresh
- Configure as Cloudflare Cache Rules (not legacy Page Rules)

### Cloudflare — security posture
- Enable **Bot Fight Mode** (free tier) — blocks known bot traffic at the edge
- Enable **DDoS protection** (on by default for proxied domains, just verify it's active)
- Do NOT add Cloudflare rate limiting rules — the app has Express + Redis rate limiting already; two rate limiters fighting causes more problems than it solves
- No WAF custom rules needed at this stage

### Cloudflare — analytics
- Enable Cloudflare Web Analytics (free, privacy-friendly, no JS snippet needed for proxied domains)
- Provides traffic visibility without relying on client-side PostHog for infrastructure metrics

### Cloudflare Worker — keep-alive (dual purpose)
- Create a **Cloudflare Worker with a cron trigger** that pings `https://[app-domain]/api/health` every 10 minutes
- Dual purpose: (1) prevents Render.com free tier spin-down (~60s cold starts), (2) demonstrates Cloudflare Workers free tier usage
- Worker lives in the repo (`workers/keep-alive/`) with a `wrangler.toml` config
- **Language:** TypeScript — matches project stack
- **Target URL:** hardcoded in the Worker script (not a wrangler.toml env var); the Worker has exactly one job
- **Deploy strategy:** manual `wrangler deploy` from a dev machine, documented in README. CI integration is over-engineering for a trivial ping Worker that rarely changes.
- Ping is fire-and-forget — no alerting, no state. Just a GET request that keeps the service warm.

### Render.com — env var parity
- render.yaml updated to full parity with `.env.example`
- All missing env vars added as `sync: false` stubs:
  - `LOOPS_API_KEY`, `APP_URL`, `BETTER_AUTH_TRUSTED_ORIGINS`
  - `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRO_PRICE_ID`
  - `RESEND_AUDIENCE_ID`, `IP_HASH_SALT`
  - `POSTHOG_PERSONAL_API_KEY`, `POSTHOG_PROJECT_ID` — add with comment `# PostHog REST API — used for scripted dashboard setup (Phase 37.1), not required at runtime`
- Values stay out of the repo — stubs are reminders for the Render dashboard

### Render.com — expiration worker
- Keep the expiration worker in-process (node-cron in the main server process) — no change
- No separate Render cron service; free tier limits make in-process the right choice

### Loops.so — subscriber sync (new)
- When a marketing subscriber confirms their email (double opt-in), fire:
  ```
  loops.sendEvent({ email, eventName: 'subscribed', contactProperties: { source: 'email-capture' } })
  ```
- Call goes in `subscribers.service.ts` `confirmSubscriber()` — alongside the existing Resend Audience sync
- Zero-knowledge safe: only email + source property, no userId or secretId

### Loops.so — account deletion (new)
- Implement a minimal account deletion flow:
  - **UI:** "Delete account" button on the dashboard settings section
  - **Confirmation:** Modal requiring the user to type `delete` to confirm. Prevents accidental deletion (standard pattern: GitHub, Linear, etc.)
  - **Backend:** New endpoint (e.g., `DELETE /api/me`) that:
    1. Calls `loops.deleteContact({ email })` before removing the user (GDPR hygiene)
    2. Sets `secrets.user_id = NULL` for all of the user's secrets (existing shared links keep working)
    3. Deletes the user via Better Auth
    4. Clears the session
  - **After deletion:** Redirect to home page with a success toast
- Zero-knowledge safe: only email is passed to Loops, no secretId co-location

### Loops.so — what NOT to add
- No secret lifecycle events (created, viewed, expired) — too frequent, risks zero-knowledge invariant
- No separate unsubscribe event to Loops — Loops manages unsubscribes via its own links; the `deleteContact` on account deletion covers the GDPR case
- `registered` event fires on full account creation (already wired in onboarding.service.ts) — no change needed

### Resend.com — status
- Audience sync is already fully wired in `subscribers.service.ts`
- No new Resend code changes needed
- render.yaml parity (above) covers the missing `RESEND_AUDIENCE_ID` stub

### Socket.dev — integration approach
- Install the **Socket.dev GitHub App** via the GitHub repo settings (or github.com/apps/socket-security)
- No code changes required — Socket reads `package.json` and `package-lock.json` automatically
- GitHub app posts PR comments and checks when a dependency change introduces supply chain risk

### Socket.dev — config file
- Add a minimal `socket.yml` to the repo root to document the policy intent (even if defaults are fine)
- Marking known-safe exceptions or adjusting thresholds signals intentionality to future contributors
- Claude's Discretion: determine if any existing dependencies trigger Socket warnings and whether they need explicit allowlisting

### Socket.dev — documentation
- Add a brief note in the README security section: "Dependency changes are automatically scanned by Socket.dev on PRs for supply chain security."

### CI auto-triggers — re-enable
- Re-enable GitHub Actions CI auto-triggers: `on: push: branches: [main]` + `on: pull_request`
- No `paths-ignore` — run CI on all changes including docs
- Currently `workflow_dispatch`-only; re-enabling activates Render's `autoDeployTrigger: checksPass` gate
- GitHub Secrets (`INFISICAL_CLIENT_ID`, `INFISICAL_CLIENT_SECRET`) are already configured — re-enable is safe
- Claude's Discretion: keep `workflow_dispatch` alongside the new triggers so manual runs remain available

### Claude's Discretion
- Whether `socket.yml` needs any package allowlisting (requires inspecting current `package-lock.json` against Socket's database)
- Exact Cloudflare Cache Rule TTLs for HTML (5 min, 1 hour, or bypass — depends on deploy frequency)
- Keep `workflow_dispatch` alongside new push/PR triggers or remove it

</decisions>

<specifics>
## Specific Ideas

- The Cloudflare Worker for uptime pinging doubles as a real demonstration of Cloudflare Workers free tier — not just infrastructure but a tangible artifact in the codebase
- "Full parity with .env.example" was the explicit goal for render.yaml — single source of truth for what a production deploy needs
- Loops contact deletion on account deletion is explicitly GDPR hygiene, not just nice-to-have
- Socket.dev PR checks are "zero friction" — the primary value is visibility without needing CI to pass first
- The `autoDeployTrigger: checksPass` in render.yaml means CI re-enablement directly activates the production deploy gate

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- `server/src/config/loops.ts`: Loops SDK singleton (`loops`) — import this, never create a new LoopsClient() elsewhere
- `server/src/services/onboarding.service.ts`: Existing `loops.sendEvent()` pattern with `contactProperties` — follow this pattern for the `subscribed` event
- `server/src/services/subscribers.service.ts`: `confirmSubscriber()` — add Loops call here, fire-and-forget alongside existing Resend Audience sync
- `server/src/middleware/require-auth.ts`: Auth guard middleware — use for the `DELETE /api/me` endpoint
- `render.yaml`: Already has `sync: false` stub pattern established for secrets

### Established Patterns
- Fire-and-forget side effects: Resend Audience sync in subscribers.service.ts uses `void promise.catch(console.error)` — use same pattern for Loops calls
- Zod env validation: `LOOPS_API_KEY` is already validated in `server/src/config/env.ts`
- Better Auth deletion: research how to delete a user via Better Auth (no existing pattern in this codebase)

### Integration Points
- `confirmSubscriber()` in subscribers.service.ts — add `loops.sendEvent('subscribed')` call
- New `DELETE /api/me` route — wire to account deletion service
- Dashboard page (`client/src/pages/dashboard`) — add "Delete account" UI section with confirmation modal
- `.github/workflows/ci.yml` — change `on: workflow_dispatch` to add push/PR triggers
- `render.yaml` — add ~10 missing env var stubs

</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 37.3-get-the-most-out-of-cloudflare-render-com-loops-so-resend-com-socket-dev-free-tier-integrations*
*Context gathered: 2026-02-28*
