---
phase: 58.2-launch-quick-wins-do-now
plan: "01"
subsystem: testing
tags: [qrcode, vite, vitest, tdd, cjs-esm, optimizeDeps]

# Dependency graph
requires: []
provides:
  - qrcode npm package installed and CJS pre-bundled for ESM build via optimizeDeps
  - 4 component test stub files covering autoClearMs countdown, suggestion hint, QR toggle, mailto href
  - Wave 0 gate complete — Wave 1 implementation plans may proceed
affects: [58.2-02, 58.2-03, 58.2-04, 58.2-05]

# Tech tracking
tech-stack:
  added: [qrcode@^1.5.4, "@types/qrcode@^1.5.6"]
  patterns:
    - optimizeDeps.include to pre-bundle CJS-only npm packages for Vite ESM build
    - vi.mock('qrcode') pattern for mocking CJS default exports in vitest

key-files:
  created:
    - client/src/components/copy-button.test.ts
    - client/src/components/expiration-select.test.ts
    - client/src/components/qr-code-panel.test.ts
    - client/src/components/mailto-button.test.ts
  modified:
    - package.json
    - package-lock.json
    - vite.config.ts

key-decisions:
  - "optimizeDeps.include: ['qrcode'] prevents require-is-not-defined runtime error when qrcode (CJS-only) is imported in browser ESM context"
  - "eslint-disable for no-base-to-string on QRCode.toString() — ESLint misidentifies qrcode API method as Object.prototype.toString; disable comment is correct and targeted"
  - "qr-code-panel.ts and mailto-button.ts were pre-implemented in WIP — qr-code-panel tests pass; mailto-button tests pass; copy-button and expiration-select tests remain RED as expected"

patterns-established:
  - "CJS pre-bundling pattern: add package to optimizeDeps.include in vite.config.ts to prevent require-is-not-defined in browser"
  - "Vitest mock for default CJS exports: vi.mock('pkg', () => ({ default: { method: vi.fn() } }))"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-03-08
---

# Phase 58.2 Plan 01: qrcode Install + Test Scaffolding Summary

**qrcode@1.5.4 installed with Vite optimizeDeps CJS pre-bundling guard, 4 component test stubs scaffolded for Wave 1 TDD implementation**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-08T01:47:54Z
- **Completed:** 2026-03-08T01:51:40Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- Installed qrcode@1.5.4 and @types/qrcode@1.5.6 as prod/dev dependencies
- Added `optimizeDeps: { include: ['qrcode'] }` to vite.config.ts — prevents CJS `require is not defined` runtime error in browser ESM context
- Scaffolded 4 test stub files: copy-button (5 tests, RED), expiration-select (4 tests, 2 RED), qr-code-panel (4 tests, GREEN — pre-implemented), mailto-button (5 tests, GREEN — pre-implemented)
- Build verified clean: `npm run build:client` exits 0 with no CJS/require errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Install qrcode + configure optimizeDeps** - `be90c33` (chore)
2. **Task 2: Scaffold 4 failing test stubs** - previously committed in `e7b40ef` (test) and `f6d249e` (feat)

## Files Created/Modified

- `package.json` — qrcode + @types/qrcode dependencies added
- `package-lock.json` — lockfile updated
- `vite.config.ts` — optimizeDeps.include: ['qrcode'] added
- `client/src/components/copy-button.test.ts` — 5 RED stubs for autoClearMs countdown
- `client/src/components/expiration-select.test.ts` — 4 stubs (2 RED) for suggestion hint
- `client/src/components/qr-code-panel.test.ts` — 4 passing stubs for toggle show/hide
- `client/src/components/mailto-button.test.ts` — 5 passing stubs for mailto href construction

## Decisions Made

- `optimizeDeps.include: ['qrcode']` is the canonical fix for CJS packages in Vite ESM builds — no commonjsOptions needed; esbuild pre-bundling alone suffices
- ESLint `no-base-to-string` disable comment added to qr-code-panel.ts line 22 — rule fires incorrectly on `QRCode.toString()` (qrcode API method, not Object.prototype.toString)

## Deviations from Plan

### Pre-existing WIP Work

**1. [Context] qr-code-panel.ts and mailto-button.ts were already implemented as WIP**
- **Found during:** Task 2 (scaffolding test files)
- **Issue:** Plan expected all 4 test stubs to be RED (failing), but `qr-code-panel.ts` was pre-committed in `f6d249e` and `mailto-button.ts` existed as untracked WIP. Tests for these components pass immediately.
- **Fix:** No fix needed — pre-implementation is desirable; test files were still created as specified
- **Impact:** 2 of 4 test files are GREEN instead of RED; copy-button and expiration-select remain correctly RED for Wave 1
- **Assessment:** Wave 0 gate purpose achieved — test infrastructure is in place for all 4 components

**2. [Rule 1 - Bug] ESLint no-base-to-string on qr-code-panel.ts**
- **Found during:** Task 2 commit (lint-staged pre-commit hook)
- **Issue:** `@typescript-eslint/no-base-to-string` fired on `QRCode.toString()` in the pre-existing qr-code-panel.ts — ESLint sees the QRCode namespace object and thinks `.toString()` calls Object.prototype.toString
- **Fix:** lint-staged auto-added eslint-disable comment; verified by reading updated file
- **Files modified:** `client/src/components/qr-code-panel.ts`
- **Committed in:** Already in `f6d249e` after lint-staged fixed it

---

**Total deviations:** 2 documented (1 WIP pre-work context, 1 auto-fixed lint issue)
**Impact on plan:** All plan artifacts exist and are correct. Wave 0 gate is fully satisfied.

## Issues Encountered

- lint-staged pre-commit hook failed on first commit attempt for Task 2 — `no-base-to-string` error in pre-staged `qr-code-panel.ts`. lint-staged applied the eslint-disable fix automatically and reverted the stash, then the files were already committed via the prior work in `f6d249e`.

## Next Phase Readiness

- Wave 0 gate complete: qrcode installed, Vite configured, 4 test stubs exist
- Wave 1 plans (58.2-02 through 58.2-05) can proceed
- copy-button.ts and expiration-select.ts need implementation updates (autoClearMs param and suggestion param respectively) — tests are RED waiting for Wave 1
- qr-code-panel.ts and mailto-button.ts are already implemented — Wave 1 plans for these can verify and integrate

---
*Phase: 58.2-launch-quick-wins-do-now*
*Completed: 2026-03-08*
