---
phase: 05-password-protection
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - server/src/routes/__tests__/secrets.test.ts
autonomous: true

must_haves:
  truths:
    - "Password-protected secret creation stores a hash and returns 201"
    - "GET /:id returns 404 for password-protected secrets (cannot bypass)"
    - "GET /:id/meta returns requiresPassword true and 3 attempts remaining for password-protected secrets"
    - "GET /:id/meta returns requiresPassword false for non-password secrets"
    - "POST /:id/verify with correct password returns ciphertext and destroys the secret"
    - "POST /:id/verify with wrong password returns 403 with decremented attemptsRemaining"
    - "3 wrong passwords auto-destroy the secret and subsequent requests return 404"
    - "GET /:id/meta for nonexistent secret returns identical 404"
    - "Anti-enumeration: destroyed secret response is identical to nonexistent secret response"
  artifacts:
    - path: "server/src/routes/__tests__/secrets.test.ts"
      provides: "Integration tests proving all Phase 5 success criteria"
      min_lines: 100
  key_links:
    - from: "server/src/routes/__tests__/secrets.test.ts"
      to: "server/src/routes/secrets.ts"
      via: "supertest requests to /api/secrets endpoints"
      pattern: "request\\(app\\)"
---

<objective>
Add integration tests proving all four Phase 5 success criteria against real PostgreSQL: password entry requirement, Argon2id verification, 3-attempt auto-destroy, and attempt counter display data.

Purpose: Verify the password protection system works correctly end-to-end, including edge cases like bypass attempts and anti-enumeration compliance.

Output: Extended integration test file covering all password protection behaviors.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-password-protection/05-RESEARCH.md
@.planning/phases/05-password-protection/05-01-SUMMARY.md

@server/src/routes/__tests__/secrets.test.ts
@server/src/routes/secrets.ts
@server/src/services/secrets.service.ts
@shared/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests for password-protected secret creation and metadata</name>
  <files>server/src/routes/__tests__/secrets.test.ts</files>
  <action>
    Add new describe blocks AFTER the existing test blocks in secrets.test.ts. Follow the existing test pattern: fresh `app = buildApp()` per test via beforeEach, supertest requests, real database assertions.

    **describe('POST /api/secrets with password'):**

    - Test: "creates password-protected secret and returns 201 with same shape as non-password secret"
      - POST with `{ ciphertext, expiresIn: '24h', password: 'test-password-123' }`
      - Assert 201, response has `id` (21 chars) and `expiresAt`

    - Test: "stores password hash in database, not plaintext"
      - Create password-protected secret
      - Direct DB query: `db.select().from(secrets).where(eq(secrets.id, id))`
      - Assert `passwordHash` is not null, does NOT equal the plaintext password, starts with `$argon2id$` (Argon2id hash format)

    - Test: "rejects password over 128 characters with 400"
      - POST with password of 129 characters
      - Assert 400, validation_error

    **describe('GET /api/secrets/:id/meta'):**

    - Test: "returns requiresPassword true for password-protected secret"
      - Create with password, GET meta
      - Assert 200, `{ requiresPassword: true, passwordAttemptsRemaining: 3 }`

    - Test: "returns requiresPassword false for non-password secret"
      - Create without password, GET meta
      - Assert 200, `{ requiresPassword: false, passwordAttemptsRemaining: 3 }`

    - Test: "returns 404 for nonexistent secret"
      - GET meta with fake 21-char ID
      - Assert 404, body matches SECRET_NOT_AVAILABLE pattern

    - Test: "does not consume the secret (can be called multiple times)"
      - Create secret, call GET meta twice, then GET /:id (for non-password) should still work
      - Assert all three calls succeed

    **describe('GET /api/secrets/:id bypass prevention'):**

    - Test: "returns 404 for password-protected secret (cannot bypass password)"
      - Create with password, GET /:id directly
      - Assert 404

    - Test: "404 response for password-protected secret is identical to nonexistent secret"
      - Compare response body from password-bypass 404 with nonexistent-ID 404
      - Assert bodies are deep equal (anti-enumeration)
  </action>
  <verify>
    Run `npm run test:run` and verify all new and existing tests pass. The test output should show the new describe blocks passing.
  </verify>
  <done>
    Tests verify: password-protected creation stores Argon2id hash (not plaintext), meta endpoint returns correct requiresPassword and attemptsRemaining, meta endpoint is non-destructive, GET /:id rejects password-protected secrets with identical 404.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for password verification, attempt counting, and auto-destroy</name>
  <files>server/src/routes/__tests__/secrets.test.ts</files>
  <action>
    Continue adding describe blocks to secrets.test.ts.

    **describe('POST /api/secrets/:id/verify'):**

    - Test: "returns ciphertext on correct password with 200"
      - Create with password, POST verify with correct password
      - Assert 200, response has `ciphertext` matching original, `expiresAt` is valid ISO string

    - Test: "destroys secret after successful verification (one-time use)"
      - Create with password, POST verify (correct), POST verify again
      - Assert second verify returns 404

    - Test: "returns 403 with attemptsRemaining on wrong password"
      - Create with password, POST verify with wrong password
      - Assert 403, body `{ error: 'wrong_password', attemptsRemaining: 2 }`

    - Test: "decrements attemptsRemaining on each wrong attempt"
      - Create with password
      - POST verify wrong: assert attemptsRemaining === 2
      - POST verify wrong: assert attemptsRemaining === 1
      - GET meta: assert passwordAttemptsRemaining === 1

    - Test: "auto-destroys secret after 3 wrong attempts"
      - Create with password
      - POST verify wrong x3
      - Third wrong attempt should return either 403 with attemptsRemaining 0 OR 404 (depending on implementation -- accept either since the secret is destroyed)
      - GET meta: assert 404 (secret is gone)
      - Direct DB: assert row no longer exists

    - Test: "correct password still works after 2 wrong attempts"
      - Create with password
      - POST verify wrong x2 (assert attemptsRemaining 2, then 1)
      - POST verify correct: assert 200 with ciphertext

    - Test: "returns 404 for nonexistent secret"
      - POST verify to fake 21-char ID
      - Assert 404

    - Test: "returns 400 for missing password in body"
      - POST verify with empty body
      - Assert 400, validation_error

    **describe('anti-enumeration (password)'):**

    - Test: "destroyed secret response is identical to nonexistent secret response"
      - Create with password, destroy via 3 wrong attempts
      - POST verify to destroyed ID: assert 404
      - POST verify to fake ID: assert 404
      - Assert both 404 bodies are deep equal
      - Also: GET meta to destroyed ID returns same 404 as GET meta to fake ID
  </action>
  <verify>
    Run `npm run test:run` and verify ALL tests pass (both existing and new). Expected: 0 failures. The full test suite should cover creation, metadata, bypass prevention, verification, attempt counting, auto-destroy, and anti-enumeration.
  </verify>
  <done>
    Complete integration test coverage for Phase 5: correct password returns ciphertext and destroys secret, wrong password returns 403 with decremented attempts, 3 wrong attempts auto-destroy the secret, anti-enumeration compliance verified for destroyed secrets. All Phase 2/3 tests continue passing.
  </done>
</task>

</tasks>

<verification>
- `npm run test:run` passes with 0 failures
- All original Phase 2/3 tests unaffected
- New tests cover all 4 Phase 5 success criteria:
  1. Password entry form data (requiresPassword from meta endpoint)
  2. Server-side Argon2id verification (hash format in DB)
  3. Auto-destroy after 3 wrong attempts (row gone from DB)
  4. Attempt counter data (attemptsRemaining in verify and meta responses)
</verification>

<success_criteria>
- All new tests pass against real PostgreSQL
- All existing tests continue passing (no regressions)
- Password hash stored in Argon2id format (verified via DB query)
- Correct password returns ciphertext exactly once
- Wrong password returns 403 with attemptsRemaining
- 3 wrong attempts destroy the secret (verified via DB query)
- Anti-enumeration: destroyed and nonexistent secrets produce identical responses
</success_criteria>

<output>
After completion, create `.planning/phases/05-password-protection/05-03-SUMMARY.md`
</output>
