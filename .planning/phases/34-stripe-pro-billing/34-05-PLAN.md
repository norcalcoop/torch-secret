---
phase: 34-stripe-pro-billing
plan: 05
type: execute
wave: 4
depends_on:
  - 34-04
files_modified: []
autonomous: false
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - BILL-04
  - BILL-05
  - BILL-06

must_haves:
  truths:
    - "Authenticated user clicking Upgrade to Pro on dashboard is redirected to Stripe Checkout and completes payment"
    - "After checkout completion, /dashboard?upgraded=true shows spinner then success banner confirming Pro status"
    - "Pro user sees 30-day option enabled in expiration dropdown when creating secrets"
    - "Free user sees 30-day option as disabled in expiration dropdown"
    - "Pro user can click Manage Subscription and be redirected to Stripe Customer Portal"
    - "After simulating customer.subscription.deleted webhook, Pro access is removed and 30-day option disappears"
    - "INVARIANTS.md has a Stripe billing row in the enforcement table"
  artifacts:
    - path: ".planning/INVARIANTS.md"
      provides: "Stripe billing enforcement row"
      contains: "Stripe billing"
    - path: "server/src/routes/webhooks.ts"
      provides: "Stripe webhook handler"
      contains: "stripeWebhookHandler"
    - path: "server/src/routes/billing.ts"
      provides: "Billing routes"
      contains: "billingRouter"
    - path: "client/src/pages/dashboard.ts"
      provides: "Pro badge, upgrade CTA, post-checkout flows"
      contains: "subscriptionTier"
  key_links:
    - from: "client/src/pages/dashboard.ts"
      to: "server/src/routes/billing.ts"
      via: "GET /api/billing/checkout returns Stripe Checkout URL"
      pattern: "initiateCheckout"
    - from: "server/src/routes/webhooks.ts"
      to: "server/src/services/billing.service.ts"
      via: "activatePro/deactivatePro on webhook events"
      pattern: "activatePro|deactivatePro"
---

<objective>
Human UAT checkpoint — verify all 6 BILL requirements against a running instance with Stripe test mode configured.

Purpose: Confirm the full subscription lifecycle works end-to-end: checkout → Pro activation → 30-day unlock → portal → cancellation → downgrade. This is the final verification gate before Phase 34 is marked complete.

Output: UAT approval or list of issues to address in gap closure plans.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT: Verify all BILL requirements end-to-end with Stripe test mode</name>
  <action>Human verification of the complete Stripe Pro Billing integration against a running app instance.</action>
  <verify>See how-to-verify steps below — requires human interaction with Stripe test mode.</verify>
  <done>All 7 UAT tests pass: checkout flow, direct verification, Pro expiration unlock, Customer Portal, webhook cancellation, cancellation toast, INVARIANTS.md updated.</done>
  <what-built>
    Full Stripe Pro Billing integration:
    - Plan 01: INVARIANTS.md updated, DB migration applied, billing service created
    - Plan 02: Billing routes (checkout, verify-checkout, portal), webhook handler, me.ts with subscriptionTier
    - Plan 03: expiration-select Pro-aware, create.ts passes isPro, billing API functions in client
    - Plan 04: Dashboard Pro badge, upgrade CTA, Manage Subscription, post-checkout banner/toast
  </what-built>
  <how-to-verify>
    **Prerequisites before testing:**
    1. Set STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, and STRIPE_PRO_PRICE_ID in .env (see Plan 01 user_setup)
    2. Configure Stripe Customer Portal in Stripe Dashboard (Settings -> Billing -> Customer portal)
    3. Run `stripe listen --forward-to localhost:3000/api/webhooks/stripe` in a terminal (keep running)
    4. Start the app: `npm run dev:server` and `npm run dev:client`

    **Test 1 — BILL-06: INVARIANTS.md updated (pre-code requirement):**
    - Run: `grep "Stripe billing" .planning/INVARIANTS.md`
    - Expected: A row appears in the enforcement table

    **Test 2 — BILL-01: Checkout flow:**
    - Log in as an authenticated free user
    - Navigate to /dashboard
    - Confirm the "Upgrade to Pro" button is visible
    - Click it — should be redirected to Stripe Checkout
    - Complete payment with Stripe test card: 4242 4242 4242 4242, any future expiry, any CVC
    - Expected: Redirected to /dashboard?upgraded=true, spinner appears, then banner: "You're now Pro — 30-day secrets unlocked"

    **Test 3 — BILL-05: Direct Stripe verification (not webhook-dependent):**
    - Immediately after the checkout redirect in Test 2
    - Confirm the banner appeared BEFORE any webhook fires (the direct API verification should be fast)
    - Expected: banner shows within ~2s of landing on dashboard

    **Test 4 — BILL-03: Pro expiration unlock:**
    - As the now-Pro user, navigate to /create
    - Confirm the expiration dropdown shows "30 days" as an enabled selectable option
    - Log out and create a new incognito/private browser session
    - Without logging in: confirm the expiration display shows "1 hour" (locked)
    - Log in as a different free user: confirm "30 days — Pro" is shown as disabled

    **Test 5 — BILL-04: Customer Portal:**
    - As the Pro user on /dashboard
    - Confirm "Manage Subscription" link is visible (not the upgrade CTA)
    - Click it — should open a new tab to the Stripe Customer Portal
    - Expected: Portal shows subscription details and cancel option

    **Test 6 — BILL-02: Webhook cancellation (subscription.deleted):**
    - In the Stripe CLI terminal, send a test event:
      `stripe trigger customer.subscription.deleted`
    - Wait ~5s, then refresh /dashboard
    - Expected: Pro badge is gone, "Upgrade to Pro" button reappears, "Manage Subscription" link is gone
    - Navigate to /create — confirm "30 days — Pro" is now disabled again

    **Test 7 — Checkout cancellation toast:**
    - Click "Upgrade to Pro" on dashboard
    - On the Stripe Checkout page, click the back button or "Cancel"
    - Expected: Returned to /dashboard and a toast appears: "Checkout cancelled — you can upgrade anytime"
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 tests pass. Describe any failures to create gap closure plans.</resume-signal>
</task>

</tasks>

<verification>
All 6 BILL requirements verified via human UAT:
- BILL-01: Checkout flow works end-to-end
- BILL-02: Webhook lifecycle updates subscription status
- BILL-03: 30-day expiration gated by Pro tier
- BILL-04: Customer Portal accessible for Pro users
- BILL-05: Direct Stripe session verification (not webhook-dependent)
- BILL-06: INVARIANTS.md updated before webhook code written
</verification>

<success_criteria>
- All 7 UAT tests pass
- No TypeScript errors in server or client: npx tsc --noEmit (both tsconfigs)
- npm run test:run passes (no regressions in existing tests)
</success_criteria>

<output>
After completion, create `.planning/phases/34-stripe-pro-billing/34-05-SUMMARY.md`
</output>
