---
phase: 07-trust-and-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - client/src/pages/create.ts
  - client/src/__tests__/accessibility.test.ts
  - vitest.config.ts
autonomous: false

must_haves:
  truths:
    - "The landing page includes a 'How it works' section explaining zero-knowledge encryption in plain language"
    - "The 3-step explanation covers: browser encryption, encrypted storage, one-time destruction"
    - "Automated accessibility tests catch structural ARIA violations on all pages"
    - "Color contrast meets WCAG 2.1 AA minimum ratios (verified by human)"
  artifacts:
    - path: "client/src/pages/create.ts"
      provides: "How It Works trust section below the create form"
      contains: "How It Works"
    - path: "client/src/__tests__/accessibility.test.ts"
      provides: "vitest-axe automated a11y tests for all pages"
      contains: "toHaveNoViolations"
  key_links:
    - from: "client/src/pages/create.ts"
      to: "create page form"
      via: "section appended after form in page wrapper"
      pattern: "aria-labelledby.*how-it-works"
    - from: "client/src/__tests__/accessibility.test.ts"
      to: "all page modules"
      via: "dynamic import and render into DOM"
      pattern: "import.*pages/"
---

<objective>
Add the "How it works" trust content section to the create page and validate all accessibility work with automated vitest-axe tests and a manual color contrast checkpoint.

Purpose: Users need to understand and trust the zero-knowledge model before sharing sensitive data. The 3-step explanation builds confidence. Automated tests prevent accessibility regressions. Manual contrast verification catches issues that JSDOM-based tools cannot detect.

Output: Create page with trust section, accessibility test suite, and human-verified color contrast.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-trust-and-accessibility/07-RESEARCH.md
@.planning/phases/07-trust-and-accessibility/07-01-SUMMARY.md
@client/src/pages/create.ts
@client/src/styles.css
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: "How It Works" trust section on create page</name>
  <files>client/src/pages/create.ts</files>
  <action>
  Add a `createHowItWorksSection()` function to create.ts that builds and returns an HTMLElement containing the trust content. Call it at the end of `renderCreatePage()`, appending the result to `wrapper` AFTER `form` is appended.

  The section structure:

  1. A `<section>` element with `aria-labelledby="how-it-works-heading"` and className `mt-12 pt-8 border-t border-gray-200`

  2. An `<h2>` with `id="how-it-works-heading"`, className `text-xl sm:text-2xl font-bold text-gray-900 text-center mb-8`, text "How It Works"

  3. A container `<div>` with className `grid grid-cols-1 sm:grid-cols-3 gap-6`

  4. Three step cards, each a `<div>` with className `text-center space-y-2`:

     **Step 1:**
     - Number circle: `<div>` with className `w-10 h-10 rounded-full bg-primary-100 text-primary-700 font-bold flex items-center justify-center mx-auto text-lg`, textContent "1", set `aria-hidden="true"` (decorative, the heading conveys the step)
     - Title: `<h3>` with className `font-semibold text-gray-900`, text "Encrypted in Your Browser"
     - Description: `<p>` with className `text-sm text-gray-600 leading-relaxed`, text "Your secret is encrypted on your device before anything is sent. The encryption key stays in your browser and never reaches our server."

     **Step 2:**
     - Number circle: same pattern, textContent "2"
     - Title: "Stored Encrypted"
     - Description: "Our server only sees scrambled data it cannot read. Even a complete database breach would reveal nothing."

     **Step 3:**
     - Number circle: same pattern, textContent "3"
     - Title: "View Once, Then Destroyed"
     - Description: "The recipient decrypts the secret in their browser using the key in the link. After viewing, the encrypted data is permanently deleted."

  **Important:** Use `text-gray-600` for description text (not `text-gray-500`) to ensure better contrast. The research flagged gray-500 as borderline for contrast.

  In `renderCreatePage()`, after `wrapper.appendChild(form)` and before `container.appendChild(wrapper)`, add:
  ```typescript
  wrapper.appendChild(createHowItWorksSection());
  ```
  </action>
  <verify>
  Run `npx tsc --noEmit` to verify TypeScript compiles. Run `npm run dev:server` briefly and visit `http://localhost:3000/` in a browser to visually confirm the "How It Works" section appears below the form with three columns on desktop.
  </verify>
  <done>
  The create page (/) displays a "How It Works" section below the form with three steps: "Encrypted in Your Browser", "Stored Encrypted", and "View Once, Then Destroyed". Each step has a numbered circle, heading, and plain-language description. The section is properly labeled with aria-labelledby for screen readers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install vitest-axe and create accessibility test suite</name>
  <files>client/src/__tests__/accessibility.test.ts, vitest.config.ts</files>
  <action>
  **Install vitest-axe:**
  ```bash
  npm install -D vitest-axe
  ```

  **Create `client/src/__tests__/accessibility.test.ts`:**

  This test file validates that rendered pages have no structural accessibility violations using axe-core.

  ```typescript
  import { describe, it, expect, beforeEach, afterEach } from 'vitest';
  import { axe } from 'vitest-axe';
  import 'vitest-axe/extend-expect';
  ```

  Before each test:
  - Create a `<div id="app">` container
  - Append it to `document.body`

  After each test:
  - Remove the container from `document.body`

  **Test cases:**

  1. **"Create page has no accessibility violations"**
     - Import `renderCreatePage` from `'../pages/create.js'`
     - Call `await renderCreatePage(container)`
     - Run `const results = await axe(container)`
     - Assert `expect(results).toHaveNoViolations()`

  2. **"Error page has no accessibility violations"**
     - Import `renderErrorPage` from `'../pages/error.js'`
     - Call `renderErrorPage(container, 'not_available')`
     - Run axe, assert no violations

  3. **"Create page 'How It Works' section has proper heading hierarchy"**
     - Render create page
     - Assert `container.querySelector('h1')` exists (page heading)
     - Assert `container.querySelector('h2#how-it-works-heading')` exists
     - Assert `container.querySelectorAll('h3').length` is 3 (three step headings)

  4. **"Create page 'How It Works' section is labeled by its heading"**
     - Render create page
     - Assert `container.querySelector('section[aria-labelledby="how-it-works-heading"]')` exists

  5. **"Loading spinner has role=status"**
     - Import `createLoadingSpinner` from `'../components/loading-spinner.js'`
     - Call `createLoadingSpinner('Testing...')`
     - Assert the wrapper has `getAttribute('role')` === `'status'`
     - Assert the wrapper has `getAttribute('aria-live')` === `'polite'`

  6. **"Copy button announces state changes"**
     - Import `createCopyButton` from `'../components/copy-button.js'`
     - Create a button: `createCopyButton(() => 'test')`
     - Append to container (so axe can inspect it)
     - Run axe, assert no violations

  **Note on axe configuration:** Disable the `color-contrast` rule in all axe calls since happy-dom cannot compute styles:
  ```typescript
  const results = await axe(container, {
    rules: { 'color-contrast': { enabled: false } }
  });
  ```

  **vitest.config.ts:** No changes needed. The test file at `client/src/__tests__/accessibility.test.ts` already matches the `client/src/**/*.test.ts` include pattern and will use the `happy-dom` environment via `environmentMatchGlobs`.
  </action>
  <verify>
  Run `npx vitest run client/src/__tests__/accessibility.test.ts` -- all tests pass. Run `npm run test:run` -- all tests pass (existing 146 + new accessibility tests).
  </verify>
  <done>
  vitest-axe is installed. Accessibility tests verify create page, error page, "How It Works" section structure, loading spinner ARIA attributes, and copy button accessibility. All tests pass with color-contrast rule disabled (manual verification in next task).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Color contrast and accessibility verification</name>
  <what-built>
  Complete WCAG 2.1 AA accessibility infrastructure across the entire SecureShare client:
  - Skip link (visible on keyboard focus)
  - Screen reader route announcements
  - Document title updates on navigation
  - Focus management (h1 focused on page change)
  - Loading spinner with role="status"
  - Copy button with aria-live feedback
  - Emoji icons hidden from screen readers
  - Password autocomplete attributes
  - focus:outline-hidden for forced-colors mode
  - "How It Works" trust section with 3-step explanation
  - Automated accessibility test suite
  </what-built>
  <how-to-verify>
  **Start the app:**
  ```bash
  npm run dev:server
  ```
  Visit http://localhost:3000/

  **1. Visual check -- "How It Works" section:**
  - Scroll below the form on the create page
  - Confirm 3 steps are displayed: "Encrypted in Your Browser", "Stored Encrypted", "View Once, Then Destroyed"
  - On mobile viewport (resize to ~375px wide), confirm steps stack vertically
  - On desktop, confirm steps display in a 3-column grid

  **2. Skip link check:**
  - Press Tab immediately after page loads
  - A "Skip to main content" link should appear at the top-left
  - Press Enter on it -- focus should jump into the main content area
  - Press Tab again -- the link should disappear

  **3. Color contrast check (requires manual tool):**
  Open browser DevTools or visit https://www.oddcontrast.com/ and check these pairs:
  - White text (#fff) on primary-600 button background -- must be >= 4.5:1
  - gray-600 text on white/gray-50 background (How It Works descriptions) -- must be >= 4.5:1
  - gray-500 text on white/gray-50 background (subtext) -- must be >= 4.5:1
  - danger-500 text on its light background -- must be >= 4.5:1
  - warning-500 text (if visible in password flow) -- must be >= 3:1 (large text) or 4.5:1 (normal text)
  - gray-400 character counter text on white -- if < 4.5:1, this is acceptable (counter is supplementary)

  **4. Keyboard navigation:**
  - Tab through the entire create page -- every interactive element should be reachable
  - Focus ring should be visible on each focused element
  - Submit the form (if database is running) and confirm the confirmation page heading receives focus

  **5. Document title:**
  - Check the browser tab shows "Share a Secret - SecureShare" on the create page
  - Navigate to a non-existent route -- tab should show "Page Not Found - SecureShare"
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any contrast failures or accessibility issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run test:run` passes all tests (existing + new accessibility tests)
2. "How It Works" section visible on create page with 3-step content
3. Color contrast verified manually by human using browser tools
4. Skip link works via keyboard
5. Document title updates on route changes
</verification>

<success_criteria>
- Create page displays "How It Works" section with 3 plain-language steps explaining zero-knowledge model
- vitest-axe tests pass for create page and error page
- Human confirms color contrast meets WCAG 2.1 AA ratios
- All existing tests continue to pass
- All Phase 7 success criteria from ROADMAP.md are met:
  1. Landing page includes "How it works" section
  2. All interactive elements keyboard-navigable and screen-reader accessible
  3. Color contrast meets WCAG 2.1 AA minimum ratios
</success_criteria>

<output>
After completion, create `.planning/phases/07-trust-and-accessibility/07-02-SUMMARY.md`
</output>
