---
phase: 49-gmail-send-mail-as
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified: []
autonomous: false
requirements: [GMAI-03, GMAI-04]
must_haves:
  truths:
    - "All 7 torchsecret.com aliases show 'verified' status in Gmail Settings (no 'Unverified' warning on any entry)"
    - "A test email sent from hello@torchsecret.com via Gmail shows 'Signed by: torchsecret.com' and no 'via gappssmtp.com' indicator"
    - "hello@torchsecret.com is the default outbound address — new Gmail compose window shows it in the From field"
    - "'Reply from the same address the message was sent to' is enabled in Gmail Settings"
  artifacts:
    - path: "Gmail Settings — Accounts and Import — Send mail as"
      provides: "7 verified aliases + hello@ set as default"
    - path: "Gmail 'Show original' for test email"
      provides: "DKIM-Signature header with d=torchsecret.com and Authentication-Results dkim=pass header.i=@torchsecret.com"
  key_links:
    - from: "Cloudflare Email Routing (Phase 46)"
      to: "torch.secrets@gmail.com"
      via: "7 verification emails forwarded from @torchsecret.com aliases"
      pattern: "Phase 46 routing rules active — all 7 aliases forward to torch.secrets@gmail.com"
    - from: "Resend DKIM key (Phase 47)"
      to: "DKIM-Signature header in test email"
      via: "smtp.resend.com signing with resend._domainkey.torchsecret.com"
      pattern: "DKIM-Signature: d=torchsecret.com; s=resend"
---

<objective>
Verify all 7 Gmail Send mail as aliases, set hello@torchsecret.com as the default outbound address, and confirm DKIM alignment via Gmail "Show original" on a test email.

Purpose: Completes GMAI-03 (all aliases verified, DKIM spot-check passes) and GMAI-04 (hello@ set as default with context-appropriate reply behavior).
Output: All 7 aliases verified; hello@ default; DKIM header confirms "Signed by: torchsecret.com" with no "via gappssmtp.com".
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-gmail-send-mail-as/49-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Verify all 7 Gmail Send mail as aliases in batch (GMAI-03)</name>
  <action>
    Open torch.secrets@gmail.com inbox. Check Spam folder too — Gmail verification emails can occasionally land in Spam.

    Find the 7 verification emails titled "Gmail Confirmation - Send Mail as [alias]@torchsecret.com" (one per alias). They typically arrive within 30 seconds of alias creation. If any are missing after 5 minutes, use the "Resend verification email" option in Gmail Settings next to the pending alias.

    For each verification email, use the code entry method (faster than clicking 7 links):
    1. Open the verification email
    2. Copy the 9-digit confirmation code from the email body
    3. Go to Gmail Settings → Accounts and Import → Send mail as
    4. Click "Verify" next to the pending alias
    5. Enter the code in the popup → Click "Verify"
    6. The alias status changes from showing a "verify" link to showing no warning

    Repeat for all 7 aliases: hello, contact, admin, info, support, security, privacy.

    Troubleshooting:
    - Verification email missing after 5 minutes: In Gmail Settings, click "Resend verification email" next to the pending alias
    - Code expired: Request a new verification email using the "Resend" option
    - Alias shows "Send through Gmail" instead of "Send through smtp.resend.com": SMTP was not saved in Plan 01. Remove the alias, re-add it following Plan 01 Task 2 instructions exactly, then verify it.
  </action>
  <verify>Gmail Settings → Accounts and Import → Send mail as: all 7 @torchsecret.com aliases show no "Unverified" label or "verify" link — verified status confirmed for all 7.</verify>
  <done>All 7 aliases (hello, contact, admin, info, support, security, privacy) show verified status in Gmail Settings with no "Unverified" warnings (GMAI-03 alias verification complete)</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Set hello@ as default, enable reply-from-same-address, confirm DKIM alignment (GMAI-04 + GMAI-03 spot-check)</name>
  <action>
    **Part A — Set default outbound address (GMAI-04):**

    1. Go to Gmail Settings → Accounts and Import → Send mail as section
    2. Click "make default" next to hello@torchsecret.com
    3. Find the "When replying to a message" setting in the same Accounts and Import tab — select "Reply from the same address the message was sent to"
    4. Confirm: Compose a new email in Gmail — the From field should default to "Torch Secret <hello@torchsecret.com>", not torch.secrets@gmail.com

    ---

    **Part B — DKIM alignment spot-check (GMAI-03 header verification):**

    5. Send a test email from hello@torchsecret.com to an external Gmail account you control:
       - In Gmail, compose a new email
       - From field: confirm it shows "Torch Secret <hello@torchsecret.com>"
       - To: [your external Gmail address]
       - Subject: "Torch Secret alias test"
       - Body: "Testing DKIM alignment for hello@torchsecret.com via Resend SMTP"
       - Send

    6. Open the received email in the external Gmail account

    7. Click the three-dot menu (⋮) → "Show original" (opens raw headers in a new tab)

    8. Inspect the headers:
       - Must be present: `DKIM-Signature: v=1; a=rsa-sha256; d=torchsecret.com; s=resend`
       - Must be present: `Authentication-Results: ... dkim=pass header.i=@torchsecret.com`
       - In Gmail UI (sender tooltip): "Signed by: torchsecret.com" must be visible
       - Must NOT appear: "via gappssmtp.com" anywhere in Gmail UI or headers

    Run this DNS check while waiting for the email to confirm DKIM key is still live from Phase 47:
    `dig resend._domainkey.torchsecret.com TXT +short`
    Expected: TXT record with "v=DKIM1; p=..." public key value

    Troubleshooting:
    - "via gappssmtp.com" appears → The alias entry shows "Send through Gmail" — remove the alias, re-add it with smtp.resend.com credentials from Plan 01 Task 2
    - "dkim=fail" in headers → Run the dig command; if DNS is live, wait 5 minutes and resend the test
    - Default address still shows torch.secrets@gmail.com → Refresh Gmail Settings and re-confirm "make default" was clicked for hello@
  </action>
  <verify>
    `dig resend._domainkey.torchsecret.com TXT +short` returns TXT record with public key.
    Gmail compose window From field defaults to "Torch Secret &lt;hello@torchsecret.com&gt;".
    Gmail "Show original" on test email shows: DKIM-Signature d=torchsecret.com, dkim=pass header.i=@torchsecret.com. No "via gappssmtp.com" in Gmail UI.
  </verify>
  <done>hello@torchsecret.com is the default Gmail outbound address. "Reply from the same address" is enabled. DKIM spot-check passes: "Signed by: torchsecret.com" confirmed, no "via gappssmtp.com". All 4 GMAI requirements satisfied (GMAI-01 through GMAI-04).</done>
</task>

</tasks>

<verification>
1. `dig resend._domainkey.torchsecret.com TXT +short` returns a TXT record with p=... public key (DKIM DNS live from Phase 47)
2. Gmail Settings → Accounts and Import → Send mail as: all 7 aliases, no "Unverified" label or "verify" link
3. Gmail compose window: From field defaults to "Torch Secret &lt;hello@torchsecret.com&gt;"
4. Gmail "Show original" on test email from hello@torchsecret.com:
   - DKIM-Signature header: d=torchsecret.com; s=resend
   - Authentication-Results: dkim=pass header.i=@torchsecret.com
   - No "via gappssmtp.com" in Gmail UI sender tooltip
</verification>

<success_criteria>
- All 7 aliases verified in Gmail Settings — no "Unverified" warnings (GMAI-03)
- hello@torchsecret.com DKIM spot-check passes: "Signed by: torchsecret.com", no "via gappssmtp.com" (GMAI-03)
- hello@torchsecret.com is the default Gmail outbound address; new compose window shows it in From field (GMAI-04)
- "Reply from the same address the message was sent to" is enabled (GMAI-04)
</success_criteria>

<output>
After completion, create `.planning/phases/49-gmail-send-mail-as/49-02-SUMMARY.md` following the summary template.
</output>
