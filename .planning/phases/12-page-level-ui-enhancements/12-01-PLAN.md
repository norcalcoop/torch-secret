---
phase: 12-page-level-ui-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/styles.css
  - client/src/components/toast.ts
  - client/src/components/terminal-block.ts
  - client/src/components/share-button.ts
  - client/src/components/copy-button.ts
autonomous: true

must_haves:
  truths:
    - "Copy actions throughout the app show a toast notification instead of inline text swap"
    - "A reusable terminal-style code block component exists for displaying secrets"
    - "A share button component uses the Web Share API on supported devices and returns null otherwise"
    - "Terminal color tokens are defined as CSS custom properties mapped through Tailwind theme"
    - "Toast notification appears at bottom-center with slide-in animation and auto-dismiss"
  artifacts:
    - path: "client/src/components/toast.ts"
      provides: "showToast() utility with singleton container and aria-live"
      exports: ["showToast"]
    - path: "client/src/components/terminal-block.ts"
      provides: "createTerminalBlock() component with header bar and copy button"
      exports: ["createTerminalBlock"]
    - path: "client/src/components/share-button.ts"
      provides: "createShareButton() with Web Share API feature detection"
      exports: ["createShareButton"]
    - path: "client/src/components/copy-button.ts"
      provides: "createCopyButton() refactored to use showToast()"
      exports: ["createCopyButton"]
    - path: "client/src/styles.css"
      provides: "Terminal color tokens and toast animation keyframes"
      contains: "--ds-color-terminal-bg"
  key_links:
    - from: "client/src/components/copy-button.ts"
      to: "client/src/components/toast.ts"
      via: "import showToast"
      pattern: "import.*showToast.*toast"
    - from: "client/src/components/terminal-block.ts"
      to: "client/src/components/copy-button.ts"
      via: "import createCopyButton for header copy"
      pattern: "import.*createCopyButton.*copy-button"
    - from: "client/src/components/share-button.ts"
      to: "client/src/components/toast.ts"
      via: "import showToast for share failure feedback"
      pattern: "import.*showToast.*toast"
---

<objective>
Create shared UI components and CSS tokens that the page-level enhancements depend on: a toast notification system, a terminal-style code block, a Web Share API button, and refactor the copy button to use toast feedback.

Purpose: These shared components are the foundation for Plan 02 (create page) and Plan 03 (confirmation/reveal pages). Building them first avoids duplication and ensures consistent behavior.
Output: Four component files (toast.ts, terminal-block.ts, share-button.ts, copy-button.ts) and CSS token additions (styles.css).
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-page-level-ui-enhancements/12-RESEARCH.md
@client/src/styles.css
@client/src/components/copy-button.ts
@client/src/components/icons.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add terminal color tokens, toast keyframes, and create toast + terminal-block components</name>
  <files>
    client/src/styles.css
    client/src/components/toast.ts
    client/src/components/terminal-block.ts
  </files>
  <action>
**styles.css additions (append to existing file):**

1. Add terminal color tokens to `:root` block (after the dot-grid variable):
```css
/* Terminal code block (muted sage green, not bright phosphor) */
--ds-color-terminal-bg:     oklch(0.20 0.020 160);
--ds-color-terminal-text:   oklch(0.68 0.060 155);
--ds-color-terminal-header: oklch(0.25 0.025 160);
```

2. Add terminal tokens to `@theme inline` block (after --color-icon):
```css
/* Terminal code block tokens */
--color-terminal: var(--ds-color-terminal-text);
--color-terminal-bg: var(--ds-color-terminal-bg);
--color-terminal-header: var(--ds-color-terminal-header);
```

3. Add toast animation keyframes (after the .dot-grid-bg rule):
```css
@keyframes toast-in {
  from {
    opacity: 0;
    transform: translateY(8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

**client/src/components/toast.ts (NEW file):**

Create a singleton toast notification utility:
- `showToast(message: string, durationMs?: number): void` — exported function
- Lazy-creates a singleton container (`#toast-container`) appended to `document.body` on first call
- Container has: `aria-live="polite"`, `aria-atomic="true"`, fixed positioning at bottom-center (`fixed bottom-6 left-1/2 -translate-x-1/2 z-50`), `pointer-events-none`, flex-col layout
- Each toast element: `px-4 py-2 rounded-lg bg-surface-raised text-text-primary text-sm shadow-lg border border-border pointer-events-auto`, with `animate-[toast-in_200ms_ease-out]` animation
- Replace strategy: remove any existing toast child before adding new one (no stacking per research recommendation)
- Default duration 3000ms; after duration, add `opacity-0 transition-opacity duration-200` classes, then remove element on `transitionend`
- Toast text set via `textContent` (never innerHTML)

**client/src/components/terminal-block.ts (NEW file):**

Create a reusable terminal code block component:
- `createTerminalBlock(content: string, options?: { headerTitle?: string }): HTMLElement` — exported function
- Wrapper: `rounded-lg border border-border overflow-hidden`
- Header bar: `flex items-center justify-between px-3 py-2 bg-terminal-header border-b border-border`
  - Left: filename label (`text-xs text-text-muted font-mono`, default "secret.txt")
  - Right: compact copy button via `createCopyButton(() => content, 'Copy')` with overridden smaller styling (`px-2 py-1 min-h-0 text-xs rounded`)
- Content area: `<pre>` with `p-4 bg-terminal-bg text-terminal font-mono text-sm whitespace-pre-wrap break-words max-h-[300px] overflow-y-auto`
- Content set via `pre.textContent = content` (NEVER innerHTML — XSS prevention)
- Import `createCopyButton` from `./copy-button.js`
- Minimal flat header style (no macOS dots per discretion recommendation)
  </action>
  <verify>
1. `npx tsc --noEmit` passes with no type errors in the new files
2. Verify styles.css has the three new --ds-color-terminal-* variables in :root, three --color-terminal-* in @theme inline, and the @keyframes toast-in rule
3. Verify toast.ts exports `showToast` function
4. Verify terminal-block.ts exports `createTerminalBlock` function and imports createCopyButton
  </verify>
  <done>
Toast component creates a singleton accessible container and displays auto-dismissing notifications. Terminal block component renders a dark code display with muted sage-green text, header bar with copy button, and 300px max-height scroll. CSS tokens for terminal colors and toast animation are in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create share button component and refactor copy button to use toast</name>
  <files>
    client/src/components/share-button.ts
    client/src/components/copy-button.ts
  </files>
  <action>
**client/src/components/share-button.ts (NEW file):**

Create a Web Share API button with progressive enhancement:
- `createShareButton(url: string, title: string): HTMLButtonElement | null` — exported function
- Feature detection: `typeof navigator !== 'undefined' && typeof navigator.share === 'function'` — return `null` if unsupported (Firefox desktop, older browsers)
- Button styling: similar to copy button but secondary style — `inline-flex items-center gap-2 px-4 py-2 min-h-[44px] rounded-lg border border-border text-text-primary hover:bg-surface-raised focus:ring-2 focus:ring-accent focus:ring-offset-2 focus:ring-offset-bg focus:outline-hidden transition-colors cursor-pointer`
- Add a Share2 Lucide icon (16px) before label text "Share"
- Click handler: `await navigator.share({ title, url })`
- Catch handler: silence `AbortError` (user cancelled — this is normal behavior). For other errors, call `showToast('Sharing failed. Try copying the link instead.')`
- Import `showToast` from `./toast.js`, `Share2` from `lucide`, `createIcon` from `./icons.js`

**client/src/components/copy-button.ts (REFACTOR):**

Refactor the existing copy button to use toast notifications instead of inline text swap:
- Add `import { showToast } from './toast.js';` at the top
- Replace the `showSuccess()` function: instead of changing button text to "Copied!" and adding `text-success` class, call `showToast('Copied to clipboard')` (or 'Link copied to clipboard' — use the toast, keep the button label stable)
- Keep the button text as the defaultLabel at all times (no more text flipping)
- Keep the `fallbackCopy()` function as-is (the textarea approach still works)
- On failure, call `showToast('Failed to copy')` instead of inline button text change
- Remove the `aria-live` attribute manipulation from the button (the toast container handles a11y announcements)
- The `showSuccess()` helper function can be removed entirely
  </action>
  <verify>
1. `npx tsc --noEmit` passes with no type errors
2. Verify share-button.ts exports `createShareButton` and imports showToast, Share2, createIcon
3. Verify copy-button.ts imports showToast and no longer contains the `showSuccess` function
4. Verify copy-button.ts click handler calls `showToast` instead of modifying button.textContent
5. `npx vitest run client/src/__tests__/accessibility.test.ts` — copy button accessibility test still passes (button no longer changes text, which should not break axe checks)
  </verify>
  <done>
Share button returns null on unsupported browsers or a styled button that invokes native OS sharing on click. Copy button no longer flips its label text; instead shows a toast notification for copy success/failure. Both components use the shared toast system for user feedback.
  </done>
</task>

</tasks>

<verification>
1. All new files exist: toast.ts, terminal-block.ts, share-button.ts
2. copy-button.ts is refactored (no showSuccess function, imports showToast)
3. styles.css has terminal tokens and toast keyframes
4. `npx tsc --noEmit` passes
5. `npx vitest run` — existing tests pass (copy button a11y test, icons test)
</verification>

<success_criteria>
- Toast notification system works as a singleton with accessible announcements
- Terminal block renders dark bg with muted green-gray text, header bar, and scrollable content area
- Share button feature-detects Web Share API and gracefully returns null when unsupported
- Copy button uses toast instead of inline text swap across the entire app
- All CSS tokens use custom properties (ready for Phase 13 light theme override)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-level-ui-enhancements/12-01-SUMMARY.md`
</output>
