---
phase: 37.2-get-the-most-out-of-infiscal-free-tier-integration
plan: 03
type: execute
wave: 3
depends_on: ["37.2-02"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Local dev server starts without a .env file when infisical CLI is installed and authenticated"
    - "Local Vite client starts without a .env file when infisical CLI is installed and authenticated"
    - "Staging Docker Compose smoke-test confirms expected secret injection behavior"
    - "CI pipeline passes with Infisical secrets injection (no hardcoded placeholders required)"
    - "Render env vars show synced production secrets from Infisical"
  artifacts:
    - path: ".planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-03-SUMMARY.md"
      provides: "Phase completion record with all smoke-test results"
  key_links:
    - from: "local .infisical.json"
      to: "infisical dev environment"
      via: "infisical run --env=dev resolves secrets before child process starts"
      pattern: "npm run dev:server starts and logs Server running on port 3000"
    - from: "GitHub Actions CI"
      to: "Infisical dev environment"
      via: "secrets-action Universal Auth injection"
      pattern: "CI test job passes green"
    - from: "Infisical prod environment"
      to: "Render service env vars"
      via: "Secret Sync auto-sync"
      pattern: "Render dashboard shows env vars matching Infisical prod"
---

<objective>
Smoke-test every injection path wired in Plans 01 and 02: local dev without .env file, staging Docker Compose, CI pipeline, and Render production sync. No code changes — this plan is a verification checkpoint confirming all wiring works end-to-end.

Purpose: Confirm Infisical is the single source of truth for secrets before declaring the phase complete.
Output: All injection paths verified; phase declared complete.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-CONTEXT.md
@.planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Smoke-test local dev and staging injection without .env file</name>
  <files>none (verification only — no repo files modified)</files>
  <action>
STEP 1 — Verify local dev starts without .env file

If a .env file exists in the repo root, temporarily rename it:
  mv .env .env.bak

Start the dev server:
  npm run dev:server
  Expected: "Server running on port 3000" — no Zod validation errors about missing env vars

In a second terminal, start the Vite client:
  npm run dev:client
  Expected: portless/Vite starts and serves the app at http://torchsecret.localhost:1355

If both start cleanly, restore the backup:
  mv .env.bak .env

STEP 2 — Staging Docker Compose smoke-test

Run (does NOT start containers — resolves config only):
  infisical run --env=staging -- docker-compose config | grep DATABASE_URL

Expected: postgresql://secureshare:secureshare@db:5432/secureshare

Note: Per RESEARCH.md Pitfall 7, docker-compose.yml has literal DATABASE_URL values that take
precedence over host-level Infisical injection. The value shown should still be correct (db:5432)
because docker-compose.yml already hardcodes the Docker-internal hostname. Record what you see.

STEP 3 — Push branch and verify CI passes

If Plan 02 changes are not yet committed, commit them now. Then push:
  git push origin gsd/v1.0-product-launch-checklist

In GitHub: Actions tab > trigger workflow manually (workflow_dispatch) or observe auto-trigger.
Verify:
  - "Import secrets from Infisical" step passes in the test job
  - test job overall passes (all 347 tests green)
  - e2e job passes (LOOPS_API_KEY gap is now fixed via Infisical injection)

STEP 4 — Verify Render production sync

1. Infisical > Project > Integrations > Secret Syncs
   Confirm: Render sync shows "Synced" status with a recent timestamp
2. Render Dashboard > torch-secret service > Environment
   Verify: env vars match Infisical prod environment values
3. If env vars changed since last deploy, trigger a manual Render deploy to apply them
  </action>
  <verify>
Run before reporting results:
  cd /Users/ourcomputer/Github-Repos/secureshare && npx vitest run --reporter=dot
  (Expected: 347 tests passing — confirm no regressions from Plan 02 code changes)

Manual checks:
- Dev server started without .env (yes/no and any error messages seen)
- Staging docker-compose config DATABASE_URL output
- CI test job status (pass/fail + link to Actions run)
- Render Secret Syncs status
  </verify>
  <done>
- Local dev server and Vite client both started cleanly without a .env file present
- Staging docker-compose config shows DATABASE_URL with db:5432 hostname (Infisical or compose literal — both are correct)
- CI test and e2e jobs both passed; "Import secrets from Infisical" step is green
- Render Secret Sync shows "Synced" for prod environment
- All 347 tests pass locally
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Final verification — Infisical is the single source of truth</name>
  <files>none (verification only)</files>
  <action>
Run the following automated checks and review the outputs:

  cd /Users/ourcomputer/Github-Repos/secureshare

  # 1. .infisical.json is committed and bound to the project
  cat .infisical.json | grep workspaceId

  # 2. Scripts use infisical run prefix
  grep "infisical run" package.json

  # 3. staging:up script exists
  grep "staging:up" package.json

  # 4. .env.example is documentation-only (no values)
  grep -E '^[A-Z_]+=.' .env.example

  # 5. BETTER_AUTH_TRUSTED_ORIGINS documented
  grep "BETTER_AUTH_TRUSTED_ORIGINS" .env.example

  # 6. Hardcoded placeholder secrets removed from ci.yml test job
  grep "BETTER_AUTH_SECRET: ci-test-secret-placeholder" .github/workflows/ci.yml

  # 7. secrets-action present in ci.yml (should appear twice: test + e2e jobs)
  grep -c "secrets-action" .github/workflows/ci.yml

  # 8. All tests pass
  npx vitest run --reporter=dot
  </action>
  <verify>
Expected results for each check:
1. workspaceId line printed (non-empty UUID)
2. Lines with "infisical run --env=dev --" present
3. staging:up line present
4. No output (empty — .env.example has no values)
5. BETTER_AUTH_TRUSTED_ORIGINS= line printed
6. No output (BETTER_AUTH_SECRET placeholder no longer in ci.yml test job env)
7. Output: 2 (secrets-action appears in both test and e2e jobs)
8. 347 tests passing
  </verify>
  <done>
All 8 automated checks pass. Infisical is the single source of truth for all secrets across local dev, staging, CI, and production.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/ourcomputer/Github-Repos/secureshare

# Confirm .infisical.json exists
cat .infisical.json | grep workspaceId

# Confirm scripts are updated
grep "infisical run" package.json

# Confirm .env.example is clean
grep -E '^[A-Z_]+=.' .env.example

# Full test suite
npx vitest run --reporter=dot
```
</verification>

<success_criteria>
- Local dev server and Vite client start without a .env file (Infisical injects secrets)
- Staging docker-compose config smoke-test shows correct DATABASE_URL behavior documented
- CI pipeline passes with Infisical secrets-action replacing hardcoded placeholder vars
- Render Secret Sync shows "Synced" for prod environment
- All 347 tests pass
- Phase 37.2 declared complete
</success_criteria>

<output>
After completion, create `.planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-03-SUMMARY.md` with:
- Local dev smoke-test result (started without .env: yes/no)
- Staging docker-compose config output (which DATABASE_URL won: Infisical or compose literal)
- CI result (pass/fail; link to run)
- Render sync status
- Phase 37.2 completion declaration
</output>
