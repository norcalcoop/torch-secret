---
phase: 37.2-get-the-most-out-of-infiscal-free-tier-integration
plan: "02"
type: execute
wave: 2
depends_on: ["37.2-01"]
files_modified:
  - .infisical.json
  - package.json
  - .github/workflows/ci.yml
  - .env.example
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - ".infisical.json exists in repo root with the correct workspaceId and is safe to commit"
    - "npm run dev:server and npm run dev:client are prefixed with infisical run --env=dev --"
    - "npm run staging:up script exists and uses infisical run --env=staging -- docker-compose up"
    - "CI test and e2e jobs authenticate to Infisical via OIDC before running tests"
    - ".env.example contains only key names and comments — no values"
    - "README Getting Started section describes Infisical-based developer onboarding"
  artifacts:
    - path: ".infisical.json"
      provides: "Project workspace binding for infisical CLI"
      contains: "workspaceId"
    - path: "package.json"
      provides: "Updated npm scripts with infisical run prefix"
      contains: "infisical run --env=dev"
    - path: ".github/workflows/ci.yml"
      provides: "OIDC-authenticated CI pipeline"
      contains: "Infisical/secrets-action"
    - path: ".env.example"
      provides: "Key documentation with no values"
      contains: "DATABASE_URL="
  key_links:
    - from: ".infisical.json"
      to: "Infisical project"
      via: "workspaceId field"
      pattern: "workspaceId.*[a-f0-9-]{36}"
    - from: ".github/workflows/ci.yml"
      to: "Infisical OIDC identity"
      via: "vars.INFISICAL_IDENTITY_ID"
      pattern: "INFISICAL_IDENTITY_ID"
---

<objective>
Create all repository file changes required to wire Infisical into local dev scripts, CI pipeline, and staging Docker Compose — making Infisical the single source of truth for secrets with no application code changes.

Purpose: These code changes are the full integration surface. After this plan, `npm run dev:server` and CI tests both pull secrets from Infisical automatically.
Output: `.infisical.json`, updated `package.json`, updated `ci.yml`, cleaned `.env.example`, updated `README.md`.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-CONTEXT.md
@.planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-RESEARCH.md
@.planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-01-SUMMARY.md

<interfaces>
<!-- Values provided by user at end of Plan 01 checkpoint -->
<!-- The executor MUST read 37.2-01-SUMMARY.md for the actual workspaceId, identityId, and projectSlug values -->
<!-- Substitute them verbatim into the files below — do NOT use placeholder strings like "<infisical-project-id>" in the committed files -->

Secrets inventory (from .env.example — 24 keys total):
DATABASE_URL, PORT, LOG_LEVEL, NODE_ENV, FORCE_HTTPS, REDIS_URL,
BETTER_AUTH_SECRET, BETTER_AUTH_URL, APP_URL, BETTER_AUTH_TRUSTED_ORIGINS,
GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET,
RESEND_API_KEY, RESEND_FROM_EMAIL, VITE_POSTHOG_KEY, VITE_POSTHOG_HOST,
STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRO_PRICE_ID,
RESEND_AUDIENCE_ID, IP_HASH_SALT, LOOPS_API_KEY

Current package.json scripts (before migration):
  "dev:server": "tsx watch server/src/server.ts",
  "dev:client": "portless torchsecret sh -c 'npx vite --port $PORT --host $HOST'"

Current ci.yml jobs that need Infisical injection: test, e2e
Jobs that must NOT be changed: lint, docker-build

CI-specific overrides that must remain as job-level env (NOT from Infisical):
  test job:  DATABASE_URL=postgresql://secureshare:secureshare@localhost:5432/secureshare, NODE_ENV=test
  e2e job:   DATABASE_URL=postgresql://secureshare:secureshare@localhost:5432/secureshare, REDIS_URL=redis://localhost:6379, NODE_ENV=test
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .infisical.json and update package.json scripts</name>
  <files>.infisical.json, package.json</files>
  <action>
    Read 37.2-01-SUMMARY.md to extract the three values the user recorded: workspaceId, identityId, projectSlug.

    **1. Create .infisical.json** in the repo root:
    ```json
    {
      "workspaceId": "<INFISICAL_WORKSPACE_ID from 37.2-01-SUMMARY.md>",
      "defaultEnvironment": "dev"
    }
    ```
    Use the actual UUID. This file is safe to commit (contains no secrets). Do NOT add it to .gitignore.

    **2. Update package.json scripts** — add infisical run prefix to dev scripts and add staging:up:

    Change:
      "dev:server": "tsx watch server/src/server.ts"
    To:
      "dev:server": "infisical run --env=dev -- tsx watch server/src/server.ts"

    Change:
      "dev:client": "portless torchsecret sh -c 'npx vite --port $PORT --host $HOST'"
    To:
      "dev:client": "infisical run --env=dev -- portless torchsecret sh -c 'npx vite --port $PORT --host $HOST'"

    Add new script (after dev:client):
      "staging:up": "infisical run --env=staging -- docker-compose up"

    All other scripts remain unchanged. The `--` separator is mandatory between infisical flags and the wrapped command. The portless invocation is wrapped in its entirety.

    **Why not remove import 'dotenv/config':** Leave `import 'dotenv/config'` in server/src/config/env.ts unchanged. Infisical injects vars before Node starts, making dotenv a harmless no-op. No application code changes required.
  </action>
  <verify>
    node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(p.scripts['dev:server'], p.scripts['dev:client'], p.scripts['staging:up'])" 2>/dev/null | grep "infisical run"
    cat .infisical.json | grep workspaceId
  </verify>
  <done>
    .infisical.json exists with a real UUID workspaceId (not a placeholder string). package.json dev:server and dev:client both start with "infisical run --env=dev --". staging:up script exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ci.yml with OIDC injection + clean .env.example + update README</name>
  <files>.github/workflows/ci.yml, .env.example, README.md</files>
  <action>
    Read 37.2-01-SUMMARY.md for INFISICAL_IDENTITY_ID and INFISICAL_PROJECT_SLUG values.

    **1. Update .github/workflows/ci.yml**

    Add top-level `permissions` block after `on:` (required for OIDC token request):
    ```yaml
    permissions:
      id-token: write
      contents: read
    ```

    For the `test` job:
    - REMOVE all placeholder env vars from the job-level `env:` block EXCEPT:
      - DATABASE_URL (CI service container address — must stay)
      - NODE_ENV: test (must stay)
    - Remove from env block: BETTER_AUTH_SECRET, BETTER_AUTH_URL, APP_URL, RESEND_API_KEY, RESEND_FROM_EMAIL, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRO_PRICE_ID, RESEND_AUDIENCE_ID, IP_HASH_SALT, LOOPS_API_KEY
    - ADD the Infisical secrets-action step as the SECOND step (after `actions/checkout@v4`, before `actions/setup-node@v4`):
    ```yaml
    - uses: Infisical/secrets-action@v1.0.12
      with:
        method: "oidc"
        identity-id: ${{ vars.INFISICAL_IDENTITY_ID }}
        domain: "https://app.infisical.com"
        project-slug: "<INFISICAL_PROJECT_SLUG from 37.2-01-SUMMARY.md>"
        env-slug: "dev"
        export-type: "env"
    ```

    For the `e2e` job:
    - REMOVE all placeholder env vars from the job-level `env:` block EXCEPT:
      - DATABASE_URL (CI service container address — must stay)
      - REDIS_URL: redis://localhost:6379 (CI service container address — must stay)
      - NODE_ENV: test (must stay)
    - Remove from env block: BETTER_AUTH_SECRET, BETTER_AUTH_URL, APP_URL, RESEND_API_KEY, RESEND_FROM_EMAIL, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRO_PRICE_ID, RESEND_AUDIENCE_ID, IP_HASH_SALT
    - (Note: LOOPS_API_KEY is missing from current e2e env block — this omission is automatically fixed because Infisical injects all dev secrets including LOOPS_API_KEY)
    - ADD the same Infisical secrets-action step as the SECOND step (after actions/checkout@v4, before actions/setup-node@v4):
    ```yaml
    - uses: Infisical/secrets-action@v1.0.12
      with:
        method: "oidc"
        identity-id: ${{ vars.INFISICAL_IDENTITY_ID }}
        domain: "https://app.infisical.com"
        project-slug: "<INFISICAL_PROJECT_SLUG from 37.2-01-SUMMARY.md>"
        env-slug: "dev"
        export-type: "env"
    ```

    The `lint` job and `docker-build` job must NOT be modified.

    Note: `${{ vars.INFISICAL_IDENTITY_ID }}` uses GitHub Actions `vars` (repository variable, non-sensitive) not `secrets`. The user will configure this variable in GitHub in Plan 03.

    **2. Clean .env.example** — Remove all values, keep only key names and comments.

    Rewrite .env.example so every key has no value (just `KEY=`), retaining all comments explaining each key. For optional/commented-out keys (FORCE_HTTPS, REDIS_URL, OAuth keys, VITE_ keys), keep them commented out as documentation.

    Final format example:
    ```
    DATABASE_URL=
    PORT=
    LOG_LEVEL=
    NODE_ENV=

    # Force HTTPS redirect (set true behind TLS-terminating reverse proxy)
    # FORCE_HTTPS=true

    # Optional: Redis URL for distributed rate limiting (multi-instance deployments)
    # REDIS_URL=

    # === Authentication ===
    # Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
    BETTER_AUTH_SECRET=
    BETTER_AUTH_URL=
    # Base URL for the application (used in OAuth callbacks and email links).
    APP_URL=
    # Required if using portless dev setup — set to the portless hostname:port
    BETTER_AUTH_TRUSTED_ORIGINS=

    # Google OAuth (optional — comment out to disable Google sign-in)
    # GOOGLE_CLIENT_ID=
    # GOOGLE_CLIENT_SECRET=

    # GitHub OAuth (optional — comment out to disable GitHub sign-in)
    # GITHUB_CLIENT_ID=
    # GITHUB_CLIENT_SECRET=

    # Email delivery via Resend
    RESEND_API_KEY=
    RESEND_FROM_EMAIL=

    # === Analytics ===
    # PostHog project API key — obtain from posthog.com
    # VITE_POSTHOG_KEY=
    # VITE_POSTHOG_HOST=

    # === Stripe Billing ===
    STRIPE_SECRET_KEY=
    STRIPE_WEBHOOK_SECRET=
    STRIPE_PRO_PRICE_ID=

    # Email Capture
    RESEND_AUDIENCE_ID=
    # Salt for SHA-256 IP hashing — generate with: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
    IP_HASH_SALT=

    # === Email Onboarding (Loops.so) ===
    LOOPS_API_KEY=
    ```

    **3. Update README.md Getting Started section**

    Replace the "### Manual setup" block (lines starting with "### Manual setup" through "Visit http://localhost:5173") with:

    ```markdown
    ### Local Development (Infisical)

    1. **Install Infisical CLI:** `brew install infisical/get-cli/infisical` (macOS) or see [infisical.com/docs/cli/installation](https://infisical.com/docs/cli/installation) for Linux
    2. **Authenticate:** `infisical login` (opens browser for one-time setup)
    3. **Start servers:**
       - Terminal 1: `npm run dev:server`
       - Terminal 2: `npm run dev:client`

    Secrets are pulled automatically from Infisical's `dev` environment. No `.env` file needed.

    ### Docker Staging

    ```bash
    npm run staging:up    # infisical run --env=staging -- docker-compose up
    ```

    Requires: `infisical login` completed. Pulls secrets from Infisical's `staging` environment.

    Visit [http://localhost:3000](http://localhost:3000).
    ```

    Also update the "### Docker (recommended)" block to remove the `cp .env.example .env` line (no longer needed):

    Change:
    ```bash
    git clone https://github.com/norcalcoop/secureshare.git torch-secret
    cd torch-secret
    cp .env.example .env
    docker compose up
    ```
    To:
    ```bash
    git clone https://github.com/norcalcoop/secureshare.git torch-secret
    cd torch-secret
    npm run staging:up    # requires infisical login first
    ```

    Also update the CLAUDE.md Prerequisites line from:
    "Prerequisites: PostgreSQL 17+ at DATABASE_URL (see .env.example). Optional REDIS_URL..."
    to:
    "Prerequisites: Infisical CLI installed and authenticated (infisical login). PostgreSQL 17+ service running."

    Do NOT change CLAUDE.md — it is the project instructions file. Only README.md is in scope.
  </action>
  <verify>
    grep -c "infisical" .github/workflows/ci.yml
    grep "BETTER_AUTH_SECRET" .github/workflows/ci.yml | grep -v "BETTER_AUTH_URL" | grep -v "#" | wc -l
    grep -v "^#" .env.example | grep "=" | grep -v "=$" | grep -v "^$" | wc -l
    grep "infisical login" README.md
  </verify>
  <done>
    ci.yml test and e2e jobs both have `Infisical/secrets-action@v1.0.12` step and no BETTER_AUTH_SECRET in their env blocks. `grep -c "BETTER_AUTH_SECRET" .github/workflows/ci.yml` returns 0. .env.example has no key=value pairs (all values stripped). README Getting Started shows Infisical login as step 2.
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:
- `grep "infisical run" package.json` — shows dev:server, dev:client, staging:up
- `cat .infisical.json` — shows real workspaceId UUID
- `grep -c "BETTER_AUTH_SECRET:" .github/workflows/ci.yml` — returns 0 (removed from env blocks)
- `grep -c "Infisical/secrets-action" .github/workflows/ci.yml` — returns 2 (test + e2e jobs)
- `grep -v "^#" .env.example | grep "=[^$]" | wc -l` — returns 0 (no values in .env.example)
- `npx vitest run --reporter=dot` — all tests pass (script changes don't affect test runner)
</verification>

<success_criteria>
- .infisical.json committed with real workspaceId
- package.json dev scripts wrapped with infisical run prefix; staging:up script added
- ci.yml test and e2e jobs use OIDC secrets-action; only DATABASE_URL / REDIS_URL / NODE_ENV remain as job-level env overrides
- .env.example is keys-only documentation (no values)
- README onboarding steps describe Infisical CLI flow
- All 328+ existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/37.2-get-the-most-out-of-infiscal-free-tier-integration/37.2-02-SUMMARY.md`
</output>
