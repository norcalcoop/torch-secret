---
phase: 31-rebrand-tech-debt
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/app.ts
  - server/src/db/schema.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TECH-02
  - TECH-03

must_haves:
  truths:
    - "GET /privacy and GET /terms return X-Robots-Tag: noindex, nofollow response header"
    - "schema.ts zero-knowledge comment lists all 7 enforcement points matching INVARIANTS.md"
    - "ROADMAP.md Phase 31 success criterion #5 reads '7 enforcement points', not '6'"
    - "REQUIREMENTS.md TECH-03 description is updated to reflect 7 (not 6) enforcement points"
  artifacts:
    - path: "server/src/app.ts"
      provides: "Extended NOINDEX_PREFIXES array"
      contains: "'/privacy'"
    - path: "server/src/db/schema.ts"
      provides: "Updated zero-knowledge enforcement comment"
      contains: "Rate limits + prompts"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected success criterion count"
      contains: "7 enforcement points"
  key_links:
    - from: "server/src/app.ts"
      to: "NOINDEX_PREFIXES middleware"
      via: "res.setHeader('X-Robots-Tag', 'noindex, nofollow')"
      pattern: "'/privacy'.*'/terms'"
    - from: "server/src/db/schema.ts"
      to: ".planning/INVARIANTS.md"
      via: "inline comment listing enforcement points"
      pattern: "Rate limits.*prompts"
---

<objective>
Clear three targeted tech debt items: add `/privacy` and `/terms` to the server-side NOINDEX_PREFIXES array so those routes return an `X-Robots-Tag: noindex, nofollow` header; update the zero-knowledge inline comment in schema.ts to list all 7 enforcement points matching the canonical INVARIANTS.md table; and correct the stale "6 enforcement points" count in ROADMAP.md and REQUIREMENTS.md to "7".

Purpose: These are small surgical changes that clear known gaps before new feature code is written. The NOINDEX fix makes the server authoritative for crawl exclusion (client-side `<meta>` alone is insufficient for search engines). The schema comment fix ensures the comment matches reality after phases 23-27 added four more enforcement points.
Output: Server returns correct noindex headers for /privacy and /terms; schema.ts comment is canonical and complete; planning docs are accurate.
</objective>

<execution_context>
@/Users/ourcomputer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ourcomputer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-rebrand-tech-debt/31-CONTEXT.md
@.planning/phases/31-rebrand-tech-debt/31-RESEARCH.md
@.planning/INVARIANTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /privacy and /terms to NOINDEX_PREFIXES in server/src/app.ts</name>
  <files>
    server/src/app.ts
  </files>
  <action>
    Read `server/src/app.ts`. Find the `NOINDEX_PREFIXES` array (currently lines 111-118 per research). It contains:
    ```typescript
    const NOINDEX_PREFIXES = [
      '/secret/',
      '/login',
      '/register',
      '/forgot-password',
      '/reset-password',
      '/dashboard',
    ];
    ```

    Add `/privacy` and `/terms` to the end of the array:
    ```typescript
    const NOINDEX_PREFIXES = [
      '/secret/',
      '/login',
      '/register',
      '/forgot-password',
      '/reset-password',
      '/dashboard',
      '/privacy',
      '/terms',
    ];
    ```

    Do NOT change anything else in app.ts. The existing `res.setHeader('X-Robots-Tag', 'noindex, nofollow')` call already handles the response — only the array needs extending.
  </action>
  <verify>
    Run: `grep -A 12 "NOINDEX_PREFIXES" server/src/app.ts` — output must include both '/privacy' and '/terms' entries.
    Manual check: `curl -s -I http://localhost:3000/privacy | grep -i x-robots` should return `X-Robots-Tag: noindex, nofollow` when server is running. (Record expected result in SUMMARY; live test requires running server.)
  </verify>
  <done>
    NOINDEX_PREFIXES array contains '/privacy' and '/terms'. Any request to GET /privacy or GET /terms will receive the X-Robots-Tag: noindex, nofollow response header from the server middleware.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update schema.ts zero-knowledge comment to 7 enforcement points; fix ROADMAP.md and REQUIREMENTS.md counts</name>
  <files>
    server/src/db/schema.ts
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    **server/src/db/schema.ts:**
    Read the file. Find the zero-knowledge invariant block comment at the top of the schema (currently lists 3 enforcement points: DB, Logger, Analytics). Replace the bullet-point list inside the comment with this corrected 7-point list (matching the canonical table in `.planning/INVARIANTS.md` exactly):

    ```
     *   DB — secrets table:       secrets.user_id is nullable FK; secrets.id is never stored in users, sessions, or accounts rows
     *   DB — users table:         No secret_id or last_secret_id column. User rows contain no secret identifiers.
     *   Logger:                   server/src/middleware/logger.ts redacts secret IDs from URL paths via regex
     *   Analytics:                PostHog sanitize_properties must strip URL fragments before any event fires — Phase 25
     *   Logger — dashboard route: redactUrl regex extended to cover /api/dashboard/secrets/:id paths — Phase 23
     *   Email (Resend):           notification email body contains only viewed-at timestamp; no secretId, label, or ciphertext — Phase 26
     *   Rate limits + prompts:    429 responses and conversion prompt events contain no userId or secretId — Phase 27
    ```

    Preserve the surrounding comment block structure (opening `/**`, closing `*/`, and any preamble text about the invariant rule). Only replace the bullet-point enforcement list. Do not alter any TypeScript code in schema.ts — this is comment-only.

    **`.planning/ROADMAP.md`:**
    Read the file. Find Phase 31 success criterion #5 which reads (approximately):
    `"The zero-knowledge inline comment in schema.ts lists all 6 enforcement points matching the canonical list in INVARIANTS.md"`
    Change `6` → `7`.

    Also verify the Phase 31 `**Requirements:**` line lists TECH-03 (it should already — just verify).

    **`.planning/REQUIREMENTS.md`:**
    Read the file. Find the TECH-03 requirement entry which reads:
    `**TECH-03**: CI workflow includes placeholder env vars... (or the schema.ts description)`
    The TECH-03 description text says "canonical 6-point list" — update to read "canonical 7-point list" to match the actual count.
  </action>
  <verify>
    Run: `grep -A 12 "enforcement" server/src/db/schema.ts` — output must show all 7 bullet points including "Rate limits + prompts" (the last one added in Phase 27).
    Run: `grep "7 enforcement\|7-point" .planning/ROADMAP.md .planning/REQUIREMENTS.md` — both files must show "7" not "6".
    Run: `grep "6 enforcement\|6-point" .planning/ROADMAP.md .planning/REQUIREMENTS.md` — must return zero results (the stale "6" is gone).
  </verify>
  <done>
    schema.ts zero-knowledge comment lists all 7 enforcement points in correct format matching INVARIANTS.md. ROADMAP.md success criterion #5 and REQUIREMENTS.md TECH-03 description both read "7 enforcement points". No stale "6" count remains in either planning document.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "'/privacy'\|'/terms'" server/src/app.ts` — both entries present in NOINDEX_PREFIXES.
2. `grep -c "DB — secrets table\|DB — users table\|Logger:\|Analytics:\|Logger — dashboard\|Email (Resend)\|Rate limits" server/src/db/schema.ts` — must return 7 (all enforcement points present).
3. `grep "enforcement" .planning/ROADMAP.md` — must show "7 enforcement points".
4. Run full lint check: `npm run lint` — must pass (comment-only and array changes should not introduce lint errors).
</verification>

<success_criteria>
- server/src/app.ts NOINDEX_PREFIXES contains '/privacy' and '/terms'
- server/src/db/schema.ts zero-knowledge comment has exactly 7 bullet points matching the INVARIANTS.md canonical table
- .planning/ROADMAP.md Phase 31 success criterion #5 reads "7 enforcement points"
- .planning/REQUIREMENTS.md TECH-03 description reads "7-point list" (no stale "6" count)
- npm run lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/31-rebrand-tech-debt/31-03-SUMMARY.md`
</output>
